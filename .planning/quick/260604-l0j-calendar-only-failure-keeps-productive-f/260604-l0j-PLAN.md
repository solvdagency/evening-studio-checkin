---
phase: quick-260604-l0j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/render/cards.ts
  - src/render/renderMessage.ts
  - src/index.ts
  - src/__tests__/runNightly.test.ts
  - src/render/__tests__/renderMessage.test.ts
autonomous: true
requirements: [REL-01, MEET-04]

must_haves:
  truths:
    - "A Calendar-only failure (Productive ok) posts the NORMAL card with real figures, not the 🤖 degraded card."
    - "A Calendar-only failure adds exactly ONE muted note ('couldn't check calendars tonight') and NO 📅 worth-a-look lines."
    - "A Productive/figures failure still posts the 🤖 degraded card and returns 0 (REL-01 preserved)."
    - "No card ever contains 'GOOGLE_SA_KEY', raw calendar error text, or a doubled 'Couldn't reach' prefix."
    - "The run still posts and returns 0 on a Calendar-only failure."
  artifacts:
    - path: "src/render/cards.ts"
      provides: "calendarUnavailable presentation-only flag on RenderContext"
      contains: "calendarUnavailable"
    - path: "src/render/renderMessage.ts"
      provides: "single muted calendars-unavailable note in the normal-card path only"
      contains: "calendarUnavailable"
    - path: "src/index.ts"
      provides: "ctx.sourceErrors = Productive-only; calendarUnavailable set from cal.sourceErrors"
  key_links:
    - from: "src/index.ts buildRenderContext"
      to: "RenderContext.calendarUnavailable"
      via: "cal.sourceErrors.length > 0"
      pattern: "calendarUnavailable"
    - from: "src/render/renderMessage.ts normal card path"
      to: "ctx.calendarUnavailable"
      via: "conditional muted textParagraph"
      pattern: "ctx\\.calendarUnavailable"
---

<objective>
Fix degraded-card behaviour so a Calendar-only failure keeps the Productive figures plus one clean muted note, instead of collapsing the whole card to the 🤖 degraded variant and leaking the GOOGLE_SA_KEY error with a doubled "Couldn't reach" prefix.

Purpose: Surfaced in the Phase 04 live pilot — injecting an invalid GOOGLE_SA_KEY dropped the good Productive figures and printed "Couldn't reach Couldn't reach Calendar for Anisha Gittins: GOOGLE_SA_KEY missing client_email/private_key …" per designer. The card must stay trustworthy when only meetings are unavailable.

Output: Calendar and Productive degrade independently. Productive failure → 🤖 degraded card (unchanged, REL-01). Calendar-only failure → normal card with real figures, no 📅 lines, one muted note, exit 0. Both fail → 🤖 degraded card with no calendar noise.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@src/index.ts
@src/render/cards.ts
@src/render/renderMessage.ts
@src/render/variants.ts
@src/render/rows.ts
@src/calendar/gather.ts
@src/__tests__/runNightly.test.ts
@src/render/__tests__/renderMessage.test.ts

<interfaces>
<!-- Key contracts already in the codebase. Use these directly. -->

From src/render/cards.ts (RenderContext — add ONE field, presentation-only):
```typescript
export interface RenderContext {
  designerNames: Record<string, string>;
  sourceErrors: string[];        // Non-empty ⇒ degraded variant (D-18)
  briefFlags: BriefFlag[];
  tentativeNotes: Record<string, TentativeNote>;
  worthALook?: Record<string, Array<{ title: string; start: string; link: string }>>;
  holidayTomorrow?: { dateLabel: string };
  closureTomorrow?: { backDayLabel: string };
  leaveNotes?: Record<string, string>;
  header: { subtitle: string; targetDate: string };
}
```

From src/render/variants.ts (UNCHANGED — already keys on sourceErrors only):
```typescript
export function selectVariant(report, ctx): Variant {
  if (ctx.holidayTomorrow) return "holiday";
  if (ctx.closureTomorrow) return "closure";
  if (ctx.sourceErrors.length > 0) return "degraded";  // becomes Productive-only once index stops merging
  return "card";
}
```

From src/index.ts (the merge to fix, ~line 209-215):
```typescript
const ctx = buildRenderContext(
  report,
  [...g.sourceErrors, ...cal.sourceErrors],  // ← stop merging cal.sourceErrors
  g.briefFlags,
  g.holidays,
  worthALook,
);
```
buildRenderContext signature (src/index.ts ~line 94) currently takes
(report, sourceErrors, briefFlags, holidays, worthALook).

From src/calendar/gather.ts:
```typescript
export interface CalendarResult {
  eventsByDesigner: Record<DesignerId, FilteredEvent[]>;
  sourceErrors: string[];  // full sentences: "Couldn't reach Calendar for {name}: {reason}"
}
```

From src/render/rows.ts (the muted sub-line pattern to mirror):
```typescript
function muted(text: string): string {
  return `<font color="${BRAND_COLORS.muted}">${text}</font>`;
}
```
BRAND_COLORS.muted is imported in renderMessage.ts already.

From src/render/__tests__/renderMessage.test.ts (the ctx test helper, ~line 60):
```typescript
function ctx(over: Partial<RenderContext>): RenderContext { /* base + overrides */ }
```
loadFixture("degraded") / loadFixture("clean") etc. read locked card JSON from ./fixtures.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split calendar degrade from figures degrade — add calendarUnavailable</name>
  <files>src/render/cards.ts, src/index.ts</files>
  <behavior>
    - buildRenderContext: ctx.sourceErrors contains ONLY g.sourceErrors (Productive/figures). cal.sourceErrors is NOT merged.
    - buildRenderContext: ctx.calendarUnavailable === true when cal.sourceErrors is non-empty, undefined/false otherwise.
    - selectVariant returns "card" (not "degraded") when only Calendar failed (g.sourceErrors empty, cal.sourceErrors non-empty) — verified indirectly via the render test in Task 2 and the runNightly test in Task 3.
  </behavior>
  <action>
    In src/render/cards.ts: add ONE presentation-only optional field to the RenderContext interface — `calendarUnavailable?: boolean`. Document it inline in the per-field style of the surrounding fields: "Set when the Calendar source failed but the figures are intact (REL-01). Drives a single muted note in the NORMAL card; NEVER a top-level variant — figures stay trusted. Carries no raw error text (the GOOGLE_SA_KEY/per-designer detail stays in console logging only, T-03-09 posture)." Do NOT touch the Cards v2 widget types.

    In src/index.ts buildRenderContext: stop the merge — pass the Productive-only list to ctx.sourceErrors. Add a parameter `calendarUnavailable: boolean` to buildRenderContext (place it after `worthALook`), and set `ctx.calendarUnavailable = calendarUnavailable` on the returned ctx ONLY when true (mirror the conditional-assign style of closureTomorrow/holidayTomorrow — keep it absent when false so existing snapshot fixtures with no field stay byte-identical).

    At the call site (~line 209): change the second arg from `[...g.sourceErrors, ...cal.sourceErrors]` to `g.sourceErrors`, and add a final arg `cal.sourceErrors.length > 0`. Update the inline comment block above the call (lines 204-208) so it no longer says calendar errors are concatenated into the degrade list — state the new rule: Productive/figures failure → degraded card; Calendar-only failure → normal card + a muted calendars-unavailable note, figures intact; both fail → degraded (Productive dominates), no calendar noise. Keep raw calendar error detail in console logging only — if index.ts does not already log cal.sourceErrors, add a single guarded `console.warn` of the verbatim cal.sourceErrors (NEVER into the card, NEVER the webhook URL) so the operator still sees the GOOGLE_SA_KEY reason in the Actions log.

    Trust boundary: no hour math added; runNightly stays the single clock read; do not touch deterministic capacity/report/clock code.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>RenderContext has `calendarUnavailable?: boolean`; buildRenderContext sets ctx.sourceErrors from g.sourceErrors only and threads calendarUnavailable from cal.sourceErrors; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render one muted calendars-unavailable note in the normal-card path only</name>
  <files>src/render/renderMessage.ts, src/render/__tests__/renderMessage.test.ts</files>
  <behavior>
    - calendarUnavailable=true + a normal "card" variant → the payload contains exactly ONE muted note (e.g. "couldn't check calendars tonight — meeting flags skipped") and NO 📅 line.
    - calendarUnavailable falsy → no such note (existing fixtures byte-identical; clean and busy snapshots unchanged).
    - The note is NOT emitted on the degraded / holiday / closure paths (those return before the normal-card assembly).
    - The degraded card NEVER contains "GOOGLE_SA_KEY", raw calendar error text, or a doubled "Couldn't reach" prefix (it now renders Productive-only sourceErrors).
  </behavior>
  <action>
    In src/render/renderMessage.ts, in the `renderTemplate` "card" path only (after the holiday/closure/degraded early returns, i.e. in the section-assembly block ~line 120+): when `ctx.calendarUnavailable` is truthy, push ONE muted textParagraph widget into the verdict section (append it to verdictWidgets so it sits with the status line, before the rows). Use the existing muted colour: `<font color="${BRAND_COLORS.muted}">couldn't check calendars tonight — meeting flags skipped</font>`. Keep the copy soft and non-technical (CLAUDE.md voice; D-04 soft voice) and contain NO error text, NO designer names, NO key reference. Add NOTHING to renderDegraded/renderHoliday/renderClosure. Because index now passes calendarUnavailable independent of worthALook, a calendar-only failure yields empty worthALook → no 📅 lines automatically; do not special-case it.

    In src/render/__tests__/renderMessage.test.ts, add a describe block "renderTemplate — calendar-unavailable note (REL-01, figures intact)" using the existing `ctx({...})` helper:
      - it: calendarUnavailable=true on a normal busy/clean report → JSON includes the exact muted note text AND does NOT include "📅". Assert the variant is the normal card (e.g. the payload still has the week-bar/button sections — assert the JSON includes "Open in Productive", which the degraded card never renders).
      - it: calendarUnavailable absent/false → JSON does NOT include "couldn't check calendars".
      - it: the existing degraded fixture path still renders Productive-only — extend the existing degraded describe (or add an it) asserting the degraded payload JSON never contains "GOOGLE_SA_KEY", never contains "Couldn't reach Couldn't reach", and never contains raw calendar error text. (Pass ctx.sourceErrors a Productive label only, as production now does.)
    Keep all existing snapshot assertions untouched and green.
  </action>
  <verify>
    <automated>node --import tsx --test "src/render/__tests__/renderMessage.test.ts"</automated>
  </verify>
  <done>New render tests pass; the muted note appears only in the normal card; degraded card is Productive-only with no leaked key/error/double-prefix; all prior render snapshots still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update orchestration tests — calendar-only stays a normal card, productive still degrades</name>
  <files>src/__tests__/runNightly.test.ts</files>
  <behavior>
    - gatherCalendar-only failure (gather ok) → runNightly posts the NORMAL card (figures present, "Open in Productive" present, the muted calendars-unavailable note present), NO 📅 line, returns 0, posts exactly once.
    - gather (Productive) failure → runNightly posts the 🤖 degraded card and returns 0 (REL-01) — and the posted payload never contains "GOOGLE_SA_KEY" or raw calendar text.
    - Happy path (case a) and post-fail (case c) stay green unchanged.
  </behavior>
  <action>
    In src/__tests__/runNightly.test.ts, rewrite the existing case (b) "degrade: a calendar sourceError still posts the degraded card": it must now assert a calendar-only failure yields the NORMAL card. Drive runNightly with `gather: stubGatherResult()` (no errors) and `gatherCalendar: stubCalendarResult(["Couldn't reach Calendar for Liam Mills: GOOGLE_SA_KEY missing client_email/private_key"])`. Assert: code === 0; posted exactly once; the posted JSON includes the muted note "couldn't check calendars" (match the substring used in Task 2); the posted JSON does NOT include "📅"; the posted JSON includes "Open in Productive" (proves it is the normal card, not degraded); the posted JSON does NOT include "GOOGLE_SA_KEY" and does NOT include "Couldn't reach Couldn't reach".

    ADD a new case "(d) productive failure still posts the 🤖 degraded card and returns 0 (REL-01)": drive with `gather: stubGatherResult(["Couldn't reach Productive: 403"])` and a clean `gatherCalendar: stubCalendarResult()`. Assert: code === 0; posted once; the posted JSON includes "🤖" (degraded marker) and does NOT include "Open in Productive" (degraded has no button); the posted JSON does NOT include "GOOGLE_SA_KEY".

    Note: stubGatherResult/stubCalendarResult already accept a sourceErrors array (see the existing helpers) — reuse them, do not add new stub builders. Leave cases (a) happy and (c) post-fail exactly as they are. Update the file-header docblock bullet (b) to describe the new calendar-only-normal-card behaviour and add a (d) line for the productive-degrade case so the header matches the tests.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Full suite green (a few more than 221 tests); calendar-only path posts the normal card with the note + no 📅 + exit 0; productive failure still posts the 🤖 degraded card + exit 0; no leaked key/double-prefix anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calendar source → card | Calendar error strings (incl. GOOGLE_SA_KEY reason) must NOT reach the posted card |
| env credential → logs/card | The SA key reason may appear in the Actions console log, never in the Chat message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-L0J-01 | Information disclosure | renderMessage / index buildRenderContext | mitigate | calendarUnavailable is a boolean only; raw cal.sourceErrors (with the GOOGLE_SA_KEY reason) never enters RenderContext or the card — kept to console logging. Tests assert the posted JSON never contains "GOOGLE_SA_KEY" or raw calendar error text. |
| T-L0J-02 | Tampering (integrity of figures) | variants/selectVariant | mitigate | Calendar failure no longer triggers the figures-degrade variant; real Productive figures stay shown (REL-01 trust). No package installs in this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (Node 22).
- `npm test` fully green, total > 221.
- Posted card on a calendar-only failure: normal figures + one muted note + no 📅 + no "GOOGLE_SA_KEY" + no doubled "Couldn't reach".
- Posted card on a Productive failure: 🤖 degraded card, exit 0 (REL-01 unchanged).
- src/calendar/__tests__/reconcile-render.e2e.test.ts stays green.
</verification>

<success_criteria>
- Calendar-only failure → NORMAL card with real Productive figures, NO 📅 lines, ONE muted "couldn't check calendars" note, posts, exit 0.
- Productive/figures failure → 🤖 degraded card, exit 0 (REL-01 preserved).
- Both fail → 🤖 degraded card, no calendar noise.
- No card contains GOOGLE_SA_KEY, raw calendar error detail, or a doubled "Couldn't reach" prefix.
- Only these files touched: src/index.ts, src/render/cards.ts, src/render/renderMessage.ts, and the two test files. Deterministic capacity/report/clock code untouched. variants.ts unchanged (already keys on sourceErrors only).
- `npm test` green (>221) and `npx tsc --noEmit` clean.
</success_criteria>

<output>
Create `.planning/quick/260604-l0j-calendar-only-failure-keeps-productive-f/260604-l0j-SUMMARY.md` when done.
</output>
