---
phase: 06-designer-working-day-availability
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/index.ts
  - src/render/rows.ts
  - src/render/cards.ts
  - src/__tests__/runNightly.test.ts
autonomous: false
requirements: [CAP-06]
must_haves:
  truths:
    - "runNightly threads gather's rosteredMinutes lookup into computeStudioReport so the live working-day pattern drives the posted card"
    - "A routine non-working day reads sensibly (e.g. \"not in <day>\") rather than literally \"on leave\", with no new domain status value (D-04, D-05)"
    - "An availability-unreadable designer renders through the existing 🤖 \"couldn't read\" missing-designer row (no new variant) (D-06)"
    - "End-to-end: a Wed/Fri target day shows Anisha mentioned with no open-time flag, and the studio rollup reflects her real working days"
  artifacts:
    - path: "src/index.ts"
      provides: "rosteredMinutes wired into StudioReportInput + a routine-not-rostered leaveNotes derivation"
      contains: "rosteredMinutes"
    - path: "src/render/rows.ts"
      provides: "D-05 non-working-day wording for the \"off\" status (not literally 'on leave')"
      contains: "off"
  key_links:
    - from: "src/index.ts buildRenderContext"
      to: "src/render/rows.ts statusLine off branch"
      via: "leaveNotes / routine-not-rostered copy distinguishes a routine off-day from booked leave"
      pattern: "leaveNotes"
    - from: "src/index.ts runNightly"
      to: "src/domain/report.ts StudioReportInput.rosteredMinutes"
      via: "g.rosteredMinutes passed into computeStudioReport"
      pattern: "rosteredMinutes: g.rosteredMinutes"
---

<objective>
Close the CAP-06 loop end-to-end: wire gather's `rosteredMinutes` lookup into `computeStudioReport` at the composition root, give a routine non-working day sensible card wording ("not in <day>") without adding a domain status, and confirm an availability-unreadable designer renders through the existing 🤖 "couldn't read" row. Then verify the real posted card on a Wed/Fri target day (Anisha mentioned, never flagged) via a manual smoke check.

Purpose: This is the user-visible payoff of CAP-06. After this plan, the nightly card stops mis-flagging a non-standard week as open time, and the studio keeps trusting the numbers (SC-2, SC-3, SC-4 surfaced; SC-5 maintained).

Output: `runNightly` passes `rosteredMinutes` into the report; the renderer's `"off"` branch + a routine-not-rostered note read sensibly; runNightly tests pin the wiring and the degraded path; a human confirms the posted card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-designer-working-day-availability/06-CONTEXT.md
@.planning/phases/06-designer-working-day-availability/06-PATTERNS.md
@.planning/phases/06-designer-working-day-availability/06-01-SUMMARY.md
@.planning/phases/06-designer-working-day-availability/06-02-SUMMARY.md

<interfaces>
<!-- Exact contracts the executor wires. No exploration needed. -->

From Plan 02 (src/productive/gather.ts):
- `GatherResult.rosteredMinutes: (designerId: DesignerId, dateKey: string) => number`.
- `GatherResult.assessedDesigners` now excludes availability-unreadable designers (D-06).

From Plan 01 (src/domain/report.ts):
- `StudioReportInput.rosteredMinutes` is REQUIRED. runNightly's input object (src/index.ts lines 206-213) MUST set it or the build fails.

From src/index.ts (the composition root):
- `StudioReportInput` built at lines 206-213: `{ now, holidays, roster, bookings, absences, assessedDesigners }` — ADD `rosteredMinutes: g.rosteredMinutes`.
- `buildRenderContext(report, sourceErrors, briefFlags, holidays, worthALook, calendarUnavailable)` (lines 95-144) builds RenderContext; `leaveNotes` is currently left empty (comment lines 85-87). This is where a routine-not-rostered note is derived.
- A 🤖 missing-designer is already rendered via `report.missingDesigners` → buildRow → missingDesignerRow (rows.ts lines 81-88, 116-118) — verify, do NOT add a variant.

From src/render/rows.ts:
- `statusLine(d, escapedName)` `"off"` branch (lines 62-64): currently `⚪ {name} — {muted("on leave / Full day off.")}`.
- `buildRow` `"off"` short-circuit (lines 124-126): minimal one-line row, returns before booked/flags.
- `escapeHtml` (lines 25-27), `muted` (lines 35-37).
- `leaveNotes?: Record<string, string>` is read in buildRow (lines 132-135) but ONLY on the non-off path (after the off short-circuit). NOTE: a routine-not-rostered designer IS status "off", so the current leaveNote read never fires for them — the wording fix must live in the off branch itself OR the off short-circuit must consult a routine-not-rostered note.

From src/render/cards.ts:
- `RenderContext.leaveNotes?: Record<string, string>` (lines 188-195) — presentation-only, domain untouched. The established channel for off-day wording detail.

From src/config.ts:
- `DESIGNER_NAMES` keyed by person id; `STUDIO_ZONE` via domain/types.

Live reality for the smoke check (06-CONTEXT): Anisha (686712) off Wed & Fri. A Tuesday or Thursday run targets Wed; a Thursday run targets Fri — both make Anisha a routine-not-rostered "off" day.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire rosteredMinutes into runNightly + routine-not-rostered wording (D-05)</name>
  <read_first>
    - src/index.ts (StudioReportInput build lines 206-213; buildRenderContext lines 95-144; the leaveNotes-empty comment lines 85-87)
    - src/render/rows.ts (statusLine off branch lines 54-66; buildRow off short-circuit lines 102-126; leaveNotes read lines 132-135)
    - src/render/cards.ts (RenderContext.leaveNotes lines 188-195)
    - src/domain/report.ts (StudioReportInput.rosteredMinutes from Plan 01)
    - src/__tests__/runNightly.test.ts (the existing runNightly test harness + stub style)
  </read_first>
  <behavior>
    - runNightly's StudioReportInput includes rosteredMinutes from gather; with a stub gather whose rosteredMinutes returns 0 for one designer on the target day, that designer renders "off" in the posted payload.
    - A routine-not-rostered "off" designer's card line reads "not in <Weekday>" (or equivalent non-leave wording), NOT literally "on leave / Full day off."
    - A designer in report.missingDesigners (availability unreadable) still renders the 🤖 "couldn't read" row (no regression, no new variant).
    - Existing runNightly tests stay green when their stub gather is given a `rosteredMinutes: () => 450` default (standard week unchanged).
  </behavior>
  <action>
    src/index.ts — in the StudioReportInput object (lines 206-213) add `rosteredMinutes: g.rosteredMinutes,`. This is the single load-bearing wiring line that makes the live working-day pattern drive the report.

    Routine-not-rostered wording (D-05) — decide the cleanest channel and implement it WITHOUT adding a domain status:
    - In `buildRenderContext` (src/index.ts), derive a per-designer routine-not-rostered note. For each `report.designers` entry whose `status === "off"` AND whose target-day rostered minutes are 0 AND who has zero absence (i.e. a routine non-working day, not booked leave), set a `leaveNotes[designerId]` value of the form `"not in {Weekday}"` where Weekday is the target day's `cccc` formatted via luxon in STUDIO_ZONE (reuse the `subtitleFor` luxon pattern, lines 75-79). Booked full-day leave (off because absence filled the day) keeps the existing "on leave" treatment.
    - Because buildRow currently short-circuits the "off" status BEFORE reading leaveNotes (rows.ts lines 124-126), update the rows.ts off path so the off row uses the routine-not-rostered wording when provided: pass leaveNotes into buildRow's off branch and have `statusLine`'s off branch (or the off short-circuit) emit `⚪ {name} — {muted(escapeHtml(routineNote))}` when a routine note exists for that designer, else the existing `muted("on leave / Full day off.")`. Keep the ⚪ emoji + muted treatment + minimal one-line row (D-22). Escape the dynamic day name via escapeHtml.

    Determining "routine vs booked leave" cleanly: the domain `DesignerResult` does not carry absence vs rostered separately (availableMin is the combined floor). The reliable signal available at buildRenderContext is gather's `rosteredMinutes(designerId, targetDay) === 0` (routine off) vs `> 0 but absence consumed it` (booked leave). Compute the routine flag in buildRenderContext using `g.rosteredMinutes` — so the renderer stays a pure consumer of the RenderContext note and src/domain stays untouched (PATTERNS.md lines 233). Thread `g.rosteredMinutes` (or the derived notes) into buildRenderContext's signature as needed.

    src/__tests__/runNightly.test.ts — update existing stub gather results to include `rosteredMinutes: () => 450` so current assertions stay green. Add cases: (a) a stub gather with a designer rostered 0 on the target day → assert the rendered payload contains that designer as "off" with the "not in <day>" wording and NOT "on leave / Full day off."; (b) a stub gather with a designer in missingDesigners → assert the 🤖 "couldn't read" row still renders. Use the existing offline stub style (no network/Google/Productive).
  </action>
  <verify>
    <automated>node --import tsx --test "src/__tests__/runNightly.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "rosteredMinutes: g.rosteredMinutes" src/index.ts` shows the wiring line.
    - `grep -n "not in" src/index.ts` shows the routine-not-rostered note derivation.
    - runNightly.test.ts asserts a routine-not-rostered designer renders "not in <Weekday>" and NOT the literal "on leave / Full day off.".
    - runNightly.test.ts asserts an availability-unreadable designer still renders the 🤖 "couldn't read" row.
    - `grep -c "off\|underbooked\|overbooked\|ok" src/render/rows.ts` shows no new status value introduced (DayStatus unchanged).
    - runNightly test file runs green.
  </acceptance_criteria>
  <done>rosteredMinutes wired into the report; routine non-working days read "not in <day>" (no new status); availability-unreadable designers render the existing 🤖 row; runNightly tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression gate + degraded-path verification</name>
  <read_first>
    - src/render/renderMessage.ts (renderDegraded lines 84-93; calendarUnavailable note lines 137-143; the missing-designer normal-card path lines 149-165)
    - src/__tests__/runNightly.test.ts (the cases from Task 1)
  </read_first>
  <action>
    Run the FULL test suite (`npm test`) and confirm zero regressions across Phases 1–5 (SC-5). The signature change to availableMinutes/computeDesignerDay and StudioReportInput must not break any existing capacity, report, gather, render, or runNightly test — fix any stale call sites surfaced (every domain/report test must supply rosteredMinutes; every runNightly stub gather must supply a rosteredMinutes function).

    Verify (read-only confirmation, no new variant) that the D-06 availability-unknown case routes through the EXISTING per-designer miss path: an availability-unreadable designer arrives in `report.missingDesigners` (because Plan 02 omitted them from assessedDesigners) and renders via the existing `missingDesignerRow` 🤖 line. Confirm a WHOLE-source `/people` failure (all designers unreadable) still posts — it does NOT hit the top-level `renderDegraded` (that is driven by `ctx.sourceErrors` from a bookings/figures failure); instead every designer shows 🤖 "couldn't read" in the normal card, the figures-bearing card still posts (REL-01, never silently skip). If the runNightly tests from Task 1 do not already cover the all-designers-unreadable case, add one asserting the payload still posts with three 🤖 rows.

    Run `npx tsc --noEmit` and confirm clean.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` reports 0 failing tests (full suite green — Phases 1–5 no regression, SC-5).
    - `npx tsc --noEmit` exits 0.
    - runNightly.test.ts covers the all-designers-availability-unreadable case posting a card with 🤖 rows (REL-01).
    - No top-level degraded variant was added for the availability case (it reuses the existing missing-designer row) — confirmed by no new function in renderMessage.ts.
  </acceptance_criteria>
  <done>Full suite green, tsc clean, no Phase 1–5 regression; the D-06 availability-unknown case verified to reuse the existing 🤖 missing-designer row and still post.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live smoke check — non-working-day flagged correctly on the posted card</name>
  <action>Operator-run verification of the real posted card against live Productive (see how-to-verify steps). No code is written in this task; it gates the phase on the human confirming the non-working-day behaviour and number-fidelity before the change is trusted in production.</action>
  <what-built>
    The CAP-06 working-day availability change is wired end-to-end: live Productive `person.availabilities` now drive per-designer available hours and the rest-of-week rollup, a routine non-working day reads "not in <day>" instead of "on leave", and an availability-unreadable designer degrades to the 🤖 "couldn't read" row. All arithmetic stays deterministic and unit-tested; the full suite is green.
  </what-built>
  <how-to-verify>
    Run a real check-in against live Productive on a day whose target lands on Anisha's non-working day (Wed or Fri), posting to the TEST Chat space:

    1. Confirm the TEST webhook is in use (per memory: .env GCHAT_WEBHOOK_URL is the TEST space — do NOT post to the main team channel).
    2. From the repo root run a manual test send:
       `npx tsx --import dotenv/config src/index.ts`
       (If today's target day is not a Wed/Fri, trigger on a Tuesday-or-Thursday evening so the target day is Anisha's off-day; or temporarily set the injected `now` in a local scratch run to a Tuesday/Thursday evening — do NOT commit that.)
    3. In the posted Test-space card, confirm:
       - Anisha appears MENTIONED with NO open-time flag (⚪, "not in Wednesday"/"not in Friday" — NOT "on leave / Full day off.", NOT a red "Xh open" flag).
       - Liam and Ella still show their normal availability against their real rostered hours (7.5h-based), not a flat assumption that contradicts Productive.
       - The "Remaining studio time this week" rollup does NOT count Anisha's Wed/Fri as open studio capacity.
       - If you force an availability read failure (e.g. temporarily break the /people query), the card still POSTS with a 🤖 "couldn't read" row for the affected designer — it never silently skips the night and never invents open time.
    4. Cross-check the numbers against the Productive scheduling UI for the target day — they must match exactly (cardinal trust rule).
  </how-to-verify>
  <resume-signal>Type "approved" if the card flags Anisha's non-working day correctly (mentioned, never flagged) and the numbers match Productive; otherwise describe the discrepancy (designer, expected vs shown wording/hours).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RenderContext → posted Cards v2 HTML | Dynamic strings (designer name, routine-not-rostered day name) are interpolated into the card's HTML subset. A stray `&`/`<`/`>` could break a row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | routine-not-rostered day-name string in the row | mitigate | The dynamic day name is escaped via `escapeHtml` before insertion (mirrors the existing leaveNotes/name escaping in rows.ts lines 113, 134). |
| T-06-08 | Information disclosure | the posted card surfacing fabricated open time on a non-working day | mitigate | rosteredMinutes drives the report; a 0-rostered day is "off" (no flag) and contributes 0 to the rollup — verified by the human smoke check against the live Productive UI. |
| T-06-09 | Denial of service | a manual smoke run posting to the main team channel | accept | Memory + the checkpoint step gate on the TEST webhook; the run is operator-initiated and one-shot. |
</threat_model>

<verification>
- Full suite green: `npm test`.
- `npx tsc --noEmit` clean.
- Human smoke check approved (Anisha's non-working day mentioned-not-flagged; numbers match Productive; degraded path posts).
</verification>

<success_criteria>
- Target-day available hours come from the real working-day pattern (SC-1, end-to-end visible).
- A not-rostered designer is mentioned with no open-time flag — a quiet "not in <day>" line (SC-2).
- The rest-of-week rollup reflects real per-designer working days (SC-3).
- Availability read failure degrades safely and still posts (SC-4).
- No regression to Phases 1–5; all arithmetic deterministic + unit-tested (SC-5).
</success_criteria>

<output>
Create `.planning/phases/06-designer-working-day-availability/06-03-SUMMARY.md` when done.
</output>
