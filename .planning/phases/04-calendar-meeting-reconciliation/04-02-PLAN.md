---
phase: 04-calendar-meeting-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/calendar/spike.ts
  - src/config.ts
  - src/calendar/__fixtures__/labelled-events.json
autonomous: false
requirements: [MEET-02, MEET-03, MEET-05]
user_setup: []

must_haves:
  truths:
    - "A standalone script pulls ~3-4 weeks of all three designers' calendar events plus their Productive bookings and writes a human-labelable sheet"
    - "Liam labels each distinct meeting (overhead / counts / not-work) and confirms client names; the results are committed to config"
    - "The real shapes of solo events and eventType-on-instances are observed from live data and pinned into the filter assumptions"
    - "Golden test fixtures are emitted from real events, including the two validated cases (COVERED Quick FDC catch up 3 Jun; WORTH A LOOK FDC IPO Launch Check-In 26 May)"
  artifacts:
    - path: "src/calendar/spike.ts"
      provides: "Standalone labelling-spike script (NOT in the nightly path)"
      contains: "buildCalendarClient"
    - path: "src/calendar/__fixtures__/labelled-events.json"
      provides: "Golden fixtures from real labelled events (zod-valid CalendarEventResource samples)"
    - path: "src/config.ts"
      provides: "MEETING_IGNORE_LIST + CLIENT_ALIAS_MAP refined/confirmed from the spike"
  key_links:
    - from: "src/calendar/spike.ts"
      to: "src/calendar/auth.ts (buildCalendarClient) + src/productive/client.ts (fetchAllPages)"
      via: "reuse — no new auth/client machinery"
      pattern: "buildCalendarClient|fetchAllPages"
---

<objective>
Run the live-data labelling spike (D-09) — the de-risking twin of the Phase-2 "briefed" discovery. Pull ~3-4 weeks of all three designers' real calendar events and their Productive bookings into a human-labelable sheet so Liam can mark each distinct meeting overhead / counts / not-work and confirm client names. The labelled output becomes committed config (ignore-list phrases, client-alias map) and golden test fixtures — pinning the two data-shape assumptions (solo-event representation A1, eventType-on-instances A2) and the fuzzy-match aliases (A3) against real data, not memory.

Purpose: D-04's bias-against-false-positives and D-12's ship-live-from-day-one are only safe because the classifier and alias map are validated against real evenings and live in committed config. This spike IS that validation. It is a one-off probe, not part of the nightly run.
Output: `src/calendar/spike.ts` (standalone), refined `MEETING_IGNORE_LIST` + `CLIENT_ALIAS_MAP` in `src/config.ts`, and `src/calendar/__fixtures__/labelled-events.json` golden fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-calendar-meeting-reconciliation/04-CONTEXT.md
@.planning/phases/04-calendar-meeting-reconciliation/04-RESEARCH.md
@.planning/phases/04-calendar-meeting-reconciliation/04-PATTERNS.md
@.planning/phases/04-calendar-meeting-reconciliation/04-01-SUMMARY.md

@src/calendar/auth.ts
@src/calendar/client.ts
@src/calendar/schemas.ts

<interfaces>
<!-- From plan 04-01 + the Productive client. Reuse; build no new auth/network machinery. -->
src/calendar/auth.ts: `loadSaKey(): Result<{client_email,private_key}>`, `buildCalendarClient(saKey, subject) → calendar client`.
src/calendar/client.ts: `listDayEvents(client, timeMin, timeMax): Promise<Result<unknown[]>>` (or call events.list directly over the 3-4 week window).
src/productive/client.ts line 85: `fetchAllPages(path, query): Promise<Result<{data,included}>>` — reuse for the bookings pull.
Productive /bookings query shape (src/productive/gather.ts lines 359-367): `filter[person_id]=...&filter[after]=YYYY-MM-DD&filter[before]=YYYY-MM-DD&filter[canceled]=false&include=person,service,event,task,task.workflow_status,task.project,task.project.company`.
Config to refine (src/config.ts, from plan 01): `MEETING_IGNORE_LIST`, `CLIENT_ALIAS_MAP`, `DESIGNER_CALENDAR_EMAILS`.

The two D-09 golden cases that MUST appear in the fixtures (04-CONTEXT §Specifics lines 195-202):
- COVERED — "Quick FDC catch up", Tue 3 Jun 2026, Liam (686717) booked 6h FDC same day → not flagged.
- WORTH A LOOK — "FDC IPO Launch Check-In", Tue 26 May 2026, Liam had NO FDC booking on the 26th → flagged.
Real overhead seen (ignore-list): Team Daily Stand-up, Team Weekly WIP, Creative team - review, Creative WIP - plan the week.
Real not-work seen (excluded): Lunch, "The Full Stack AI Workflow…" webinar (solo), Falcon Dinner (17:30 after-hours).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the standalone labelling-spike script</name>
  <files>src/calendar/spike.ts</files>
  <read_first>
    - src/calendar/auth.ts + src/calendar/client.ts (plan 01 — reuse, do not rebuild)
    - src/productive/client.ts lines 85-118 (fetchAllPages — reuse for the bookings pull)
    - src/productive/gather.ts lines 359-367 (the live-confirmed /bookings query + include) and lines 172-191 (task→project→company resolution from included, to dump the alias linkage)
    - .planning/phases/04-calendar-meeting-reconciliation/04-RESEARCH.md §"The labelling spike as a buildable artifact" (lines 358-375), Pitfall 1 (solo, A1), Pitfall 3 (eventType, A2)
    - .planning/phases/04-calendar-meeting-reconciliation/04-PATTERNS.md §spike.ts (line 417)
  </read_first>
  <action>
    Create `src/calendar/spike.ts` — a STANDALONE script (run: `node --import tsx src/calendar/spike.ts`), NOT imported by the nightly path. It:
    1. Loads `loadSaKey()`; for each `DESIGNER_CALENDAR_EMAILS` entry builds a client via `buildCalendarClient` and reads events over a ~3-4 week window: `timeMin = now.minus({ days: 28 }).toISO()`, `timeMax = now.toISO()`, `singleEvents: true`, `orderBy: "startTime"`, `timeZone: "Australia/Sydney"`. (Single multi-week read, not the one-day `listDayEvents`.)
    2. Pulls the SAME window's Productive bookings via `fetchAllPages("/bookings", query)` with the include from gather.ts, and resolves each day's booked client companies (task→project→company id + name from `included`).
    3. Writes a flat, human-labelable Markdown table to a GITIGNORED scratch file `.planning/phases/04-calendar-meeting-reconciliation/spike-output.md` with columns: `date | designer | meeting title | eventType | attendees (self?, count) | start (studio time) | booked clients that day (id · name)`. One row per event instance. Include the raw `attendees` shape note so the solo representation (A1) is visible.
    4. Also dumps the raw `CalendarEventResource`-valid JSON for the distinct events to a scratch JSON so task 3 can transcribe golden fixtures from real shapes.
    The script NEVER posts to Chat, NEVER logs the SA key, and writes only to the gitignored scratch path. Add `spike-output*.{md,json}` to `.gitignore` if not already covered.
  </action>
  <verify>
    <automated>node --import tsx -e "import('./src/calendar/spike.ts').then(()=>console.log('IMPORT-OK')).catch(e=>{console.error(e);process.exit(1)})" 2>&1 | grep -qE 'IMPORT-OK|GOOGLE_SA_KEY|credentials' && grep -q 'buildCalendarClient' src/calendar/spike.ts && grep -q 'fetchAllPages' src/calendar/spike.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `src/calendar/spike.ts` exists, imports `buildCalendarClient`/`loadSaKey` from `./auth.ts` and `fetchAllPages` from `../productive/client.ts` (reuse, no new auth/network code).
    - It writes to `.planning/phases/04-calendar-meeting-reconciliation/spike-output.md` (gitignored) and never to Chat.
    - `grep -rn 'postToChat\|chat.googleapis' src/calendar/spike.ts` returns nothing.
    - No nightly-path module imports `spike.ts` (`grep -rn "calendar/spike" src/index.ts src/calendar/gather.ts` returns nothing).
  </acceptance_criteria>
  <done>A runnable standalone spike script that pulls 3-4 weeks of all three calendars + bookings and emits a human-labelable sheet plus raw event JSON to a gitignored scratch file, reusing the plan-01 calendar client and the Productive client.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Liam labels the spike output (overhead / counts / not-work + client names)</name>
  <action>Pause for the human labelling step. No code change in this task — it gates task 3, which transcribes the labels into committed config + fixtures. Liam runs the spike, labels each distinct meeting, confirms client names, and notes the real solo/eventType shapes.</action>
  <what-built>The labelling spike script (task 1). It reads all three designers' last ~4 weeks of real calendar events and their Productive bookings and writes a labelable Markdown sheet + raw event JSON to a gitignored scratch file.</what-built>
  <how-to-verify>
    1. Ensure `GOOGLE_SA_KEY` is available locally (point it at the gitignored `secrets/evening-studio-checkin-72c1031a6884.json`, e.g. via `.env` or `export GOOGLE_SA_KEY="$(cat secrets/evening-studio-checkin-72c1031a6884.json)"`). Also ensure `PRODUCTIVE_AUTH_TOKEN` / `PRODUCTIVE_ORG_ID` are set (existing `.env`).
    2. Run: `node --import tsx src/calendar/spike.ts`
    3. Open `.planning/phases/04-calendar-meeting-reconciliation/spike-output.md`. For each DISTINCT meeting title, label it: **overhead** (never count — Stand-up/WIP/Creative team), **counts** (client/1:1/training/Problem-SOLVD — should be in Productive), or **not-work** (lunch / solo webinar / after-hours). Confirm each client meeting's client name + any short code (e.g. "FDC" → FDC Construction / FDCC).
    4. Note the real `attendees` shape for a solo event (is the array absent, or one self:true entry?) and confirm `eventType` is populated on the OOO / focusTime instances.
    5. Confirm the two validated cases appear: "Quick FDC catch up" (3 Jun, Liam booked FDC same day → covered) and "FDC IPO Launch Check-In" (26 May, no FDC booking that day → worth a look).
  </how-to-verify>
  <resume-signal>Paste the labelled sheet (or the key conclusions: final ignore-list phrases, the client-alias entries, the real solo/eventType shapes), then type "labelled" to proceed to committing config + fixtures.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit the labelled results — refine config + emit golden fixtures</name>
  <files>src/config.ts, src/calendar/__fixtures__/labelled-events.json</files>
  <read_first>
    - src/config.ts (the MEETING_IGNORE_LIST + CLIENT_ALIAS_MAP seeded in plan 01 — refine in place)
    - src/calendar/schemas.ts (CalendarEventResource — fixtures must be valid against it)
    - src/productive/__fixtures__/bookings-page.json (the captured-real-shape fixture style to mirror)
    - The labelled sheet + scratch JSON produced by tasks 1-2 (the human checkpoint output)
    - .planning/phases/04-calendar-meeting-reconciliation/04-RESEARCH.md §Wave 0 Gaps (lines 472-477)
  </read_first>
  <action>
    Transcribe the human-labelled spike results into committed artifacts:
    - Refine `MEETING_IGNORE_LIST` in src/config.ts to the exact overhead phrases Liam confirmed (specific phrases, D-07 — must still NOT match a hypothetical "FDC WIP").
    - Refine `CLIENT_ALIAS_MAP` with every client meeting's confirmed company id + name + code + aliases observed in the sheet (FDC already seeded; add others surfaced).
    - Record the pinned solo-event representation (A1) and the eventType reliability (A2) as a docblock comment near the relevant config / in a `src/calendar/SPIKE-FINDINGS.md` note so plan 03's filter.ts builds against the real shape, not the assumption.
    - Create `src/calendar/__fixtures__/labelled-events.json`: an array of `CalendarEventResource`-valid real event samples covering one each of: declined (self responseStatus), all-day (start.date), out-of-office (eventType), solo (the real shape from A1), after-hours (17:30+ start.dateTime), an overhead meeting, the COVERED "Quick FDC catch up" (3 Jun), and the WORTH A LOOK "FDC IPO Launch Check-In" (26 May). These are the golden fixtures plan 03's filter + reconcile tests assert against.
  </action>
  <verify>
    <automated>node --import tsx -e "import('node:fs').then(fs=>import('./src/calendar/schemas.ts').then(m=>{const a=JSON.parse(fs.readFileSync('src/calendar/__fixtures__/labelled-events.json','utf8'));for(const e of a){const r=m.CalendarEventResource.safeParse(e);if(!r.success){console.error('INVALID',e.id);process.exit(1)}}if(!a.some(e=>/IPO Launch/i.test(e.summary||''))||!a.some(e=>/Quick FDC/i.test(e.summary||''))){console.error('MISSING golden cases');process.exit(1)}console.log('PASS')}))"</automated>
  </verify>
  <acceptance_criteria>
    - `src/calendar/__fixtures__/labelled-events.json` is a non-empty array; every entry passes `CalendarEventResource.safeParse`; it contains both golden cases (summaries matching /Quick FDC/i and /IPO Launch/i) AND one each of declined / all-day / OOO / solo / after-hours / overhead.
    - `src/config.ts` MEETING_IGNORE_LIST + CLIENT_ALIAS_MAP reflect the labelled results (`grep -c` shows the refined entries); no test-only or placeholder phrases.
    - The pinned solo/eventType shapes are recorded (grep finds the A1/A2 note in config.ts or src/calendar/SPIKE-FINDINGS.md).
  </acceptance_criteria>
  <done>The spike's human labels are committed: the ignore-list and alias map are real and confirmed, the solo/eventType shapes are pinned for plan 03, and the golden fixtures (including both validated FDC cases) are zod-valid and ready for the reconcile tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | `GOOGLE_SA_KEY` enters the spike via env; must never be logged or written to a committed file |
| spike output → repo | The scratch sheet may contain real meeting titles (potentially sensitive) — kept gitignored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | spike.ts SA key handling | mitigate | Reuse plan-01 `loadSaKey`/`buildCalendarClient`; never `console.*` the key; the key is never written to the scratch file. |
| T-04-06 | Information Disclosure | spike-output.md (real calendar contents) | mitigate | Scratch output written ONLY to a gitignored path; never committed. Only the abstracted config (phrases, alias map) + curated fixtures are committed. |
| T-04-07 | Information Disclosure | committed golden fixtures | accept | Fixtures contain a small set of real meeting titles already documented in 04-CONTEXT §Specifics (FDC cases); committing them is intentional and bounded. |
</threat_model>

<verification>
- `node --import tsx src/calendar/spike.ts` runs and writes the gitignored sheet (manual, in the checkpoint).
- `git status` shows `spike-output*` is NOT staged (gitignored).
- The labelled-events fixtures validate against `CalendarEventResource`.
</verification>

<success_criteria>
- The classifier inputs (ignore-list, alias map, solo/eventType shapes) are pinned against real data per D-09.
- Golden fixtures including both validated FDC cases exist and are zod-valid.
</success_criteria>

<output>
Create `.planning/phases/04-calendar-meeting-reconciliation/04-02-SUMMARY.md` when done.
</output>
