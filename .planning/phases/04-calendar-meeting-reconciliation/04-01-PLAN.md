---
phase: 04-calendar-meeting-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/config.ts
  - src/calendar/auth.ts
  - src/calendar/schemas.ts
  - src/calendar/client.ts
  - src/calendar/gather.ts
  - src/calendar/__tests__/gather.test.ts
  - src/calendar/__fixtures__/events-day.json
  - src/productive/gather.ts
autonomous: true
requirements: [MEET-01]
user_setup:
  - service: google-workspace
    why: "The nightly run reads the three designers' calendars via the service account; the SA private-key JSON must be available to CI as a secret (it already exists locally in gitignored secrets/)."
    env_vars:
      - name: GOOGLE_SA_KEY
        source: "Contents of secrets/evening-studio-checkin-72c1031a6884.json (the full SA JSON), added as a GitHub Actions repository secret"

must_haves:
  truths:
    - "For each of the three designers, the target day's events are fetched from their primary Google calendar via service-account DWD impersonation"
    - "Recurring events arrive expanded to real instances (no RRULE masters)"
    - "Every event is zod-validated at the boundary; a drift on one event skips that event and degrades, never throws"
    - "A calendar read failure for a designer becomes a sourceErrors string and the run continues (degrade, never throw)"
    - "The Productive gather now also surfaces each designer's set of booked client company ids for the target day, read from the SAME already-fetched included data (no second Productive call)"
  artifacts:
    - path: "src/calendar/auth.ts"
      provides: "buildCalendarClient(saKey, subject) → googleapis calendar client via google.auth.JWT"
      contains: "google.auth.JWT"
    - path: "src/calendar/schemas.ts"
      provides: "CalendarEventResource zod schema (safeParse-only, tolerant)"
      contains: "CalendarEventResource"
    - path: "src/calendar/client.ts"
      provides: "listDayEvents(client, timeMin, timeMax) → Result<unknown[]>, non-throwing"
      contains: "singleEvents"
    - path: "src/calendar/gather.ts"
      provides: "gatherCalendar(deps) → CalendarResult { eventsByDesigner, sourceErrors }"
      exports: ["gatherCalendar", "CalendarResult", "CalendarGatherDeps", "FilteredEvent"]
    - path: "src/productive/gather.ts"
      provides: "GatherResult extended with bookedClientsByDesignerDay"
      contains: "bookedClientsByDesignerDay"
  key_links:
    - from: "src/calendar/gather.ts"
      to: "src/calendar/client.ts"
      via: "injected stubbable fetchEvents (default = real listDayEvents)"
      pattern: "fetchEvents"
    - from: "src/calendar/auth.ts"
      to: "process.env.GOOGLE_SA_KEY"
      via: "JSON.parse of the SA key (never logged)"
      pattern: "GOOGLE_SA_KEY"
---

<objective>
Build the one genuinely new external surface of Phase 4: an unattended Google Calendar read for the three designers, as a non-throwing twin of `src/productive/`. Add `googleapis` as a dependency, add the non-secret calendar config, and surface from the existing Productive pull each designer's booked-client company set for the target day (Open Question 1) — read from the data already fetched, with no second Productive call.

Purpose: Calendar is the only new ingestion source in this phase. Everything downstream (filter, reconcile, render) is pure rules over what this slice produces. This plan delivers a real, validated, degradable calendar read threaded to the point where later plans consume it.
Output: `src/calendar/auth.ts`, `schemas.ts`, `client.ts`, `gather.ts` + test/fixture; `googleapis` in package.json; calendar config in `src/config.ts`; `bookedClientsByDesignerDay` on `GatherResult`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calendar-meeting-reconciliation/04-CONTEXT.md
@.planning/phases/04-calendar-meeting-reconciliation/04-RESEARCH.md
@.planning/phases/04-calendar-meeting-reconciliation/04-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — do not re-explore. -->

Reuse the Productive Result type (src/productive/client.ts lines 23-25) — import, do NOT redefine:
```typescript
export type Result<T> = { ok: true; value: T } | { ok: false; error: string };
```

The Productive gather result to EXTEND (src/productive/gather.ts lines 66-79) — add ONE field:
```typescript
export interface GatherResult {
  bookings: DatedBooking[];
  absences: DatedAbsence[];
  briefFlags: BriefFlag[];
  holidays: HolidaySet;
  assessedDesigners: DesignerId[];
  sourceErrors: string[];
  // ADD: bookedClientsByDesignerDay
}
```

Existing booking→company linkage in the Productive pull (gather.ts): the /bookings include is
`person,service,event,task,task.workflow_status,task.project,task.project.company` (line 361),
so each company resource IS in `bookingsResult.value.included`. The path to a booking's company id:
booking.relationships.task.data.id → (task in included).relationships.project.data.id →
(project in included).relationships.company.data.id. `mappers.ts linkedId` (lines 130-135) is the
relationship-id reader; `indexProjects` (lines 172-191) already iterates `included` for projects.

Productive client (src/productive/client.ts line 85), reused by the spike in plan 02, not here:
```typescript
export async function fetchAllPages(path: string, query: string):
  Promise<Result<{ data: unknown[]; included: unknown[] }>>;
```

Config style to mirror (src/config.ts): `export const NAME = [...] as const;` with a per-constant docblock.
DESIGNER_PERSON_IDS = ["686717","686712","686716"]; DESIGNER_NAMES keyed by the same ids.

CalendarEventResource zod schema to build (RESEARCH lines 335-356, PATTERNS lines 86-105) — tolerant, .loose(), all-optional except id; validate ONLY the fields this phase reads: id, status, summary, htmlLink, eventType, start{date,dateTime,timeZone}, attendeesOmitted, attendees[]{self,responseStatus}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add googleapis dependency + calendar config to src/config.ts</name>
  <read_first>
    - package.json (current deps: luxon ^3.7.2, zod ^4.4.3; test script `node --import tsx --test "src/**/*.test.ts"`)
    - src/config.ts (mirror the `export const NAME = ... as const;` + docblock style of DESIGNER_PERSON_IDS lines 28, DESIGNER_NAMES lines 36-40)
    - .planning/phases/04-calendar-meeting-reconciliation/04-RESEARCH.md §Standard Stack (googleapis ^173, NOT the stale ^144 in CLAUDE.md) + §Package Legitimacy Audit (slopcheck OK)
    - .planning/phases/04-calendar-meeting-reconciliation/04-PATTERNS.md lines 256-276 (the exact config constants to add)
  </read_first>
  <action>
    Add `"googleapis": "^173"` to package.json dependencies (the package is already present in node_modules at 173.0.0 from research; run `npm install googleapis@^173` to reconcile package.json + package-lock.json, then commit both — per RESEARCH §Runtime State Inventory build-artifacts row). Run `npm view googleapis version` first to confirm still 173.x.

    Add these committed, non-secret constants to src/config.ts (each with a docblock naming the decision it satisfies, matching the existing style):
    - `MEETING_IGNORE_LIST: readonly string[]` seeded with `"Daily Stand-up", "Weekly WIP", "Creative WIP", "Creative team"` (D-07; specific PHRASES, not loose keywords, so a future "FDC WIP" is not swallowed). Plan 02's spike refines this list.
    - `interface ClientAlias { companyId: string; companyName: string; code?: string; aliases: string[]; }` and `CLIENT_ALIAS_MAP: readonly ClientAlias[]` seeded with the FDC entry: companyId "1333899", companyName "FDC Construction", code "FDCC", aliases ["FDC","FDC Construction","FDCC","IPO Launch"] (D-03/D-09 §Specifics; spike confirms/extends).
    - `DESIGNER_CALENDAR_EMAILS` keyed by person id to align with DESIGNER_PERSON_IDS: "686717"→"liamm@solvdagency.com.au", "686712"→"anishag@solvdagency.com.au", "686716"→"ellaw@solvdagency.com.au" (live-confirmed, STATE.md).
    - `WORK_DAY_START = "08:30"` and `WORK_DAY_END = "17:30"` (D-08 studio-hours window, used by the after-hours filter in plan 03).
    Add a docblock note (mirroring the existing secret-posture note at config.ts lines 4-9) that the SA private key is a SECRET read from `process.env.GOOGLE_SA_KEY`, NEVER committed here.
  </action>
  <verify>
    <automated>npm view googleapis version && grep -q '"googleapis"' package.json && grep -q 'MEETING_IGNORE_LIST' src/config.ts && grep -q 'CLIENT_ALIAS_MAP' src/config.ts && grep -q 'DESIGNER_CALENDAR_EMAILS' src/config.ts && grep -q 'WORK_DAY_START' src/config.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies includes `googleapis` at `^173`; `package-lock.json` updated to match.
    - `src/config.ts` exports `MEETING_IGNORE_LIST`, `ClientAlias`, `CLIENT_ALIAS_MAP`, `DESIGNER_CALENDAR_EMAILS`, `WORK_DAY_START`, `WORK_DAY_END`.
    - `grep -n GOOGLE_SA_KEY src/config.ts` returns the docblock note only (the key value never appears as a literal in this committed file).
    - `node --import tsx -e "import('./src/config.ts').then(m=>{if(!m.CLIENT_ALIAS_MAP[0].companyId) process.exit(1)})"` exits 0.
  </acceptance_criteria>
  <done>googleapis ^173 is a committed dependency and the four non-secret calendar config constants exist with the FDC alias seed and the three live designer emails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Calendar auth + zod boundary + non-throwing client</name>
  <read_first>
    - src/productive/client.ts lines 1-15 (secret-from-env / never-log docblock posture), lines 23-25 (Result type to IMPORT), lines 56-83 (the non-throwing getJson try/catch contract to mirror)
    - src/productive/schemas.ts (tolerant `.loose()` + `.safeParse`-only export posture; never export a `.parse` wrapper)
    - .planning/phases/04-calendar-meeting-reconciliation/04-RESEARCH.md Pattern 1 (lines 174-195, the JWT+subject constructor — copy verbatim), Pattern 2 (lines 197-219, the events.list params), §"zod boundary" (lines 335-356)
    - .planning/phases/04-calendar-meeting-reconciliation/04-PATTERNS.md §auth.ts (lines 416), §client.ts (lines 37-69), §schemas.ts (lines 72-106)
  </read_first>
  <behavior>
    - schemas: `CalendarEventResource.safeParse({ id: "x" })` succeeds; `.safeParse({})` (no id) fails; an event carrying unknown extra Google fields still parses (tolerant).
    - client: `listDayEvents` returns `{ ok: true, value: items }` on success and `{ ok: false, error }` (status/message only, no key/token) when the googleapis client throws — it NEVER throws across the boundary.
  </behavior>
  <action>
    Create `src/calendar/auth.ts`: `buildCalendarClient(saKey: { client_email: string; private_key: string }, subject: string)` → `google.calendar({ version: "v3", auth })` where `auth = new google.auth.JWT({ email: saKey.client_email, key: saKey.private_key, scopes: ["https://www.googleapis.com/auth/calendar.readonly"], subject })` (RESEARCH Pattern 1, verbatim). Also export `loadSaKey()` that reads `process.env.GOOGLE_SA_KEY`, `JSON.parse`s it, and returns a `Result<{client_email,private_key}>` (mirror authHeaders' missing-secret degrade at client.ts lines 35-54). NEVER `console.*` the parsed key or the minted client.

    Create `src/calendar/schemas.ts`: export `CalendarEventResource` exactly as RESEARCH lines 335-356 (tolerant `.loose()`, `id` required, everything else optional, `start` = `EventDateTime{date,dateTime,timeZone}`, `attendees[]{self,responseStatus}`). safeParse-usable only — no `.parse` wrapper export.

    Create `src/calendar/client.ts`: import `Result` from `../productive/client.ts` (do NOT redefine). `export async function listDayEvents(client, timeMin: string, timeMax: string): Promise<Result<unknown[]>>` wrapping `client.events.list({ calendarId: "primary", timeMin, timeMax, singleEvents: true, orderBy: "startTime", timeZone: "Australia/Sydney", maxResults: 250 })` in a try/catch → `{ ok: true, value: res.data.items ?? [] }` or `{ ok: false, error: e instanceof Error ? e.message : String(e) }`. The error string carries status/message only (PATTERNS lines 53-69). `client` typed loosely (the googleapis calendar_v3.Calendar type) so tests can stub it.
  </action>
  <verify>
    <automated>node --import tsx --test src/calendar/__tests__/schemas-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/calendar/__tests__/schemas-client.test.ts` exists and passes: asserts the three schema behaviors above + that `listDayEvents` with a stub client whose `events.list` rejects returns `{ ok: false }` (no throw) and the error string does NOT contain "private_key" or "BEGIN".
    - `grep -n 'console' src/calendar/auth.ts` returns nothing that logs the key or client.
    - `grep -q 'calendar.readonly' src/calendar/auth.ts` and `grep -q 'singleEvents' src/calendar/client.ts`.
    - `src/calendar/client.ts` imports `Result` from `../productive/client.ts` (no local `type Result` redefinition).
  </acceptance_criteria>
  <done>The auth layer mints a per-subject DWD calendar client from GOOGLE_SA_KEY without logging the key, the zod boundary validates the Event shape tolerantly, and the client returns a Result and never throws.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: gatherCalendar ingestion root + bookedClientsByDesignerDay on Productive gather</name>
  <read_first>
    - src/productive/gather.ts lines 61-93 (GatherResult + GatherDeps shapes to mirror), lines 320-347 (the gather orchestration head + `degraded()` helper + clock/window derivation), lines 369-405 (the per-entry safeParse degrade loop), lines 172-191 (indexProjects — the included-iteration pattern), and the include string at line 361
    - src/productive/mappers.ts lines 130-135 (linkedId relationship reader)
    - src/productive/__tests__/gather.test.ts lines 1-50 (the stubbed-fetcher harness, fixed NOW const, fileURLToPath fixture loader — copy this harness exactly)
    - src/calendar/auth.ts, src/calendar/client.ts, src/calendar/schemas.ts (this plan's tasks 1-2)
    - .planning/phases/04-calendar-meeting-reconciliation/04-PATTERNS.md §gather.ts (lines 110-173) and §"No Analog / Open Q1" (lines 419-430)
    - src/domain/types.ts (DesignerId type)
  </read_first>
  <behavior>
    - gatherCalendar happy path: a stub `fetchEvents` returning the events-day.json fixture for each designer yields `eventsByDesigner` keyed by the three designer ids with the validated events; `sourceErrors` empty.
    - gatherCalendar degrade path: a stub `fetchEvents` returning `{ ok: false, error: "boom" }` for one designer pushes ONE sourceErrors string mentioning Calendar and that designer, returns a well-formed result (no throw), and the other designers still populate.
    - gatherCalendar shape-drift: an events array containing one entry missing `id` skips that entry (pushes a sourceErrors note) and keeps the valid ones.
    - Productive gather: given the existing bookings fixture, `bookedClientsByDesignerDay["686717"]` is a `Set<string>` of company ids derived from the target-day bookings' task→project→company linkage (empty set, not undefined, when a designer has no client booking).
  </behavior>
  <action>
    Create `src/calendar/gather.ts` mirroring `src/productive/gather.ts`:
    - `export interface FilteredEvent { id: string; summary: string; htmlLink: string; startLabel: string; startDateTime?: string; startDate?: string; eventType?: string; responseStatusSelf?: string; attendeeCount: number; }` — the clean output type (no raw Google type crosses the boundary; plan 03's filter/reconcile read THIS). Populate it from the validated `CalendarEventResource` (startLabel = a studio-zone time label like "2:30pm" via luxon from start.dateTime, or the date for all-day).
    - `export interface CalendarResult { eventsByDesigner: Record<DesignerId, FilteredEvent[]>; sourceErrors: string[]; }`
    - `export interface CalendarGatherDeps { now: DateTime; fetchEvents?: (subject: string, timeMin: string, timeMax: string) => Promise<Result<unknown[]>>; }` — `fetchEvents` defaults to building a client via `loadSaKey()`+`buildCalendarClient(saKey, subject)` then `listDayEvents`; tests inject a stub so NO network/credentials are needed.
    - `export async function gatherCalendar(deps): Promise<CalendarResult>`: derive `targetDay`/`targetKey` with the SAME `nextWorkingDay(now, holidays)` derivation as productive/gather (lines 332-334) so calendar and productive agree on "tomorrow"; build the Australia/Sydney day window `timeMin`/`timeMax` via luxon (`day.startOf("day").toISO()` / `day.endOf("day").toISO()`, RESEARCH Pattern 2); loop the three `DESIGNER_CALENDAR_EMAILS` calling `fetchEvents(email, timeMin, timeMax)`; on `!ok` push `"Couldn't reach Calendar for {name}: {error}"` and continue (degrade, REL-01); on ok, `CalendarEventResource.safeParse` each item, skip+note drift, map survivors to `FilteredEvent`. Returns well-formed result even if every read fails. NEVER throws.
    - This plan does NOT apply the mechanical filters or overhead ignore-list yet — those are plan 03 (filter.ts). gatherCalendar emits ALL validated events as FilteredEvent so the spike (plan 02) and plan 03 see the full shape.

    Extend `src/productive/gather.ts`: add `bookedClientsByDesignerDay: Record<DesignerId, Set<string>>` to `GatherResult` and populate it (Open Q1 — read the SAME already-fetched `bookingsResult.value.included`, NO second call). For each target-day confirmed booking, resolve task→project→company via the included resources (use `linkedId`/the indexProjects iteration pattern), and add the company id to that designer's set. Initialise every assessed designer to an empty Set. Add company id to `degraded()` as `{}`. Do NOT add `company` to the domain `Booking` type — `src/domain` stays untouched (PATTERNS lines 425-430).
  </action>
  <verify>
    <automated>node --import tsx --test src/calendar/__tests__/gather.test.ts && node --import tsx --test src/productive/__tests__/gather.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/calendar/__tests__/gather.test.ts` exists, uses `node:test` + `node:assert/strict`, a stubbed `fetchEvents` (NO network), a fixed studio-zone `NOW`, and loads `src/calendar/__fixtures__/events-day.json` via `fileURLToPath(new URL(...))` — and passes all four behaviors above.
    - `src/calendar/__fixtures__/events-day.json` exists and is `CalendarEventResource`-valid (a small set of timed + all-day events).
    - `grep -q 'bookedClientsByDesignerDay' src/productive/gather.ts` and the existing `src/productive/__tests__/gather.test.ts` still passes (no regression; extend it with one assertion that the new field is a populated `Record<DesignerId,Set>`).
    - `gatherCalendar` never imports from `src/domain/` and never reads the system clock (only the injected `now`).
  </acceptance_criteria>
  <done>gatherCalendar reads + validates + degrades per designer producing clean FilteredEvent lists, and the Productive gather additionally surfaces each designer's target-day booked-client company-id set from already-fetched data — resolving Open Question 1 with no second Productive call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | `GOOGLE_SA_KEY` (SA private key JSON) enters via `process.env`; untrusted to log |
| Google Calendar API → app | Event payloads (titles, links) are untrusted input crossing into the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | src/calendar/auth.ts (SA private key) | mitigate | Key read ONLY from `process.env.GOOGLE_SA_KEY`; never `console.*` the parsed key or minted client; error strings carry status/message only (mirror productive/client.ts). Test asserts the error string contains no "private_key"/"BEGIN". |
| T-04-02 | Elevation of Privilege | DWD scope | mitigate | Single read-only scope `calendar.readonly`; three configured subjects only — already authorised in Admin console (STATE.md). |
| T-04-03 | Tampering | CalendarEventResource boundary | mitigate | zod `.safeParse` on every event; a drift skips that event and degrades, never crashes (V5). |
| T-04-04 | Denial of Service | calendar read failure | mitigate | Additive source → `sourceErrors` string → existing 🤖 degraded card still posts (REL-01); gatherCalendar never throws. |
| T-04-SC | Tampering | npm install googleapis | accept | RESEARCH §Package Legitimacy Audit: googleapis [OK] via slopcheck, 14yr-old official Google package, no postinstall, 8.58M weekly downloads. No [ASSUMED]/[SUS] package → no blocking checkpoint required. |
</threat_model>

<verification>
- `npm test` (`node --import tsx --test "src/**/*.test.ts"`) green — calendar boundary tests + no Productive regression.
- `grep -rn 'GOOGLE_SA_KEY' src/` shows reads only in auth.ts/gather.ts, never in a log call.
- `src/calendar/` imports nothing from `src/domain/`.
</verification>

<success_criteria>
- googleapis ^173 committed; the three calendar config constants + alias seed present.
- gatherCalendar fetches, validates, and degrades per designer with no network in tests.
- `GatherResult.bookedClientsByDesignerDay` populated from already-fetched Productive `included` (Open Q1 resolved, no second call).
</success_criteria>

<output>
Create `.planning/phases/04-calendar-meeting-reconciliation/04-01-SUMMARY.md` when done.
</output>
