---
phase: 04-calendar-meeting-reconciliation
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-03"]
files_modified:
  - src/render/cards.ts
  - src/render/rows.ts
  - src/render/renderMessage.ts
  - src/render/__tests__/renderMessage.test.ts
  - src/render/__fixtures__/worth-a-look.json
  - src/index.ts
autonomous: true
requirements: [MEET-04]
user_setup: []

must_haves:
  truths:
    - "An unaccounted-for meeting appears as a 📅 sub-line nested under the relevant designer's row, in the soft 'worth a look' voice — never 'conflict'"
    - "The meeting title in the 📅 line deep-links to the Calendar event (MSG-06)"
    - "The 📅 line uses the same nested-sub-line widget pattern as the existing ⚠️/📄 lines and every dynamic string is HTML-escaped"
    - "The nightly composition root reads the calendar, runs the reconciler, and puts the result on RenderContext.worthALook; a calendar read failure degrades to the existing 🤖 card and still posts (REL-01)"
    - "A calendar failure is NEVER the POST-failure exit-1 path — calendar is a data source (degrade + exit 0)"
  artifacts:
    - path: "src/render/cards.ts"
      provides: "RenderContext.worthALook field"
      contains: "worthALook"
    - path: "src/render/rows.ts"
      provides: "the 📅 worth-a-look sub-line in buildRow"
      contains: "📅"
    - path: "src/index.ts"
      provides: "calendar gather + reconcile wired into runNightly; sourceErrors concatenated"
      contains: "gatherCalendar"
  key_links:
    - from: "src/index.ts"
      to: "src/calendar/gather.ts + src/calendar/reconcile.ts"
      via: "gatherCalendar({now}) → reconcileMeetings(...) → ctx.worthALook"
      pattern: "gatherCalendar|reconcileMeetings"
    - from: "src/render/rows.ts"
      to: "ctx.worthALook[d.designerId]"
      via: "📅 sub-line loop after the 📄 brief loop"
      pattern: "worthALook"
    - from: "src/index.ts"
      to: "g.sourceErrors"
      via: "calendar sourceErrors concatenated before buildRenderContext (REL-01)"
      pattern: "sourceErrors"
---

<objective>
Surface the reconciler's output to the team and wire the whole calendar slice into the nightly run. Add a `worthALook` field to `RenderContext`, emit the 📅 "worth a look" sub-line under the designer row (exactly like the existing ⚠️/📄 lines, with deep-linked title), and thread `gatherCalendar` + `reconcileMeetings` into `runNightly` so a real unaccounted meeting shows up in the nightly card — while a calendar failure degrades to the existing 🤖 card and still posts.

Purpose: This is the slice that makes the phase visible to the user. After this plan, a designer with a counting meeting that isn't in Productive for the day gets a soft, deep-linked nudge in the nightly Chat post (D-14, MEET-04, MSG-06/07).
Output: modified `cards.ts`/`rows.ts`/`renderMessage.ts` (render the 📅 line) + `index.ts` (wire the source), with a render test over a populated `worthALook` fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-calendar-meeting-reconciliation/04-CONTEXT.md
@.planning/phases/04-calendar-meeting-reconciliation/04-RESEARCH.md
@.planning/phases/04-calendar-meeting-reconciliation/04-PATTERNS.md
@.planning/phases/04-calendar-meeting-reconciliation/04-01-SUMMARY.md
@.planning/phases/04-calendar-meeting-reconciliation/04-03-SUMMARY.md

@src/render/cards.ts
@src/render/rows.ts
@src/render/renderMessage.ts
@src/index.ts

<interfaces>
<!-- Extracted from the codebase. The 📅 line copies the ⚠️/📄 pattern exactly. -->

RenderContext (src/render/cards.ts lines 155-178) — ADD one field after tentativeNotes (line 163):
```typescript
/** Per-designer "worth a look" meetings → the 📅 sub-line (D-14). Presentation-only. */
worthALook?: Record<string, Array<{ title: string; start: string; link: string }>>;
```
(This shape matches reconcile.ts's WorthALookItem = { title, start, link }.)

buildRow (src/render/rows.ts lines 100-153) — the ctx param object (lines 102-108) carries designerNames/briefFlags/tentativeNotes/leaveNotes/missingDesigners; ADD worthALook. The 📄 loop (lines 146-151) and ⚠️ block (lines 139-143) are the exact widget pattern. Reuse `escapeHtml` (lines 24-29) and `muted` (lines 37-39).
The 📅 block to add AFTER the 📄 loop (RESEARCH Pattern 5 lines 262-265):
```typescript
for (const m of ctx.worthALook?.[d.designerId] ?? []) {
  const titleLink = `<a href="${escapeHtml(m.link)}">${escapeHtml(m.title)}</a>`;
  lines.push(`📅 ${titleLink} · ${muted(escapeHtml(m.start))} · ${muted("worth a look")}`);
}
```

renderMessage.ts buildRow call (lines 142-149): add `worthALook: ctx.worthALook` to the options object alongside tentativeNotes/leaveNotes.

index.ts runNightly (lines 130-172): after `gather` (line 138) + `computeStudioReport` (line 150), call gatherCalendar + reconcile, concat sourceErrors, set ctx.worthALook. The two-path reliability rule (docblock lines 19-31): calendar is a DATA source (degrade + exit 0), never the POST exit-1 path.

reconcile.ts (plan 03): `reconcileMeetings(eventsByDesigner, bookedClientsByDesignerDay, CLIENT_ALIAS_MAP, MEETING_IGNORE_LIST) → Record<DesignerId, WorthALookItem[]>`.
calendar gather (plan 01): `gatherCalendar({ now }) → { eventsByDesigner, sourceErrors }`.
productive gather (plan 01): `g.bookedClientsByDesignerDay`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add worthALook to RenderContext + emit the 📅 sub-line</name>
  <read_first>
    - src/render/cards.ts lines 130-149 (TentativeNote — the presentation-only-in-RenderContext rationale to mirror) and lines 155-178 (RenderContext fields + per-field docblock style)
    - src/render/rows.ts lines 18-39 (escapeHtml + muted), lines 100-153 (buildRow, the ⚠️ block 139-143 and 📄 loop 146-151)
    - src/render/renderMessage.ts lines 138-152 (the buildRow call in the busy-rows loop)
    - src/render/__tests__/renderMessage.test.ts (existing harness to extend)
    - .planning/phases/04-calendar-meeting-reconciliation/04-PATTERNS.md §cards.ts (lines 280-295), §rows.ts (lines 298-318), §renderMessage.ts (lines 321-327)
  </read_first>
  <behavior>
    - A RenderContext with `worthALook["686717"] = [{ title: "FDC IPO Launch Check-In", start: "9:00am", link: "https://www.google.com/calendar/event?eid=abc" }]` renders, under Liam's row, a 📅 line containing the deep-linked title (`<a href=...>FDC IPO Launch Check-In</a>`), the start, and the literal "worth a look".
    - The 📅 line appears nested in the same `decoratedText.text` (`<br>`-joined) as the ⚠️/📄 lines, after the 📄 brief lines.
    - Every dynamic string (title, link, start) is HTML-escaped — a title containing `<script>` or `&` is escaped, never injected raw.
    - The voice is "worth a look" — the rendered text never contains "conflict".
    - A designer with no worthALook entry renders exactly as before (no 📅 line); an empty/absent worthALook map is a no-op (no regression to the existing variants).
    - A `missingDesigners` (🤖) row never gains a 📅 line (the early-return at rows.ts line 113 is before the 📅 block).
  </behavior>
  <action>
    Add the `worthALook?` field to `RenderContext` (cards.ts, after tentativeNotes line 163) with a docblock matching the per-field style, naming D-14. Extend buildRow's `ctx` param type (rows.ts lines 102-108) to carry `worthALook?: Record<string, Array<{ title: string; start: string; link: string }>>` and add the 📅 loop AFTER the 📄 brief loop (the exact block in <interfaces>). Reuse `escapeHtml`/`muted`. Thread `worthALook: ctx.worthALook` into the buildRow call in renderMessage.ts (lines 142-149) alongside tentativeNotes/leaveNotes — no other renderMessage change; the variant cascade and degraded path are untouched (renderDegraded already joins sourceErrors verbatim, so a calendar failure needs NO renderer change).
    Add `src/render/__fixtures__/worth-a-look.json` — a RenderContext-or-report fixture with a populated worthALook for the test.
  </action>
  <verify>
    <automated>node --import tsx --test src/render/__tests__/renderMessage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/render/__tests__/renderMessage.test.ts` (extended) passes and asserts: the 📅 line is present under the correct designer with the deep-linked title; a title containing `<script>` is HTML-escaped; the output contains "worth a look" and does NOT contain "conflict"; a context with no worthALook produces no 📅 line.
    - `grep -q '📅' src/render/rows.ts` and `grep -q 'worthALook' src/render/cards.ts src/render/renderMessage.ts`.
    - The full existing render test suite still passes (no variant regression).
  </acceptance_criteria>
  <done>RenderContext carries worthALook and buildRow emits a soft, deep-linked, HTML-escaped 📅 sub-line nested under the designer row exactly like the ⚠️/📄 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Wire calendar gather + reconcile into runNightly</name>
  <read_first>
    - src/index.ts lines 1-44 (the two-path reliability docblock + imports), lines 84-123 (buildRenderContext), lines 130-172 (runNightly composition)
    - src/calendar/gather.ts (gatherCalendar signature, plan 01)
    - src/calendar/reconcile.ts (reconcileMeetings signature, plan 03)
    - src/productive/gather.ts (GatherResult.bookedClientsByDesignerDay, plan 01)
    - src/config.ts (CLIENT_ALIAS_MAP, MEETING_IGNORE_LIST)
    - .planning/phases/04-calendar-meeting-reconciliation/04-PATTERNS.md §index.ts (lines 330-351)
  </read_first>
  <action>
    In `runNightly` (after `gather` at line 138 and `computeStudioReport` at line 150):
    1. Call `const cal = await gatherCalendar({ now });` (the SAME injected `now`, so calendar and productive agree on the target day).
    2. `const worthALook = reconcileMeetings(cal.eventsByDesigner, g.bookedClientsByDesignerDay, CLIENT_ALIAS_MAP, MEETING_IGNORE_LIST);`
    3. Concatenate calendar failures into the existing degrade list BEFORE buildRenderContext: pass `[...g.sourceErrors, ...cal.sourceErrors]` so a calendar read failure flows through the existing 🤖 degraded card (REL-01) with NO renderer change.
    4. Set `ctx.worthALook = worthALook` (or pass it into buildRenderContext and assign there, mirroring how tentativeNotes is built — keep buildRenderContext the place that assembles ctx).
    The two-path rule (docblock lines 19-31): a calendar failure is a DATA source — it degrades and the night still posts (exit 0). It is NEVER the POST-failure exit-1 branch (lines 165-168), which stays solely the postToChat outcome. gatherCalendar reads `process.env.GOOGLE_SA_KEY` inside the calendar layer called from here — this composition root stays the one env/clock boundary; the pure reconciler/filter never read env or the clock.
  </action>
  <verify>
    <automated>npm test && node --import tsx -e "import('./src/index.ts').then(()=>console.log('OK')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'gatherCalendar' src/index.ts` and `grep -q 'reconcileMeetings' src/index.ts`.
    - The calendar `sourceErrors` are merged into the list passed to buildRenderContext (`grep -n 'cal.sourceErrors' src/index.ts` shows the concat before render).
    - `ctx.worthALook` is set from reconcileMeetings.
    - Importing `src/index.ts` does not trigger a real run (the `import.meta.main` guard is intact) and does not throw.
    - `npm test` (full suite) green.
  </acceptance_criteria>
  <done>runNightly reads the calendar, reconciles meetings into worthALook, and merges calendar sourceErrors into the degrade path — a real unaccounted meeting appears in the nightly card and a calendar outage degrades to 🤖 and still posts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciler output → Chat card | Untrusted meeting titles/links rendered into Cards v2 HTML |
| calendar source → nightly run | A calendar failure must degrade, never abort the post |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering / XSS | rows.ts 📅 line (title, link, start) | mitigate | Every dynamic string passes the existing `escapeHtml` before insertion (same as ⚠️/📄 lines, threat T-03-01). Test asserts a `<script>` title is escaped. |
| T-04-12 | Denial of Service | calendar failure aborting the post | mitigate | Calendar sourceErrors merged into g.sourceErrors → existing 🤖 degraded card still posts (REL-01); calendar is never the exit-1 path. |
| T-04-13 | Spoofing | asserting a definite conflict | mitigate | Voice is "worth a look" only (D-04/MEET-04); test asserts the output never contains "conflict". |
</threat_model>

<verification>
- `npm test` green (render + wiring + no regression to Phases 1-3 variants).
- The 📅 line renders with a deep-link and soft voice; HTML-escaped.
- A calendar failure path degrades (exit 0), POST failure still exit 1 (unchanged).
- `grep -rn 'conflict' src/render/rows.ts` returns nothing.
</verification>

<success_criteria>
- MEET-04 + MSG-06/07: unaccounted meetings surface as a soft, deep-linked, visually-distinct 📅 sub-line.
- The calendar source is fully wired into runNightly behind the existing degrade-don't-throw and two-path reliability rules.
</success_criteria>

<output>
Create `.planning/phases/04-calendar-meeting-reconciliation/04-04-SUMMARY.md` when done.
</output>
