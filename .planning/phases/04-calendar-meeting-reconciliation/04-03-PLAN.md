---
phase: 04-calendar-meeting-reconciliation
plan: 03
type: tdd
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/calendar/filter.ts
  - src/calendar/reconcile.ts
  - src/calendar/__tests__/filter.test.ts
  - src/calendar/__tests__/reconcile.test.ts
autonomous: true
requirements: [MEET-02, MEET-03, MEET-05]
user_setup: []

must_haves:
  truths:
    - "Declined (the designer's own RSVP), all-day, out-of-office, solo, and after-hours events are excluded from reconciliation"
    - "The known recurring overhead ceremonies are hard-excluded via the committed ignore-list; a future client 'FDC WIP' is NOT swallowed"
    - "A counting meeting is treated as covered (not flagged) when the designer has a confirmed Productive booking for that meeting's client on the SAME target day"
    - "When a meeting's client cannot be confidently matched, the reconciler stays quiet (treats as covered) — bias against false positives"
    - "The two golden cases resolve correctly: Quick FDC catch up (3 Jun, booked FDC) → not flagged; FDC IPO Launch Check-In (26 May, no FDC booking that day) → worth a look"
  artifacts:
    - path: "src/calendar/filter.ts"
      provides: "Pure mechanical filters + overhead matcher + the counting-meeting predicate"
      exports: ["isDeclined", "isAllDay", "isOutOfOffice", "isSolo", "isAfterHours", "isOverhead", "isCountingMeeting"]
    - path: "src/calendar/reconcile.ts"
      provides: "Pure reconcileMeetings(...) → per-designer worthALook map"
      exports: ["reconcileMeetings", "matchTitleToClient", "WorthALookItem"]
  key_links:
    - from: "src/calendar/reconcile.ts"
      to: "src/config.ts (CLIENT_ALIAS_MAP) + GatherResult.bookedClientsByDesignerDay"
      via: "injected inputs — never recomputes hours, never reads the clock"
      pattern: "matchTitleToClient|bookedClient"
---

<objective>
Build the deterministic heart of Phase 4: pure mechanical filters (declined / all-day / OOO / solo / after-hours / overhead, MEET-05/MEET-02) and the same-day same-client reconciler (MEET-03). Given the filtered "counting" meetings plus each designer's booked-client company set for the target day (from plan 01's `bookedClientsByDesignerDay`) and the committed client-alias map (pinned by plan 02's spike), produce a per-designer "worth a look" list — biased hard against false positives (D-04).

Purpose: This is the trust-critical rules layer. It reads pre-computed facts (the report's figures + a ready-made `Set<companyId>`) and NEVER recomputes hours, reads the clock, or calls the network (CLAUDE.md trust boundary). TDD because the I/O is fully defined: the two golden cases plus each filter's truth table.
Output: `src/calendar/filter.ts` + `reconcile.ts` with full unit tests over the plan-02 golden fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-calendar-meeting-reconciliation/04-CONTEXT.md
@.planning/phases/04-calendar-meeting-reconciliation/04-RESEARCH.md
@.planning/phases/04-calendar-meeting-reconciliation/04-PATTERNS.md
@.planning/phases/04-calendar-meeting-reconciliation/04-01-SUMMARY.md
@.planning/phases/04-calendar-meeting-reconciliation/04-02-SUMMARY.md

@src/calendar/gather.ts
@src/config.ts

<interfaces>
<!-- From plans 01-02. The reconciler consumes these; it builds NO ingestion machinery. -->
FilteredEvent (src/calendar/gather.ts, plan 01): { id, summary, htmlLink, startLabel, startDateTime?, startDate?, eventType?, responseStatusSelf?, attendeeCount }.
GatherResult.bookedClientsByDesignerDay (src/productive/gather.ts, plan 01): Record<DesignerId, Set<companyId-string>>.
Config (src/config.ts, refined by plan 02): MEETING_IGNORE_LIST: readonly string[]; CLIENT_ALIAS_MAP: readonly ClientAlias[] where ClientAlias = { companyId, companyName, code?, aliases: string[] }; WORK_DAY_START="08:30"; WORK_DAY_END="17:30".
Golden fixtures (src/calendar/__fixtures__/labelled-events.json, plan 02): includes declined/all-day/OOO/solo/after-hours/overhead + the two FDC cases.

Solo/eventType real shapes: use the A1/A2 findings pinned by plan 02 (SPIKE-FINDINGS), not the raw assumption.

Reconcile core (RESEARCH Pattern 4 lines 242-250; PATTERNS lines 191-202):
  for each counting meeting → matchTitleToClient(summary, CLIENT_ALIAS_MAP):
    null (uncertain) → continue (STAY QUIET, D-04)
    matched & bookedClientIdsToday.has(client.companyId) → continue (covered same-day, D-01/D-02)
    else → worthALook[designerId].push({ title, start, link })
matchTitleToClient: case-insensitive substring, longest-alias-first so "FDC Construction" beats "FDC"; first confident hit wins; no hit → null (RESEARCH lines 386-394).
</interfaces>
</context>

<feature>
  <name>Mechanical filters + overhead matcher (MEET-02, MEET-05)</name>
  <files>src/calendar/filter.ts, src/calendar/__tests__/filter.test.ts</files>
  <behavior>
    Pure predicates over a FilteredEvent (RESEARCH Pitfalls 1-4, D-08), each unit-tested against the plan-02 golden fixtures:
    - isDeclined(e) ⟺ the designer's own RSVP is declined (FilteredEvent.responseStatusSelf === "declined"); needsAction/tentative/accepted are all in play (D-08 — Liam never RSVPs).
    - isAllDay(e) ⟺ startDate present (start.date), no startDateTime.
    - isOutOfOffice(e) ⟺ eventType ∈ {outOfOffice, focusTime, workingLocation}.
    - isSolo(e) ⟺ the pinned A1 shape (attendeeCount ≤ 1 / absent-or-self-only) — use plan-02's confirmed representation.
    - isAfterHours(e) ⟺ for a timed event, the studio-zone (Australia/Sydney) local start hour:min is outside WORK_DAY_START..WORK_DAY_END (luxon `.setZone`, RESEARCH Pitfall 2 — keep the comparison in studio zone, never UTC). The 17:30 Falcon Dinner is excluded; a 16:00 meeting is kept.
    - isOverhead(title) ⟺ MEETING_IGNORE_LIST.some(p => title.toLowerCase().includes(p.toLowerCase())). MUST exclude "Team Weekly WIP" but NOT "FDC WIP" (specific phrases, D-07).
    - isCountingMeeting(e) ⟺ NOT (declined | all-day | OOO | solo | after-hours | overhead). The single composed predicate the reconciler consumes.
    Edge: a missing summary ("(No title)") → not overhead, but still passes other filters; a missing start → treat as excluded (no time to attribute).
  </behavior>
  <implementation>
    Small pure boolean functions mirroring src/productive/briefed.ts predicate style. luxon for the after-hours studio-zone parse. Read MEETING_IGNORE_LIST/WORK_DAY_START/END from src/config.ts. No clock reads (parse the event's own start), no network, no domain import. Build the golden test from the plan-02 fixtures' real shapes (NOT the A1 assumption).
  </implementation>
</feature>

<feature>
  <name>Same-day same-client reconciler (MEET-03, MEET-04 input)</name>
  <files>src/calendar/reconcile.ts, src/calendar/__tests__/reconcile.test.ts</files>
  <behavior>
    - matchTitleToClient(title, CLIENT_ALIAS_MAP) → ClientAlias | null: case-insensitive substring, longest-alias-first, first hit wins, no hit → null.
    - reconcileMeetings(eventsByDesigner, bookedClientsByDesignerDay, aliasMap, ignoreList) → Record<DesignerId, WorthALookItem[]> where WorthALookItem = { title, start, link }:
      for each designer, for each event that isCountingMeeting: match title→client; null → skip (stay quiet, D-04); matched & the designer's target-day booked-client set has client.companyId → skip (covered same-day, D-01/D-02); else push { title: summary, start: startLabel, link: htmlLink }.
    - GOLDEN CASE 1 (COVERED): "Quick FDC catch up" for Liam(686717) with bookedClients{686717} containing "1333899" → NOT in the output.
    - GOLDEN CASE 2 (WORTH A LOOK): "FDC IPO Launch Check-In" for Liam with bookedClients{686717} NOT containing "1333899" → present in the output with the correct title/start/link.
    - An overhead or declined event never reaches the output (filtered first).
    - The reconciler reads NO hours, NO clock, NO network — only its injected inputs.
  </behavior>
  <implementation>
    Pure function mirroring src/productive/brief.ts assessBriefs (pre-resolved inputs + lookup map → per-designer flags). Compose isCountingMeeting from filter.ts. Output type mirrors the BriefFlag per-designer grouping. Tests load src/calendar/__fixtures__/labelled-events.json and construct the bookedClients sets for the two golden cases by hand from 04-CONTEXT §Specifics. node:test + node:assert/strict, offline.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event title → reconciler | Untrusted calendar text used for matching (not yet rendered here — escaping happens in plan 04) |
| report figures → reconciler | The reconciler must read figures/sets only, never recompute (trust boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | reconcile.ts recomputing hours | mitigate | reconcile.ts/filter.ts import NOTHING from src/domain and never touch capacity.ts; they read the ready-made `Set<companyId>` + report figures only (CLAUDE.md trust rule). Test asserts no domain import. |
| T-04-09 | Spoofing | uncertain client match → false flag | mitigate | D-04 prime directive — no confident alias match → stay quiet (treat covered). Unit test covers the null-match-stays-quiet case. |
| T-04-10 | Information Disclosure | filter reading event text | accept | Pure in-memory rules; no logging of event payloads. |
</threat_model>

<verification>
- `node --import tsx --test src/calendar/__tests__/filter.test.ts src/calendar/__tests__/reconcile.test.ts` green.
- Each TDD feature follows RED→GREEN: commit the failing test, then the implementation.
- `grep -rn "from \"../../domain\|from '../../domain\|capacity" src/calendar/filter.ts src/calendar/reconcile.ts` returns nothing.
- Both golden cases assert as specified.
</verification>

<success_criteria>
- All six filters + isCountingMeeting behave per the truth tables, including "FDC WIP" NOT swallowed and the studio-zone after-hours cut.
- reconcileMeetings produces the two golden outcomes and stays quiet on uncertain matches.
- The layer is pure: no domain import, no clock, no network.
</success_criteria>

<output>
Create `.planning/phases/04-calendar-meeting-reconciliation/04-03-SUMMARY.md` when done.
</output>
