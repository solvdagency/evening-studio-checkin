---
phase: 05-llm-renderer-optional
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/llm/schema.ts
  - src/llm/prompt.ts
  - src/llm/assemble.ts
  - src/llm/applyVerdicts.ts
  - src/llm/applyVerdicts.test.ts
  - src/llm/flagFairness.test.ts
  - scripts/eval-llm-renderer.ts
  - src/config.ts
  - src/index.ts
autonomous: true
requirements: [LLM-02]

must_haves:
  truths:
    - "With the fuzzy-judgment toggle OFF (default), the assembled card is byte-identical to Slice-1-only output — the worth-a-look 📅 lines are exactly what reconcileMeetings produced"
    - "With the toggle ON, a 'drop' verdict suppresses a worth-a-look line, 'soften' replaces its wording, 'keep' leaves it as the reconciler raised it"
    - "The LLM can NEVER invent a flag: a verdict for an id the reconciler never raised, or a drop of an item not in the worth-a-look set, is a no-op"
    - "The LLM NEVER drops a flag the reference set labels as genuine client work (offline flag-fairness eval enforces this)"
    - "Numbers, rows, week-bar and links remain byte-identical to the deterministic output regardless of verdicts"
  artifacts:
    - path: "src/llm/applyVerdicts.ts"
      provides: "applyVerdicts(worthALook, meetingVerdicts) — pure: applies keep/soften/drop, ignores unknown/invented ids (no-op), never adds a flag"
      contains: "applyVerdicts"
    - path: "src/llm/applyVerdicts.test.ts"
      provides: "structural tests: invent-a-flag no-op, drop-unknown-id no-op, toggle-OFF byte-identity, keep/soften/drop applied"
      min_lines: 30
    - path: "scripts/eval-llm-renderer.ts"
      provides: "offline tsx harness running the real prompt over labelled-events.json — prints a keep/soften/drop pass/fail table (dev key, NOT in CI)"
      min_lines: 30
    - path: "src/llm/flagFairness.test.ts"
      provides: "the never-drop-a-genuine-flag structural assertion over the labelled golden cases (network-free)"
      min_lines: 20
  key_links:
    - from: "src/llm/assemble.ts"
      to: "src/llm/applyVerdicts.ts"
      via: "assembleCardsV2 calls applyVerdicts on the worth-a-look list before rendering rows, only when the toggle is on"
      pattern: "applyVerdicts"
    - from: "scripts/eval-llm-renderer.ts"
      to: "src/calendar/__fixtures__/labelled-events.json"
      via: "loads the Phase-4 labelled meetings as the flag-fairness reference set"
      pattern: "labelled-events"
---

<objective>
Ship Slice 2: extend the single Anthropic call to also return per-meeting keep/soften/drop
verdicts that adjudicate the deterministic reconciler's "worth a look" flags, applied by
deterministic code BEFORE rendering — behind a config toggle defaulting OFF so it ships
disabled until trusted. The LLM is precision-focused and biased to silence: it may soften or
drop a borderline flag, but can NEVER invent a flag and NEVER drop a meeting the reference set
labels as genuine client work. Numbers stay untouched.

Purpose: Satisfy LLM-02 (the LLM adjudicates fuzzy meeting reconciliation from pre-computed
facts only). Slice 2 must be cleanly separable — with the toggle OFF, behaviour is
byte-identical to Slice-1-only.

Output: verdict-application logic + schema/prompt extension + the offline flag-fairness eval
harness over the Phase-4 labelled data + the never-drop-a-genuine-flag assertion + a default-OFF
config toggle wired through.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llm-renderer-optional/05-AI-SPEC.md
@.planning/phases/05-llm-renderer-optional/05-01-SUMMARY.md

@src/calendar/reconcile.ts
@src/config.ts
@CLAUDE.md

<interfaces>
<!-- Contracts the executor consumes. -->

From src/calendar/reconcile.ts — the fuzzy-judgment input (the reconciler's bias-to-silence output):
```ts
export interface WorthALookItem {
  title: string;
  durationMinutes?: number;
}
export function reconcileMeetings(
  eventsByDesigner, bookedClientsByDesignerDay, aliasMap, ignoreList,
): Record<DesignerId, WorthALookItem[]>;
```

From src/llm/schema.ts (Slice 1, extend in place) — the verdict shape is ALREADY in LlmOutput:
```ts
meetingVerdicts: z.array(z.object({
  id: z.number().int().nonnegative(),   // echoes the index we sent in buildFacts
  verdict: z.enum(["keep","soften","drop"]),
  line: z.string().max(160),            // softened wording (no numbers); escaped before insertion
})).max(20);
```

From src/llm/assemble.ts (Slice 1) — the commented Slice-2 seam where verdicts get applied to
the worth-a-look list before rendering rows.

From src/calendar/__fixtures__/labelled-events.json — ~4 weeks of REAL meetings already tagged
(counts/covered/worth-a-look/not-work/etc.) with golden Case A (covered → stay silent) and
Case B (worth-a-look → must flag). This is the flag-fairness reference set — do NOT relabel.

Verdict semantics (AI-SPEC §4 Core Pattern + §6): keep = surface the 📅 line as raised;
soften = surface with the model's gentler `line` wording; drop = suppress. Because the
reconciler already biases hard to silence, the assembler treats an unknown/unexpected id, or a
drop of an item the reconciler did not raise, as a NO-OP — the model can only soften or remove
a real flag, never invent one.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for verdict application (toggle-OFF identity + no-invent + apply)</name>
  <files>src/llm/applyVerdicts.test.ts, src/llm/flagFairness.test.ts</files>
  <read_first>src/calendar/reconcile.ts (WorthALookItem), src/llm/schema.ts (meetingVerdicts), src/calendar/__fixtures__/labelled-events.json (the golden Case A/B), 05-AI-SPEC.md §5 (flag-fairness rubric) and §6 (invent/harden suppression guardrail).</read_first>
  <behavior>
    - applyVerdicts.test.ts (structural, network-free):
      - toggle-OFF / empty verdicts: applyVerdicts(worthALook, []) deepStrictEqual worthALook (byte-identical to Slice-1).
      - keep: a "keep" verdict for id N leaves item N unchanged.
      - soften: a "soften" verdict for id N replaces item N's rendered wording with the model's `line` (escaped), but the item still appears.
      - drop: a "drop" verdict for id N removes item N from the list.
      - INVENT no-op: a verdict for id 999 (never raised) adds nothing.
      - DROP-UNKNOWN no-op: a "drop" for an id not in the set changes nothing.
      - the worth-a-look ids must map by the SAME stable index buildFacts assigned.
    - flagFairness.test.ts (network-free, over the labelled golden cases): for the meetings labelled genuine client work (Case B / worth-a-look-labelled), assert that NO sequence of model verdicts can cause applyVerdicts to remove them WHEN the verdict is anything other than an explicit, matched "drop" — and document/encode the rule that the eval (Task 3) enforces the model never EMITS a drop for those. The structural half here: a genuine-labelled flag with NO verdict (the toggle-OFF / model-silent case) is always kept.
    - Tests MUST fail first (applyVerdicts.ts does not exist) — RED.
  </behavior>
  <action>Write the two test files against the not-yet-existing src/llm/applyVerdicts.ts (`applyVerdicts(worthALook: WorthALookItem[], verdicts: LlmOutput["meetingVerdicts"]): WorthALookItem[]`). Use node:test + assert/strict. For flagFairness.test.ts, load labelled-events.json via the readFileSync(new URL(...)) pattern and select the genuine-client-work-labelled entries as the protected set. Do not implement applyVerdicts. Keep these tests pure — no network, no key.</action>
  <verify>
    <automated>node --import tsx --test "src/llm/applyVerdicts.test.ts" "src/llm/flagFairness.test.ts" 2>&1 | grep -q "fail" && echo "RED ok" || (echo "expected RED" && exit 1)</automated>
  </verify>
  <done>Both test files exist, import the missing applyVerdicts module, and FAIL (RED). No network/key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement applyVerdicts + extend prompt/assemble behind a default-OFF toggle</name>
  <files>src/llm/applyVerdicts.ts, src/llm/prompt.ts, src/llm/assemble.ts, src/config.ts, src/index.ts</files>
  <read_first>05-AI-SPEC.md §4 (verdict semantics) + §6 (invent/harden suppression = no-op); src/llm/assemble.ts (the Slice-2 seam from 05-01); src/llm/prompt.ts (SYSTEM_PROMPT + buildFacts); src/config.ts (the committed-config conventions — add the toggle here as a documented constant default OFF).</read_first>
  <action>
    1. src/llm/applyVerdicts.ts: implement `applyVerdicts(worthALook, verdicts)` — pure, never throws. Build the worth-a-look list with the SAME stable indexing buildFacts uses. For each item index: find a matching verdict by id; "keep" or no verdict → unchanged; "soften" → replace the rendered title/wording with `escapeHtml(verdict.line)` (numbers/duration still from the item — the line carries no numbers); "drop" → omit. Any verdict whose id is not a current worth-a-look index → ignored (no-op). The function can only shrink or reword the list, never grow it (AI-SPEC §6 invent suppression).
    2. src/llm/prompt.ts: extend SYSTEM_PROMPT with the keep/soften/drop semantics + the precision-focused bias-to-silence rule ("may soften or drop a borderline flag; NEVER invent a flag; NEVER drop a meeting that is genuine client work") and one inline few-shot example of a borderline meeting → soften (AI-SPEC §4b few-shot). Ensure buildFacts already emits worth-a-look as `{ id, title, durationLabel }` with id = stable index (it does, from Slice 1) — confirm the ids are exactly what applyVerdicts keys on.
    3. src/llm/assemble.ts: at the Slice-2 seam, when the toggle is ON, call `applyVerdicts(ctx.worthALook-derived list, out.meetingVerdicts)` and render the resulting list; when OFF, render the raw reconciler list unchanged (byte-identical to Slice 1). Numbers/rows/week-bar/links remain sourced from report/ctx.
    4. src/config.ts: add `export const USE_LLM_MEETING_JUDGMENT` defaulting to `false` (read from `process.env.USE_LLM_MEETING_JUDGMENT === "true"` at the wiring point, or a committed const — match the file's existing config style; document it ships OFF until trusted).
    5. src/index.ts: thread the toggle so the verdict application only runs when both USE_LLM_RENDERER and the meeting-judgment toggle are on. With the judgment toggle off, the LLM still writes prose (Slice 1) but verdicts are ignored.
    Per CLAUDE.md scope discipline: touch only these files; do not alter reconcile.ts or the numbers path.
  </action>
  <verify>
    <automated>node --import tsx --test "src/llm/applyVerdicts.test.ts" "src/llm/flagFairness.test.ts" && npx tsc --noEmit</automated>
  </verify>
  <done>Task 1 tests PASS (GREEN). tsc clean. Toggle defaults OFF → byte-identical to Slice 1. With it ON, verdicts apply; invented/unknown ids are no-ops; genuine-labelled flags can't vanish without an explicit matched drop.</done>
</task>

<task type="auto">
  <name>Task 3: Offline flag-fairness eval harness + full-suite regression</name>
  <files>scripts/eval-llm-renderer.ts</files>
  <read_first>05-AI-SPEC.md §5 (Eval Tooling + Reference Dataset — the offline tsx harness over labelled-events.json, dev key, NOT in CI) and §7; src/calendar/__fixtures__/labelled-events.json; src/llm/prompt.ts + renderLlm.ts (the real call to reuse).</read_first>
  <action>Create scripts/eval-llm-renderer.ts — a one-off `tsx` script (NOT a node:test, so CI never runs it) that: loads labelled-events.json, builds the facts payload for the worth-a-look candidates, runs the REAL prompt via the Slice-1 client using `process.env.ANTHROPIC_API_KEY` (the personal dev key), collects the keep/soften/drop verdicts, and prints a pass/fail table scoring each verdict against the PM label. The cardinal assertion: flag any case where the model DROPPED a meeting labelled genuine client work (a hard fail — the never-drop rule). Print a summary line (n cases, drops-of-genuine count, soften/keep counts). Exit non-zero if any genuine flag was dropped so the harness is usable as a manual pre-ship gate. Add a short header comment: run with `ANTHROPIC_API_KEY=$DEV_KEY tsx scripts/eval-llm-renderer.ts`; this is the manual gate before changing the prompt/model/few-shots; it is intentionally excluded from `npm test`. Confirm `npm test` still matches only `src/**/*.test.ts` so the harness isn't picked up. Then run the full suite.</action>
  <verify>
    <automated>npm test && npx tsc --noEmit && test -f scripts/eval-llm-renderer.ts</automated>
  </verify>
  <done>Full `npm test` green (no regression; harness not picked up by the test glob). tsc clean. The offline harness exists, runs the real prompt over the labelled set on the dev key, prints a keep/soften/drop pass/fail table, and exits non-zero if the model dropped a genuine-client-work flag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The fuzzy meeting-judgment layer (toggle ON) plus the offline flag-fairness harness scored against the Phase-4 labelled real meetings.</what-built>
  <how-to-verify>
    1. Run `ANTHROPIC_API_KEY=$DEV_KEY tsx scripts/eval-llm-renderer.ts` and read the keep/soften/drop table.
    2. Check: did the model drop any meeting you (as PM/resourcing owner) consider genuine client work? (Must be ZERO — that is the never-drop rule.)
    3. Check: are the softened/dropped flags ones a PM would reasonably agree are borderline/overhead? Over-flagging should go down, never up.
    4. Confirm the toggle defaults OFF (Slice 2 ships disabled) and that with it OFF the card is byte-identical to the Slice-1 output.
  </how-to-verify>
  <resume-signal>Type "approved" if no genuine flag was dropped and the verdicts read fair, or describe the misjudged flags so the prompt/few-shots can be tuned and the harness re-run.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model verdicts → worth-a-look list | Untrusted keep/soften/drop verdicts adjudicate the deterministic reconciler's flags. |
| model line → Cards v2 HTML | Softened wording crosses into the card. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Spoofing/Tampering | model invents/hardens a flag | mitigate | applyVerdicts can only shrink/reword the reconciler's list; unknown/invented ids are no-ops (AI-SPEC §6). Structural test enforces it. |
| T-05-06 | Tampering | model drops a genuine flag | mitigate | Offline flag-fairness eval over the PM-labelled set; harness exits non-zero on any drop of genuine client work; ships behind a default-OFF toggle until trusted. |
| T-05-07 | Tampering | softened `line` → Cards v2 HTML | mitigate | escapeHtml the model line before insertion (reused renderer primitive). |
| T-05-08 | Tampering | verdicts contaminate numbers | accept→mitigate | applyVerdicts only touches the worth-a-look title list; numbers/rows/week-bar/links stay sourced from report/ctx (Slice-1 number-fidelity test still covers the assembled output). |
</threat_model>

<verification>
- `npm test` green across all phases; the eval harness is NOT picked up by the test glob.
- `npx tsc --noEmit` clean.
- Toggle OFF (default) → byte-identical to Slice 1 (clean separability).
- applyVerdicts proven to never invent a flag and to no-op unknown ids.
- Offline harness scores keep/soften/drop against the Phase-4 labelled real meetings and hard-fails on any drop of genuine client work.
</verification>

<success_criteria>
- LLM-02 satisfied: the optional LLM adjudicates fuzzy meeting reconciliation from pre-computed facts only; numbers untouched; falls back to the template on any failure (inherited from Slice 1).
- Precision-focused, bias-to-silence: may soften/drop borderline flags, never invents one, never drops genuine client work.
- Ships behind a default-OFF toggle, cleanly separable from Slice 1.
</success_criteria>

<output>
Create `.planning/phases/05-llm-renderer-optional/05-02-SUMMARY.md` when done.
</output>
