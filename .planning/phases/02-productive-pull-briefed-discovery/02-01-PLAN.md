---
phase: 02-productive-pull-briefed-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - .env.example
  - src/productive/types.ts
  - src/productive/schemas.ts
  - src/productive/client.ts
  - src/productive/__tests__/schemas.test.ts
  - src/productive/__fixtures__/bookings-page.json
autonomous: false
requirements: [BRIEF-01]
user_setup:
  - service: productive.io
    why: "Read-only Productive API access to pull the three designers' bookings and absences"
    env_vars:
      - name: PRODUCTIVE_AUTH_TOKEN
        source: "Productive → Settings → API access → personal access token (X-Auth-Token)"
      - name: PRODUCTIVE_ORG_ID
        source: "The numeric org id prefix of the slug 34092-solvd-agency (candidate value 34092)"

must_haves:
  truths:
    - "An authenticated GET to Productive with X-Auth-Token + X-Organization-Id returns 200, not 403 (org-id value confirmed)"
    - "A raw /bookings JSON:API response is validated by zod safeParse using the corrected field names (booking_method_id, draft, canceled) and never throws"
    - "A network or shape failure returns a Result error value, never an exception across the boundary"
  artifacts:
    - path: "src/productive/client.ts"
      provides: "fetch + 3-header + pagination JSON:API client returning Result<T>, never throwing"
      contains: "X-Auth-Token"
    - path: "src/productive/schemas.ts"
      provides: "zod boundary schemas for bookings/page with corrected API field names"
      contains: "booking_method_id"
    - path: "src/config.ts"
      provides: "designer person IDs, org id env wiring, NSW region, studio closures list"
      contains: "686717"
    - path: ".env.example"
      provides: "committed secret-name template (no values)"
      contains: "PRODUCTIVE_AUTH_TOKEN"
  key_links:
    - from: "src/productive/client.ts"
      to: "process.env.PRODUCTIVE_AUTH_TOKEN / PRODUCTIVE_ORG_ID"
      via: "header construction from env (never hardcoded)"
      pattern: "process\\.env\\.PRODUCTIVE_(AUTH_TOKEN|ORG_ID)"
    - from: "src/productive/client.ts"
      to: "src/productive/schemas.ts"
      via: "safeParse of every fetched page before returning"
      pattern: "safeParse"
---

<objective>
Establish the Productive ingestion spine: a non-throwing JSON:API fetch client (3 headers from env, full pagination), zod boundary schemas using the CORRECTED API field names, the committed non-secret config, and a live auth probe that confirms the exact `X-Organization-Id` value before any later plan trusts it.

Purpose: Every later slice depends on being able to authenticate to Productive and turn raw JSON into validated, never-throwing values. This plan proves that end-to-end (real 200 response + a captured fixture the schema parses) and pins down the two execution unknowns the research flagged: the org-id value (A1) and that the corrected field names match a real response (Pitfall 1).
Output: `src/config.ts`, `.env.example`, `src/productive/{types,schemas,client}.ts`, a schema test against a captured real-response fixture, and a confirmed working org-id value documented in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md
@.planning/phases/02-productive-pull-briefed-discovery/02-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Phase 1 house style the executor must mirror (analogs). No codebase exploration needed beyond these. -->

Module style + import rules (mandatory, from tsconfig nodenext + verbatimModuleSyntax):
- Relative imports MUST carry the `.ts` extension: `import { x } from "./schemas.ts"`.
- Type-only imports MUST use `import type`.
- ESM only (`"type": "module"`); no axios/got — native `fetch` only.
- Every module opens with a `/** ... */` doc-header stating what it does, its trust posture, and the D-IDs it implements (see src/domain/capacity.ts lines 1-14 for the exact shape).

Non-throwing posture to mirror (src/domain/capacity.ts lines 67-69):
  function safeMinutes(minutes: number): number { return Number.isFinite(minutes) ? minutes : 0; }

Constant-export + rationale style to mirror (src/domain/types.ts lines 27-35):
  export const STUDIO_ZONE = "Australia/Sydney" as const;

Test skeleton to mirror (src/domain/__tests__/capacity.test.ts lines 1-22):
  import { describe, it } from "node:test";
  import assert from "node:assert/strict";

Test runner (package.json): `npm test` → `node --import tsx --test "src/**/*.test.ts"` — the glob already covers `src/productive/__tests__/`. No config change.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify + install net-new dependencies (zod, date-holidays, dotenv)</name>
  <what-built>This phase adds three net-new packages. slopcheck could not run at research time, so all are tagged [ASSUMED] and need a human legitimacy check before install (threat T-02-SC). All three are CLAUDE.md/D-13-locked, long-lived, high-download, with public source repos.</what-built>
  <read_first>
    - .planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md (section "Package Legitimacy Audit", lines 102-116)
    - ./CLAUDE.md (Recommended Stack — zod, dotenv; What NOT to Use)
  </read_first>
  <how-to-verify>
    1. Confirm each package on its registry page:
       - https://www.npmjs.com/package/zod  (expect: github.com/colinhacks/zod, 7+ yrs, very high downloads)
       - https://www.npmjs.com/package/date-holidays  (expect: github.com/commenthol/date-holidays, latest 3.30.x ~2026-05)
       - https://www.npmjs.com/package/dotenv  (expect: github.com/motdotla/dotenv, 7+ yrs, very high downloads)
    2. Confirm none are typosquats (exact names: `zod`, `date-holidays`, `dotenv`).
    3. On approval, the executor runs: `npm install zod date-holidays` and `npm install --save-dev dotenv`.
  </how-to-verify>
  <acceptance_criteria>
    - All three npmjs.com pages reviewed and names confirmed exact (no typosquat).
    - After install: `node -e "require('zod'); require('date-holidays')"` exits 0; `package.json` lists `zod` + `date-holidays` in dependencies and `dotenv` in devDependencies.
  </acceptance_criteria>
  <resume-signal>Type "approved" to install, or name any package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Config + secret template + zod boundary schemas (corrected field names)</name>
  <files>src/config.ts, .env.example, src/productive/types.ts, src/productive/schemas.ts, src/productive/__fixtures__/bookings-page.json</files>
  <read_first>
    - src/domain/types.ts (constant-export + rationale style; STUDIO_ZONE/TARGET_MINUTES STAY here — do not move)
    - .planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md (sections "/bookings attributes (exact field names)", "Pattern 3: zod at the boundary", "Pitfall 1")
    - .planning/phases/02-productive-pull-briefed-discovery/02-PATTERNS.md (sections "src/config.ts", "src/productive/types.ts", "src/productive/schemas.ts")
    - .gitignore (confirms .env ignored, .env.example committed)
  </read_first>
  <action>
    Create `src/config.ts` exporting non-secret committed config (per D-14/D-15): `DESIGNER_PERSON_IDS = ["686717","686712","686716"] as const` (Liam/Anisha/Ella), `PRODUCTIVE_BASE_URL = "https://api.productive.io/api/v2" as const`, `HOLIDAY_REGION = { country: "AU", state: "NSW" } as const`, and `STUDIO_CLOSURES: readonly string[]` (empty array placeholder + doc comment for committed "yyyy-MM-dd" studio shutdown days). Secrets (`PRODUCTIVE_AUTH_TOKEN`, `PRODUCTIVE_ORG_ID`) are read from `process.env` in client.ts — NEVER placed in this committed file (D-15). Add the doc-header rationale block mirroring src/domain/types.ts.
    Create `.env.example` (committed template, no real values) with `PRODUCTIVE_AUTH_TOKEN=` and `PRODUCTIVE_ORG_ID=` plus a one-line comment for each.
    Create `src/productive/types.ts` holding ingestion-internal raw types ONLY (never imported into src/domain): raw booking attributes, task, workflow_status shapes. Doc-header notes the boundary rule.
    Create `src/productive/schemas.ts`: zod schemas validating only the fields the phase uses, tolerant of extra fields (use loose/passthrough so a new Productive field never breaks the pull). Use the CORRECTED attribute names: `booking_method_id` (number), `time`/`total_time`/`percentage` (number nullable), `started_on`/`ended_on` (string), `draft` (boolean), `canceled` (boolean), `booking_type` (string), `approval_status` (number nullable). Booking relationships: `person`, `task`, `service`, `event` (each `{ data: {id,type}|null }`). Add a `JsonApiPage` schema with `data: array`, `included: array optional`, and `meta` carrying `current_page`, `total_pages`, `total_count`, `page_size`. Export `safeParse`-usable schemas; do NOT export a `.parse` wrapper.
    Capture a real `/bookings` response into `src/productive/__fixtures__/bookings-page.json` (one page, the three designers, target window) — captured during Task 4's live probe; for now create the file with a representative real response so the schema test can run. (If the live capture in Task 4 differs, update this fixture.)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/productive/schemas.ts` uses `booking_method_id`, `draft`, `canceled` — NOT `booking_method`, `is_draft`, `is_canceled` (grep: `grep -E "is_draft|is_canceled|booking_method[^_]" src/productive/schemas.ts` returns nothing).
    - `src/config.ts` contains the three person IDs `686717`, `686712`, `686716` and does NOT contain any token/secret value (grep: `grep -iE "auth.?token|x-auth" src/config.ts` returns nothing).
    - `.env.example` contains `PRODUCTIVE_AUTH_TOKEN` and `PRODUCTIVE_ORG_ID` with empty values (no real token).
    - `src/productive/types.ts` does not import from `../domain/` (grep: `grep "domain" src/productive/types.ts` returns nothing) — and nothing in src/domain imports from src/productive.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Config, secret template, ingestion raw types, and zod boundary schemas exist with corrected field names; typecheck passes; secrets live only in env.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Non-throwing JSON:API client + pagination + schema fixture test</name>
  <files>src/productive/client.ts, src/productive/__tests__/schemas.test.ts</files>
  <behavior>
    - schemas.test.ts: parsing the captured real fixture (bookings-page.json) via safeParse succeeds (`.success === true`).
    - schemas.test.ts: a deliberately malformed page (missing `meta.total_pages`) safeParses to `.success === false` (degrades, no throw).
    - schemas.test.ts: a booking object using the OLD names (`is_draft`) fails the corrected schema — guards against Pitfall 1 regression.
    - client.ts (unit, no network): `getJson` of a stubbed non-ok fetch returns `{ ok: false, error }`; a thrown fetch returns `{ ok: false, error }` — never throws.
  </behavior>
  <read_first>
    - .planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md (Pattern 1 "Result type", Pattern 2 "Paginate until total_pages", Auth headers lines 122-127, Security lines 531-537)
    - .planning/phases/02-productive-pull-briefed-discovery/02-PATTERNS.md (section "src/productive/client.ts")
    - src/domain/__tests__/capacity.test.ts (node:test skeleton + fixture-builder helper style)
    - src/productive/schemas.ts (the schemas this client validates against)
  </read_first>
  <action>
    Create `src/productive/client.ts`: a `Result<T> = { ok: true; value: T } | { ok: false; error: string }` discriminated union; a `getJson(url, headers)` that wraps `fetch`, returns `{ ok:false, error: "HTTP <status>" }` on non-ok and `{ ok:false, error }` on a thrown exception (NEVER throws). Build the three headers from env: `X-Auth-Token: process.env.PRODUCTIVE_AUTH_TOKEN`, `X-Organization-Id: process.env.PRODUCTIVE_ORG_ID`, `Content-Type: application/vnd.api+json`. Add a `fetchAllPages(path, query)` that requests `page[size]=200&page[number]=N`, safeParses each page with `JsonApiPage`, accumulates `data` + `included`, and loops until `current_page >= total_pages` — never assumes one page. On any fetch error or shape-drift, return a Result error (degrade). SECURITY: never log the token or a full URL containing it; if logging a URL for debug, redact auth. Use `PRODUCTIVE_BASE_URL` from config.ts.
    Create `src/productive/__tests__/schemas.test.ts` covering the behaviors above using the captured fixture and the node:test skeleton. For the client unit tests, stub `globalThis.fetch` (no real network in unit tests).
  </action>
  <verify>
    <automated>npm test -- 2>&1 | grep -q "pass" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx --test "src/productive/__tests__/schemas.test.ts"` exits 0.
    - `client.ts` returns a Result on both non-ok and thrown-fetch paths (no `throw` statement reachable across the boundary; grep: client has no bare `throw` outside guard helpers).
    - Headers are built from `process.env.PRODUCTIVE_AUTH_TOKEN` / `process.env.PRODUCTIVE_ORG_ID` (grep both present).
    - The malformed-page test asserts `.success === false`; the old-field-name (`is_draft`) test asserts failure.
  </acceptance_criteria>
  <done>A non-throwing, env-authenticated, paginating JSON:API client exists; schema + client behaviors are unit-tested against a real fixture and the corrected field names.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Live auth probe — confirm the exact X-Organization-Id value (A1)</name>
  <what-built>The research could not re-verify whether `X-Organization-Id` is the bare number `34092` or the slug `34092-solvd-agency` (A1 / Open Q1). A wrong value returns 403 on every call → a silent empty pull every night. This probe confirms the working value once, before any later plan trusts it, and captures the real fixture for Task 2's schema.</what-built>
  <read_first>
    - .planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md (Auth headers lines 122-127, Pitfall 2, Open Q1, A1)
    - src/productive/client.ts (the client under test)
  </read_first>
  <how-to-verify>
    1. Ensure a local `.env` exists (gitignored) with `PRODUCTIVE_AUTH_TOKEN` set and `PRODUCTIVE_ORG_ID=34092`.
    2. Executor runs a one-off `tsx` probe: GET `/people/686717` (or `/bookings?page[size]=1`) via client.ts, printing the HTTP status only (NEVER the token).
    3. Expected: HTTP 200. If 403, retry with `PRODUCTIVE_ORG_ID=34092-solvd-agency`. Record which value returned 200.
    4. With a 200, capture one real `/bookings` page (target→Friday window, the 3 person IDs) into `src/productive/__fixtures__/bookings-page.json` (strip nothing structural; this is the schema's ground truth). Confirm the captured response uses `booking_method_id`/`draft`/`canceled` (validates Pitfall-1 assumption against live data).
  </how-to-verify>
  <acceptance_criteria>
    - A 200 response is observed and the working `PRODUCTIVE_ORG_ID` value is recorded in the SUMMARY (explicitly: `34092` vs slug).
    - The captured fixture exists at `src/productive/__fixtures__/bookings-page.json` and parses under `JsonApiPage` (`npm test` schema test green).
    - The captured live booking attributes contain `booking_method_id`, `draft`, `canceled` (confirm against the fixture); if the live names differ, schemas.ts is corrected and the SUMMARY notes it.
    - No token value appears in any console output or committed file.
  </acceptance_criteria>
  <resume-signal>Type "200 confirmed, org-id=<value>" or paste the error if it 403s.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingestion → Productive API | Outbound read-only HTTPS; static API token crosses here (no inbound surface, no app auth). |
| Productive JSON → src/productive | Untrusted external JSON enters; zod safeParse is the validation gate. |
| src/productive → src/domain | Only clean typed values may cross (later plans); raw shapes must NOT leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | client.ts headers / logs | mitigate | Token + org-id read only from `process.env`; never logged, never in committed code; redact auth in any debug URL log (D-15). |
| T-02-02 | Information disclosure | .env vs committed config | mitigate | Secrets in gitignored `.env` / Actions secrets; only non-secret config (person IDs, region, closures) committed in config.ts; `.env.example` carries names not values (D-15). |
| T-02-03 | Denial of service | malformed/unexpected Productive JSON | mitigate | `JsonApiPage` zod `safeParse` + Result-type client; shape drift → Result error, never a thrown crash. |
| T-02-04 | Tampering | wrong X-Organization-Id → silent empty pull | mitigate | Live 200/403 probe (Task 4) confirms the value before any later plan trusts it; documented in SUMMARY. |
| T-02-SC | Tampering | npm installs (zod/date-holidays/dotenv, all [ASSUMED]) | mitigate | Task 1 blocking-human checkpoint verifies each on npmjs.com before install; never auto-approvable. |
</threat_model>

<verification>
- `npm test` passes (schema fixture test + client unit tests green).
- `npx tsc --noEmit` exits 0.
- Live probe returned 200; working org-id value recorded.
- No secret value in any committed file: `grep -rIE "sk-|X-Auth-Token: [^$]" src/ .env.example` returns nothing.
</verification>

<success_criteria>
A confirmed-working, non-throwing, env-authenticated JSON:API client and corrected zod boundary schemas exist; a real /bookings page parses cleanly; the exact X-Organization-Id value is proven live. The spine root for every later slice is in place.
</success_criteria>

<output>
Create `.planning/phases/02-productive-pull-briefed-discovery/02-01-SUMMARY.md` when done. MUST record: the working `PRODUCTIVE_ORG_ID` value (34092 vs slug), confirmation that live field names match the corrected schema, and the captured fixture path.
</output>
