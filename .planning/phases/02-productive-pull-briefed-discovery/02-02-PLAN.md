---
phase: 02-productive-pull-briefed-discovery
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/productive/mappers.ts
  - src/holidays.ts
  - src/productive/__tests__/mappers.test.ts
  - src/holidays.test.ts
autonomous: true
requirements: [BRIEF-01]

must_haves:
  truths:
    - "A raw Productive booking is normalized to exact minutes-on-the-target-day for all three booking_method_id values (1 per-day, 3 total-hours, 2 percentage)"
    - "Work bookings (booking_type=service) become Booking[] and absence bookings (booking_type=event) become Absence[]; isTentative ⟺ draft===true"
    - "NSW public holidays plus committed studio closures produce a HolidaySet the Phase 1 clock accepts, with date keys that match the clock's toISODate() keys"
    - "The mapped Booking[]/Absence[]/HolidaySet feed computeStudioReport and produce capacity numbers without throwing"
  artifacts:
    - path: "src/productive/mappers.ts"
      provides: "per-day minutes normalization (D-09) + Productive→Booking/Absence split (D-11)"
      contains: "booking_method_id"
    - path: "src/holidays.ts"
      provides: "date-holidays(NSW) + studio closures → HolidaySet (D-13)"
      contains: "AU"
  key_links:
    - from: "src/productive/mappers.ts"
      to: "src/domain/types.ts"
      via: "imports TARGET_MINUTES (value) + Booking/Absence/DesignerId (type-only)"
      pattern: "from \"\\.\\./domain/types\\.ts\""
    - from: "src/productive/mappers.ts"
      to: "src/domain/clock.ts"
      via: "imports isWorkingDay for method-3 working-days-in-range count"
      pattern: "isWorkingDay"
    - from: "src/holidays.ts"
      to: "src/domain/clock.ts"
      via: "produces the HolidaySet that nextWorkingDay/restOfWeekWindow consume"
      pattern: "toISODate"
---

<objective>
Turn validated raw Productive bookings into the exact Phase-1 contracts (`Booking[]`, `Absence[]`) and wire `date-holidays` (NSW) plus committed studio closures into the `HolidaySet` the Phase-1 clock already accepts.

Purpose: This is the slice that makes the capacity half of the pipeline real end-to-end — after this plan, validated Productive data flows into `computeStudioReport` and produces correct available/booked/open numbers. The trust-critical per-day minutes math (D-09) and the holiday-key gotcha (Pitfall 4) are both pinned down here with unit tests against fixtures.
Output: `src/productive/mappers.ts`, `src/holidays.ts`, and their tests; a demonstrable path from raw JSON → Booking/Absence/HolidaySet → StudioReport.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md
@.planning/phases/02-productive-pull-briefed-discovery/02-PATTERNS.md

<interfaces>
<!-- Phase 1 contracts this plan maps INTO. Extracted from src/domain — no exploration needed. -->

From src/domain/types.ts (mappers OUTPUT exactly these; import Booking/Absence/DesignerId as `import type`, TARGET_MINUTES as value):
  export const TARGET_MINUTES = 450 as const;
  export const STUDIO_ZONE = "Australia/Sydney" as const;   // value import for luxon zone
  export interface Booking { designerId: DesignerId; minutes: number; isTentative: boolean; }
  export interface Absence { designerId: DesignerId; minutes: number; }
  export type DesignerId = string & { readonly __brand: "DesignerId" };
  export type HolidaySet = ReadonlySet<string>;   // "yyyy-MM-dd" studio-zone keys

From src/domain/clock.ts (REUSE — do not re-derive weekday math):
  export function isWorkingDay(day: DateTime, holidays: HolidaySet): boolean;  // false for Sat/Sun + holiday keys

Defensive coercion to mirror (src/domain/capacity.ts lines 67-69):
  Number.isFinite(x) ? x : 0   // apply to time/total_time/percentage; default branch returns 0, never throws

date-holidays usage (RESEARCH Pattern 6, VERIFIED github.com/commenthol/date-holidays):
  new Holidays("AU", "NSW"); hd.getHolidays(year) → [{ date, start: Date, end, name, type }]; filter type === "public".
  GOTCHA: h.date is "YYYY-MM-DD HH:mm:ss" (space+time) — do NOT push raw. Use DateTime.fromJSDate(h.start, { zone: STUDIO_ZONE }).toISODate().

Per-day minutes (D-09, RESEARCH Pattern 5):
  method 1 (per day) → time ?? 0
  method 3 (total hours) → workingDaysInRange > 0 ? Math.round((total_time ?? 0) / workingDaysInRange) : 0
  method 2 (percentage) → Math.round(((percentage ?? 0) / 100) * TARGET_MINUTES)
  default → 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Holiday set from date-holidays(NSW) + studio closures (D-13)</name>
  <files>src/holidays.ts, src/holidays.test.ts</files>
  <behavior>
    - A known NSW public holiday (e.g. Australia Day 2026 = 2026-01-26) appears in the set as the exact key "2026-01-26".
    - The returned key format matches the clock's toISODate() output (no time component, no space).
    - Committed STUDIO_CLOSURES from config.ts are merged into the same set.
    - Enumerating two years (current + next) covers a December-Friday-targeting-January window.
    - A non-public holiday type is excluded.
  </behavior>
  <read_first>
    - .planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md (Pattern 6, Pitfall 4, A5)
    - .planning/phases/02-productive-pull-briefed-discovery/02-PATTERNS.md (section "src/holidays.ts")
    - src/domain/clock.ts (isWorkingDay / toISODate key contract — this file PRODUCES what clock consumes)
    - src/domain/types.ts (HolidaySet = ReadonlySet<string>, STUDIO_ZONE)
    - src/config.ts (HOLIDAY_REGION, STUDIO_CLOSURES)
  </read_first>
  <action>
    Create `src/holidays.ts` exporting `buildHolidaySet(years: number[], closures: readonly string[]): HolidaySet`. Construct `new Holidays(HOLIDAY_REGION.country, HOLIDAY_REGION.state)` ("AU","NSW"), iterate the given years, `getHolidays(year)`, filter `type === "public"`, and for each derive the key via `DateTime.fromJSDate(h.start, { zone: STUDIO_ZONE }).toISODate()` (NOT `h.date` raw — Pitfall 4). Seed the set with the committed `closures` ("yyyy-MM-dd"). Import `STUDIO_ZONE` (value) from `../domain/types.ts`... note this file is at `src/` so import path is `./domain/types.ts`. Add a helper to derive the years from a target window if useful, or accept explicit years. Doc-header cites D-13.
    Create `src/holidays.test.ts` covering the behaviors using node:test + node:assert/strict.
  </action>
  <verify>
    <automated>node --import tsx --test "src/holidays.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx --test "src/holidays.test.ts"` exits 0.
    - Test asserts the set contains `"2026-01-26"` (Australia Day) as an exact string key.
    - holidays.ts derives keys from `h.start` via luxon, NOT from `h.date` (grep: `grep "h.date" src/holidays.ts` returns nothing, or only in a comment).
    - holidays.ts merges `STUDIO_CLOSURES` (grep: references config closures).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A HolidaySet builder produces clock-compatible "yyyy-MM-dd" keys from NSW public holidays + committed closures; unit-tested against a known holiday.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-day minutes normalization + Productive→Booking/Absence mappers</name>
  <files>src/productive/mappers.ts, src/productive/__tests__/mappers.test.ts</files>
  <behavior>
    - method 1 (per day): time=240, target day in [started_on,ended_on] → 240 minutes; target day outside range → 0.
    - method 3 (total hours): total_time=480 over Jun 3–4 (2 working days) → 240/day (D-09 real example); workingDaysInRange=0 → 0 (no divide-by-zero/Infinity).
    - method 2 (percentage): percentage=50 → Math.round(0.5 * 450) = 225.
    - unknown booking_method_id → 0 (never throws).
    - null/NaN time/total_time/percentage coerced to 0.
    - mapBookings splits booking_type=service → Booking[] and booking_type=event → Absence[]; isTentative ⟺ draft===true (NOT approval_status).
    - person id maps to the branded DesignerId; canceled bookings already filtered upstream (assert mapper does not silently include them if present).
  </behavior>
  <read_first>
    - .planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md (Pattern 5, D-09, D-07, D-11, Pitfall 5, Open Q3)
    - .planning/phases/02-productive-pull-briefed-discovery/02-PATTERNS.md (section "src/productive/mappers.ts")
    - src/domain/types.ts (Booking/Absence/DesignerId/TARGET_MINUTES — the exact output shapes)
    - src/domain/clock.ts (isWorkingDay — reuse for working-days-in-range)
    - src/domain/capacity.ts (safeMinutes coercion style to mirror)
    - src/productive/schemas.ts (the validated input shapes from plan 02-01)
  </read_first>
  <action>
    Create `src/productive/mappers.ts`. Export `minutesOnDay(attrs, dayKey, workingDaysInRange)` implementing D-09: method 1 → `time ?? 0`; method 3 → `workingDaysInRange > 0 ? Math.round((total_time ?? 0)/workingDaysInRange) : 0`; method 2 → `Math.round(((percentage ?? 0)/100) * TARGET_MINUTES)`; `default → 0`. Guard a day outside `[started_on, ended_on]` → 0 (use yyyy-MM-dd string compare, which is exact). Apply `Number.isFinite(...) ? x : 0` coercion. Export `workingDaysInRange(started_on, ended_on, holidays)` that counts inclusive weekdays excluding holidays by reusing `isWorkingDay` from `../domain/clock.ts` (Open Q3 decision: holidays DO reduce the divisor — document this). Export `mapToBookingsAndAbsences(rawBookings, targetDayKey, holidays)` returning `{ bookings: Booking[], absences: Absence[] }`: split by `booking_type` (`service`→Booking, `event`→Absence), set `minutes = minutesOnDay(...)` for the target day, set `isTentative = draft === true` for work bookings (D-07). Map `person` relationship id to `DesignerId` (cast through the brand). Productive raw shapes stay in this file — output is ONLY clean domain types.
    Create `src/productive/__tests__/mappers.test.ts` covering all behaviors with a local fixture-builder helper (mirror capacity.test.ts), D-ID-cited describe/it titles (e.g. "480 min over Jun 3–4 = 240/day").
  </action>
  <verify>
    <automated>node --import tsx --test "src/productive/__tests__/mappers.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx --test "src/productive/__tests__/mappers.test.ts"` exits 0.
    - A test asserts method-3 `total_time=480 / 2 working days = 240` and a separate test asserts `workingDaysInRange=0 → 0` (no Infinity).
    - A test asserts `isTentative === (draft === true)` and that `approval_status` does NOT drive isTentative (grep: `grep "approval_status" src/productive/mappers.ts` returns nothing).
    - mappers.ts imports `isWorkingDay` from `../domain/clock.ts` and `TARGET_MINUTES` from `../domain/types.ts` (no re-derived weekday math; grep both imports present).
    - mappers.ts output type is `Booking`/`Absence` only; nothing in src/domain imports mappers.ts.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Validated raw bookings normalize to exact target-day minutes across all three booking methods and split cleanly into Phase-1 Booking[]/Absence[]; trust-critical math is unit-tested with fixtures including the real D-09 example.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/productive → src/domain | Mappers convert raw Productive shapes to clean Booking/Absence; raw shapes must NOT cross. |
| Productive numeric fields → arithmetic | Untrusted nullable numbers (time/total_time/percentage) feed minute math. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | leaking Productive shapes into src/domain | mitigate | Mappers emit only Booking/Absence/HolidaySet; acceptance grep confirms src/domain has no productive import; type-only domain imports keep the boundary one-way. |
| T-02-06 | Denial of service | divide-by-zero / NaN in D-09 minutes math | mitigate | `workingDaysInRange > 0` guard; `Number.isFinite ? x : 0` coercion; `default → 0` for unknown booking_method_id — never throws (mirrors Phase 1 safeMinutes). |
| T-02-07 | Tampering | holiday keys silently mismatching the clock (Pitfall 4) | mitigate | Derive keys from `h.start` via luxon `toISODate()` in STUDIO_ZONE; unit test asserts a known holiday key matches the clock's format. |
| T-02-08 | Tampering | wrong tentative signal (approval_status vs draft) | mitigate | isTentative ⟺ `draft===true` only (D-07); acceptance grep forbids approval_status in mapper. |
</threat_model>

<verification>
- `npm test` passes (holidays + mappers tests green) — run `node --import tsx --test "src/**/*.test.ts"`.
- `npx tsc --noEmit` exits 0.
- src/domain imports nothing from src/productive: `grep -rl "productive" src/domain` returns nothing.
- A manual smoke check: a small ad-hoc script mapping the captured fixture (02-01) → Booking[]/Absence[] → computeStudioReport runs without throwing (optional, but recommended to confirm the end-to-end capacity slice).
</verification>

<success_criteria>
Validated Productive bookings become exact Phase-1 Booking[]/Absence[] across all three booking methods, and NSW holidays + studio closures produce a clock-compatible HolidaySet — the capacity half of the pipeline now runs end-to-end into computeStudioReport.
</success_criteria>

<output>
Create `.planning/phases/02-productive-pull-briefed-discovery/02-02-SUMMARY.md` when done. Record the Open-Q3 decision (holidays reduce the method-3 divisor) and any deviation found when mapping the real fixture.
</output>
