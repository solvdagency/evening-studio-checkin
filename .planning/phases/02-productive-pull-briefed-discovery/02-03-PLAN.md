---
phase: 02-productive-pull-briefed-discovery
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/productive/briefed.ts
  - src/productive/brief.ts
  - src/productive/__tests__/briefed.test.ts
autonomous: false
requirements: [BRIEF-01, BRIEF-02, BRIEF-03]

must_haves:
  truths:
    - "A per-workflow map of the Briefed status position is built dynamically from /workflow_statuses (the 6 status IDs are NOT hardcoded)"
    - "A task is briefed ⟺ its workflow-status position ≥ that workflow's Briefed position AND its description is non-empty; a workflow with no Briefed status fails safe to not-briefed"
    - "Each confirmed target-day client booking yields a BriefFlag when it has no task, a not-briefed task, or a blank brief — keyed by job/task, never by PM"
    - "Internal/non-client bookings (project with no client company) are excluded from brief flags but still count toward hours"
  artifacts:
    - path: "src/productive/briefed.ts"
      provides: "dynamic Briefed-position map + isBriefed(task) per D-02/D-03/D-04"
      contains: "position"
    - path: "src/productive/brief.ts"
      provides: "BriefFlag output shape (BRIEF-01/02/03), surfaced by job/task never PM"
      contains: "reason"
  key_links:
    - from: "src/productive/briefed.ts"
      to: "Productive /workflow_statuses"
      via: "dynamic position lookup by Briefed status name per workflow"
      pattern: "position"
    - from: "src/productive/brief.ts"
      to: "BriefFlag.reason"
      via: "no-task | not-briefed | blank-brief discrimination"
      pattern: "no-task|not-briefed|blank-brief"
---

<objective>
Implement the BRIEF core: dynamically resolve each workflow's "Briefed" column position from `/workflow_statuses`, judge whether a target-day booking's task is briefed (at/past Briefed + non-empty description), and emit `BriefFlag`s by job/task for the three failure modes — confirming the briefed mapping and the internal-vs-client signal against live Productive data before trusting them.

Purpose: This is BRIEF-01/02/03 — the phase's reason for existing. The load-bearing subtlety (D-02: at-OR-PAST Briefed, not equals) and the false-trust guard (D-04: blank-brief) are both pinned here. Two execution unknowns get verified live: the include-chain depth (A7) and the D-06 internal/client signal (A2/A3/A4).
Output: `src/productive/briefed.ts`, `src/productive/brief.ts`, their tests, and live-confirmed answers to the include-depth and internal/client questions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md
@.planning/phases/02-productive-pull-briefed-discovery/02-PATTERNS.md
@.planning/phases/02-productive-pull-briefed-discovery/02-CONTEXT.md

<interfaces>
<!-- Contracts + analogs the executor needs. -->

From src/domain/types.ts:
  export type DesignerId = string & { readonly __brand: "DesignerId" };

BriefFlag output shape to build (RESEARCH "Brief-check Output Shape" lines 415-422):
  export interface BriefFlag {
    designerId: DesignerId;
    bookingId: string;            // for Phase 3 deep-link (MSG-06)
    taskId: string | null;        // null = no task linked (BRIEF-01)
    jobLabel: string;             // project/task title — the "job", NEVER a PM
    reason: "no-task" | "not-briefed" | "blank-brief";
    isTentative: boolean;         // context only; tentative is NOT brief-flagged (D-05)
  }

isBriefed logic (RESEARCH Pattern 4, mirror src/domain/capacity.ts classifyDay pure-function style):
  function isBriefed(taskStatus: { workflowId: string; position: number; descriptionNonEmpty: boolean }, map: Map<string, number>): boolean {
    const briefedPos = map.get(taskStatus.workflowId);
    if (briefedPos === undefined) return false;     // D-03: no Briefed column → fail safe
    return taskStatus.position >= briefedPos && taskStatus.descriptionNonEmpty;  // D-02 + D-04
  }

Live-confirmed Briefed positions (from the discovery spike, CONTEXT D-01): SOLVD Standard workflow → Briefed at position 3; SOLVD Design Retainers → position 2. Resolve dynamically by status name "Briefed"; do NOT hardcode these.

D-06 internal/client (RESEARCH "D-06 Internal-vs-Client Distinction", A2/A3/A4): exclude from brief flags ONLY when the booking's service→project chain resolves to a project with NO `company` relationship (no client company). company-absence is the load-bearing signal; project_type_id enum direction is disputed — do NOT hardcode the integer. Internal bookings still count toward hours.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dynamic Briefed-position resolution + isBriefed (D-02/D-03/D-04)</name>
  <files>src/productive/briefed.ts, src/productive/__tests__/briefed.test.ts</files>
  <behavior>
    - buildBriefedPositionMap from a /workflow_statuses fixture: produces { workflowId → position of the status named "Briefed" }, resolved by NAME not hardcoded id.
    - isBriefed: position === briefedPos AND non-empty → true (at Briefed).
    - isBriefed: position > briefedPos (e.g. "Working on it") AND non-empty → true (PAST Briefed — the load-bearing D-02 case).
    - isBriefed: position < briefedPos (e.g. "Not Started") → false (before Briefed).
    - isBriefed: position ≥ briefedPos but description empty → false (D-04 blank-brief).
    - isBriefed: task's workflow has no "Briefed" status (not in map) → false (D-03 fail safe).
  </behavior>
  <read_first>
    - .planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md (Pattern 4, D-01/D-02/D-03/D-04, Pitfall 3, Workflow statuses section lines 158-161)
    - .planning/phases/02-productive-pull-briefed-discovery/02-PATTERNS.md (section "src/productive/briefed.ts")
    - src/domain/capacity.ts (classifyDay pure-function + fail-safe-default style to mirror)
    - src/productive/schemas.ts (workflow_status raw shape — add a schema here if missing)
  </read_first>
  <action>
    Create `src/productive/briefed.ts`. Export `buildBriefedPositionMap(workflowStatuses)` returning `Map<workflowId, number>` — for each status whose `name === "Briefed"`, record `{ workflowId → position }` (resolve the workflow id from the status's `workflow` relationship). Export `isBriefed(taskStatus, map)` per the interface logic above: `undefined → false` (D-03), `position >= briefedPos && descriptionNonEmpty` (D-02 + D-04). Use a strict equality on the name "Briefed" (case-sensitive per the live spike; document if a case-insensitive match is safer). NEVER hardcode the status IDs 101563/111230 etc. (D-03). Doc-header cites D-02/D-03/D-04 and warns about Pitfall 3 (status==Briefed is wrong).
    If schemas.ts (02-01) lacks a `workflow_statuses` schema, add it there or co-locate a small zod schema here for the `/workflow_statuses` response (`name`, `position`, `workflow` relationship).
    Create `src/productive/__tests__/briefed.test.ts` covering all behaviors with a fixture-builder helper; describe/it titles cite D-IDs and name the concrete case (e.g. "past Briefed (Working on it) still briefed").
  </action>
  <verify>
    <automated>node --import tsx --test "src/productive/__tests__/briefed.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx --test "src/productive/__tests__/briefed.test.ts"` exits 0.
    - A test asserts `position > briefedPos` returns true (the at-OR-past D-02 case) and `position < briefedPos` returns false.
    - A test asserts a non-empty-guard failure: at/past Briefed + empty description → false (D-04).
    - A test asserts a missing-Briefed workflow → false (D-03 fail safe).
    - briefed.ts does NOT contain the literal status ids `101563` or `111230` (grep returns nothing); positions are resolved by name "Briefed".
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Briefed positions resolve dynamically per workflow, and isBriefed correctly treats at-or-past-Briefed + non-empty as briefed while failing safe — the core BRIEF-02 judgment, unit-tested for the load-bearing edge cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BriefFlag emission by job/task for the three failure modes (BRIEF-01/02/03)</name>
  <files>src/productive/brief.ts, src/productive/__tests__/briefed.test.ts</files>
  <behavior>
    - A confirmed target-day client booking with no task → BriefFlag reason "no-task", taskId null (BRIEF-01 / D-10).
    - A confirmed booking whose task is before Briefed → reason "not-briefed" (BRIEF-02).
    - A confirmed booking whose task is at/past Briefed but description empty → reason "blank-brief" (D-04).
    - A confirmed booking fully briefed → no flag.
    - A tentative (draft=true) booking → NO flag regardless of brief state (D-05).
    - An internal booking (project with no client company) → NO flag even with no task (D-06); but it still appears in the capacity Booking[] (assert it is not suppressed from hours).
    - jobLabel is the project/task title; no field ever stores or surfaces a PM (BRIEF-03).
  </behavior>
  <read_first>
    - .planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md ("Brief-check Output Shape" lines 409-426, D-05/D-06/D-08/D-10, "D-06 Internal-vs-Client Distinction")
    - .planning/phases/02-productive-pull-briefed-discovery/02-PATTERNS.md (section "src/productive/brief.ts")
    - src/domain/capacity.ts (DesignerResult per-field-documented interface style to mirror)
    - src/productive/briefed.ts (isBriefed from Task 1)
  </read_first>
  <action>
    Create `src/productive/brief.ts`. Export the `BriefFlag` interface (per the <interfaces> shape) with per-field doc comments mirroring DesignerResult. Export `assessBriefs(input)` that, for each CONFIRMED (draft=false), TARGET-DAY, CLIENT booking (D-05/D-08/D-06), produces 0 or 1 `BriefFlag`: `reason="no-task"` when no task linked; else `reason="not-briefed"` when isBriefed returns false due to position; else `reason="blank-brief"` when at/past Briefed but description empty; else no flag. Determine CLIENT vs internal via the company-absence signal: a booking is internal when its resolved project has NO `company` relationship — accept the project/company resolution as an input map (the gather step in 02-04 resolves projects); brief.ts itself takes `isClientBooking: (bookingId) => boolean` or a pre-resolved flag so it stays pure and testable. `jobLabel` = project/task title; NEVER read or store a PM (BRIEF-03). Doc-header cites BRIEF-01/02/03 and the never-name-a-PM constraint.
    Extend `src/productive/__tests__/briefed.test.ts` (or add brief.test.ts) covering all behaviors above with fixtures.
  </action>
  <verify>
    <automated>node --import tsx --test "src/productive/__tests__/briefed.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert all three reasons ("no-task", "not-briefed", "blank-brief") are produced for the right inputs, and fully-briefed → no flag.
    - A test asserts a tentative booking yields NO BriefFlag (D-05).
    - A test asserts an internal (no-company) booking yields NO BriefFlag but is still present in the capacity bookings (hours unaffected, D-06).
    - brief.ts has no field/variable referencing a PM/manager/owner person (grep: `grep -iE "pm|manager|owner|approver" src/productive/brief.ts` returns nothing meaningful — jobLabel is project/task only).
    - `node --import tsx --test "src/productive/__tests__/briefed.test.ts"` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>BriefFlags are emitted by job/task for the three failure modes on confirmed client target-day bookings only, never naming a PM, with internal work and tentative work correctly suppressed from flags — BRIEF-01/02/03 implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live verification — include-chain depth (A7) + internal/client signal (A2/A3/A4) + Briefed mapping (SC-2)</name>
  <what-built>Three research assumptions need live confirmation before the brief logic is trusted: (A7) whether `include=task,task.workflow_status` resolves in one /bookings call or needs a follow-up /tasks fetch; (A2/A3/A4) that the known internal booking's project genuinely has no client company and the service→project chain resolves; and SC-2: that the dynamically-resolved Briefed mapping matches what PMs see in the live Productive UI.</what-built>
  <read_first>
    - .planning/phases/02-productive-pull-briefed-discovery/02-RESEARCH.md (Open Q2, A7, "D-06 Internal-vs-Client Distinction", Assumptions Log A2-A4)
    - .planning/phases/02-productive-pull-briefed-discovery/02-CONTEXT.md (D-01 live Briefed positions; the "R1 EDM Design" blank-brief case)
    - src/productive/briefed.ts, src/productive/brief.ts
  </read_first>
  <how-to-verify>
    1. INCLUDE DEPTH (A7): run a `tsx` probe calling /bookings with `include=task,task.workflow_status,service` for the target window. Confirm the `included` array contains the task's workflow_status with a `position`. If 2-level nesting is capped, confirm the fallback: a follow-up `/tasks?filter[id]=any_of(...)&include=workflow_status`. Record which path works.
    2. WORKFLOW POSITIONS: fetch `/workflow_statuses`, build the Briefed-position map, and confirm SOLVD Standard → Briefed position 3 and SOLVD Design Retainers → position 2 (matches D-01). Confirm the map resolves positions by NAME dynamically.
    3. INTERNAL/CLIENT (A2/A3/A4): pull the known internal booking ("Liam time for AI – Q2 2026"), resolve its service→project, and confirm the project has NO `company` relationship (company-absence = internal). Note the project's `project_type_id` value for the SUMMARY but do NOT hardcode it.
    4. SC-2 HAND-CHECK: for 2-3 real target-day bookings, compare the tool's briefed verdict against what the PM sees in the Productive UI (is the task at/past the Briefed column? is the description filled?). Spot-check the R1-EDM-style blank-brief case if present.
  </how-to-verify>
  <acceptance_criteria>
    - The working include path (single 2-level include vs follow-up /tasks fetch) is recorded in the SUMMARY; gather (02-04) will use it.
    - The Briefed-position map matches D-01 live (Standard=3, Retainers=2); resolved by name, not hardcoded.
    - The internal booking's project is confirmed to have no client company; the project_type_id value is recorded (not hardcoded).
    - At least 2 real bookings' briefed verdicts agree with the live Productive UI (SC-2); any disagreement is documented and the logic corrected.
    - No secret value printed in any probe output.
  </acceptance_criteria>
  <resume-signal>Type "verified" with the include path used and the internal-project company-absence confirmation, or describe any mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Productive workflow/task data → brief judgment | Untrusted external status/description data drives the briefed verdict. |
| brief output → message (Phase 3) | BriefFlag must never carry a PM identity (tone/trust constraint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering / trust erosion | over-strict Briefed check (status==Briefed) flags active work | mitigate | D-02 at-OR-past position compare; live SC-2 hand-check (Task 3) confirms against the PM UI; unit test covers the past-Briefed case. |
| T-02-10 | Trust erosion | blank-brief false-trust (Briefed-but-empty) | mitigate | D-04 non-empty description guard; unit test for the R1-EDM-style case. |
| T-02-11 | Trust erosion | internal work flagged as not-briefed (noise) | mitigate | company-absence exclusion (D-06); live confirmation the internal project has no company (Task 3); hours still counted. |
| T-02-12 | Information disclosure | surfacing a PM by name (tone violation) | mitigate | BriefFlag has no PM field; jobLabel is project/task only; acceptance grep forbids pm/manager/owner references. |
| T-02-13 | Tampering | hardcoded status IDs breaking on column reorder | mitigate | D-03 dynamic resolution by name; acceptance grep forbids the literal status ids. |
</threat_model>

<verification>
- `npm test` passes (briefed + brief tests green).
- `npx tsc --noEmit` exits 0.
- Live Task 3 confirmed: include path, Briefed positions match D-01, internal project has no company, ≥2 bookings agree with the UI.
- src/domain imports nothing from src/productive.
</verification>

<success_criteria>
The studio's actual Briefed convention is dynamically resolved and confirmed against the live UI; each confirmed client target-day booking correctly reports task-linked / briefed / blank-brief by job/task (never PM); internal and tentative work are correctly excluded from flags. BRIEF-01, BRIEF-02, BRIEF-03 are implemented and live-validated.
</success_criteria>

<output>
Create `.planning/phases/02-productive-pull-briefed-discovery/02-03-SUMMARY.md` when done. Record: the working include path (A7), the live Briefed-position map (vs D-01), and the internal-booking company-absence confirmation + observed project_type_id (A2/A3/A4).
</output>
