---
phase: 03-template-renderer-chat-delivery
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/index.ts
  - src/__tests__/guard.test.ts
  - src/config.ts
  - .github/workflows/nightly.yml
  - .env.example
  - assets/avatar-asterisk.png
  - README.md
autonomous: false
requirements: [SCHED-01, SCHED-02, REL-01, REL-02]
user_setup:
  - service: google-chat
    why: "Deliver the nightly card to the studio Chat space"
    env_vars:
      - name: GCHAT_WEBHOOK_URL
        source: "Google Chat space -> Apps & integrations -> Webhooks -> Create (copy the full URL)"
    dashboard_config:
      - task: "Create an incoming webhook in the target Chat space and copy its URL"
        location: "Google Chat -> target space -> Manage webhooks"
      - task: "Add GCHAT_WEBHOOK_URL as a GitHub Actions repository secret"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions"
must_haves:
  truths:
    - "Running src/index.ts on a weekend exits 0 without posting (weekday-only guard, SCHED-01)"
    - "The composition root wires gather -> computeStudioReport -> renderTemplate -> postToChat, injecting studio-zone now"
    - "A data-source failure (gather sourceErrors) produces a degraded card that still posts (REL-01)"
    - "A post failure (postToChat ok:false) exits the process non-zero so the GitHub built-in failed-run email fires (REL-02, D-25)"
    - "The workflow runs on a weekday 4:30pm Australia/Sydney cron and on manual workflow_dispatch (SCHED-01, SCHED-02)"
    - "The avatar PNG is a hosted raster PNG (white asterisk on black circle, imageType CIRCLE) committed and reachable at the public HTTPS URL config.ts references (D-04, D-07)"
  artifacts:
    - path: "src/index.ts"
      provides: "runNightly composition root + studio-tz weekday guard + two-path reliability"
    - path: ".github/workflows/nightly.yml"
      provides: "weekday 4:30pm Sydney cron + workflow_dispatch + node --import tsx src/index.ts"
      contains: "workflow_dispatch"
    - path: "src/__tests__/guard.test.ts"
      provides: "weekend/weekday guard test with injected now"
    - path: "assets/avatar-asterisk.png"
      provides: "the hosted CIRCLE avatar — white asterisk on black raster PNG at a public HTTPS URL (D-04, D-07)"
  key_links:
    - from: "src/index.ts"
      to: "gather -> computeStudioReport -> renderTemplate -> postToChat"
      via: "composition spine, injected now"
      pattern: "gather|computeStudioReport|renderTemplate|postToChat"
    - from: ".github/workflows/nightly.yml"
      to: "src/index.ts"
      via: "node --import tsx src/index.ts with secrets in env"
      pattern: "src/index.ts"
    - from: "src/index.ts"
      to: "process.env.GCHAT_WEBHOOK_URL"
      via: "secret passed into postToChat at the boundary"
      pattern: "GCHAT_WEBHOOK_URL"
---

<objective>
Wire the full nightly pipeline and ship it: the `src/index.ts` composition root (studio-zone weekday guard -> gather -> report -> render -> post), the two-path reliability split (data-source failure degrades and posts; post failure exits non-zero), the GitHub Actions weekday-4:30pm-Sydney cron + manual dispatch, the committed/hosted avatar PNG, and a real manual smoke post into a test space. This is the moment Phase 3 becomes a running, scheduled product.

Purpose: SCHED-01/02 (scheduled + manual) and the wiring of REL-01 (degraded posts) / REL-02 (post failure alerts) into a single runnable entrypoint. After this plan the studio gets a real nightly card.
Output: `src/index.ts`, `guard.test.ts`, extended `config.ts` (designer names), `.github/workflows/nightly.yml`, `.env.example`, `assets/avatar-asterisk.png`, a README run/setup note, and a verified manual post.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-template-renderer-chat-delivery/03-CONTEXT.md
@.planning/phases/03-template-renderer-chat-delivery/03-RESEARCH.md
@.planning/phases/03-template-renderer-chat-delivery/03-PATTERNS.md
@.planning/phases/03-template-renderer-chat-delivery/03-01-SUMMARY.md
@.planning/phases/03-template-renderer-chat-delivery/03-02-SUMMARY.md
@.planning/phases/03-template-renderer-chat-delivery/03-03-SUMMARY.md

<interfaces>
gather (src/productive/gather.ts):
  GatherResult { bookings: DatedBooking[]; absences: DatedAbsence[]; briefFlags: BriefFlag[]; holidays: HolidaySet; assessedDesigners: DesignerId[]; sourceErrors: string[] }
  GatherDeps { now: DateTime; fetchPages?: (...) }; export async function gather(deps): Promise of GatherResult
computeStudioReport(input: StudioReportInput): StudioReport  (src/domain/report.ts)
renderTemplate(report, ctx): CardsV2Payload  (src/render/renderMessage.ts)
postToChat(payload, webhookUrl): Promise of Result of void  (src/chat/postToChat.ts)
STUDIO_ZONE, DESIGNER_PERSON_IDS, STUDIO_CLOSURES already exist (types.ts / config.ts).
The ONLY place allowed to call DateTime.now() is index.ts (the boundary); everything below takes now injected.
nightly.yml (RESEARCH Pattern 6 verbatim base): schedule.cron "30 16 * * 1-5" + timezone "Australia/Sydney"; workflow_dispatch present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composition root src/index.ts + studio-tz weekday guard + guard test</name>
  <read_first>
    - src/index.ts, src/__tests__/guard.test.ts (being created)
    - src/productive/gather.ts lines 60-93 (GatherResult + GatherDeps), 320-350 (gather composition + injected now + sourceErrors accumulation)
    - src/domain/report.ts lines 60-120 (StudioReportInput + StudioReport), 164-223 (computeStudioReport derives targetDay from injected now)
    - src/domain/clock.ts (nextWorkingDay / working-day logic, for holiday/closure context), src/holidays.ts lines 8-18 (studio-zone toISODate keys)
    - src/domain/types.ts lines 27-41 (STUDIO_ZONE, DesignerId), src/config.ts (DESIGNER_PERSON_IDS, STUDIO_CLOSURES, AVATAR_PNG_URL, deep-link template)
    - src/render/renderMessage.ts + cards.ts (renderTemplate + RenderContext shape, incl. leaveNotes), src/chat/postToChat.ts (postToChat signature)
    - 03-RESEARCH.md Pattern 6 (lines 261-293 in-code guard), Pitfall 1 (lines 316-320 TWO reliability paths, DO NOT MERGE), Architecture diagram (lines 72-108)
    - 03-PATTERNS.md "src/index.ts" section (lines 150-171 injected now, composition spine, two-path rule, holiday/closure wiring)
    - src/domain/__tests__/clock.test.ts (injected-now test pattern to mirror)
  </read_first>
  <action>
    Create `src/index.ts` as the `runNightly` composition root — the ONE place that calls `DateTime.now().setZone(STUDIO_ZONE)` (every module below takes `now` injected). Order:
    1. Weekday guard (SCHED-01, defence-in-depth even though the cron is weekday-only): extract a tiny pure predicate `shouldSkipForWeekend(now): boolean` (`now.weekday >= 6`); if true, log "weekend skipping" and `process.exit(0)`. Gate only on weekday, not on the exact minute, so a delayed scheduled run and a `workflow_dispatch` manual run both still fire on a weekday (RESEARCH note on scheduled-run delays).
    2. `const g = await gather({ now });`
    3. Build `StudioReportInput` from `g` (roster = DESIGNER_PERSON_IDS as DesignerId[], holidays = g.holidays, bookings/absences = g.bookings/g.absences, assessedDesigners = g.assessedDesigners) and call `computeStudioReport`.
    4. Derive `RenderContext`: `sourceErrors = g.sourceErrors`; `briefFlags = g.briefFlags`; distinguish `holidayTomorrow` (D-20 public holiday: report.targetDay in the holiday set but NOT in STUDIO_CLOSURES) vs `closureTomorrow` (D-21: report.targetDay in STUDIO_CLOSURES); `designerNames` from a new `DESIGNER_NAMES` const added to config.ts keyed by person id (Liam Mills 686717, Anisha Gittins 686712, Ella Wright 686716, from 03-CONTEXT specifics line 120); `header.subtitle` = "Tomorrow Weekday Date" formatted from report.targetDay via luxon in STUDIO_ZONE (use the middot separator per D-06); `header.targetDate` = report.targetDay; `leaveNotes` omitted unless half-day absence detail is available.
    5. `renderTemplate(report, ctx)` -> payload.
    6. `const posted = await postToChat(payload, process.env.GCHAT_WEBHOOK_URL ?? "")`.
    THE TWO-PATH RULE (RESEARCH Pitfall 1 — do NOT merge): data-source failures are ALREADY inside `g.sourceErrors` -> the renderer emits the degraded card -> it STILL posts (REL-01). A POST failure (`posted.ok === false`, including a missing `GCHAT_WEBHOOK_URL`) must `console.error` the status (NEVER the URL) and `process.exit(1)` so the GitHub built-in failed-run email fires (REL-02). Do NOT wrap the post in a swallow-and-exit-0 catch.
    Add `DESIGNER_NAMES` to `src/config.ts` in the existing `as const` style (do not rewrite existing exports).
    Create `src/__tests__/guard.test.ts` mirroring clock.test.ts: inject Saturday and Sunday `now` -> `shouldSkipForWeekend` true; a Wednesday -> false. Keep the predicate pure so the test never invokes the network or `process.exit`.
  </action>
  <verify>
    <automated>node --import tsx --test "src/__tests__/guard.test.ts" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - guard.test.ts passes: Saturday and Sunday injected now -> skip true; a Wednesday -> skip false
    - index.ts calls DateTime.now() exactly once: `grep -c "DateTime.now()" src/index.ts` equals 1
    - composition spine present: `grep -nE "gather|computeStudioReport|renderTemplate|postToChat" src/index.ts` shows all four
    - two-path reliability: `grep -q "process.exit(1)" src/index.ts` (post-failure path) AND no top-level catch wrapping the post exits 0
    - webhook URL never logged: in src/index.ts GCHAT_WEBHOOK_URL is passed to postToChat and never inside a console.* call (manual grep confirm)
    - config.ts gained DESIGNER_NAMES and kept existing exports
    - `npx tsc --noEmit` clean; `npm test` green
  </acceptance_criteria>
  <done>src/index.ts wires the full pipeline with injected studio-zone now, degrades-and-posts on source failure, exits non-zero on post failure, and skips weekends; the guard is unit-tested green.</done>
</task>

<task type="auto">
  <name>Task 2: Avatar PNG + GitHub Actions workflow + .env.example + README setup note</name>
  <read_first>
    - .github/workflows/nightly.yml, .env.example, README.md, assets/avatar-asterisk.png (being created)
    - /Users/liammills/Documents/CLAUDE/Solvd Brand/assets/icons/icon-asterisk.svg (the source asterisk to export white-on-black to PNG)
    - /Users/liammills/Documents/CLAUDE/Solvd Brand/visual-rules.md (white-on-black avatar rule, brand black #0A0A0A)
    - src/config.ts (AVATAR_PNG_URL — the hosting must make this URL resolve)
    - package.json (repository/homepage already point at github.com/solvdagency/evening-studio-checkin)
    - 03-RESEARCH.md Pattern 6 (lines 261-293 the workflow), Pitfall 4 (lines 334-338 avatar must be anonymously fetchable), Runtime State Inventory (lines 340-351 new secret + hosted asset), Open Question 1 (lines 416-419 repo public/private)
    - 03-PATTERNS.md ".github/workflows/nightly.yml" (lines 202-204) + config EXTEND (lines 208-212)
  </read_first>
  <action>
    Export the brand asterisk (white asterisk on a black `#0A0A0A` circle, the CIRCLE avatar) to a raster PNG (~256x256) and commit it to `assets/avatar-asterisk.png`. Confirm `config.ts` `AVATAR_PNG_URL` matches the public raw URL serving this file (github.com/solvdagency/evening-studio-checkin raw on the default branch). Confirm the repo is PUBLIC so Google's server-side image fetch is anonymous (Open Question 1 / Pitfall 4); if it is private, host the PNG on a public bucket / public assets repo and point AVATAR_PNG_URL there instead, then note that in the README.
    Create `.github/workflows/nightly.yml` from 03-RESEARCH Pattern 6 verbatim: `schedule.cron: "30 16 * * 1-5"` + `timezone: "Australia/Sydney"` (native DST-aware field), `workflow_dispatch`, job on ubuntu-latest: checkout@v4, setup-node@v4 node 22, `npm ci`, `node --import tsx src/index.ts` with env `GCHAT_WEBHOOK_URL`, `PRODUCTIVE_AUTH_TOKEN`, `PRODUCTIVE_ORG_ID` from secrets.
    Create/extend `.env.example` mirroring the secret names (GCHAT_WEBHOOK_URL, PRODUCTIVE_AUTH_TOKEN, PRODUCTIVE_ORG_ID) with placeholder values and a comment that real secrets live in GitHub Actions secrets / a gitignored .env. Add a short README section: how to set the GitHub secret, how to trigger the manual run (Actions tab -> workflow_dispatch), the 60-day-inactivity scheduled-disable caveat, and the D-25 total-failure alert (GitHub's built-in failed-run email to repo owners — no extra infra).
  </action>
  <verify>
    <automated>test -f assets/avatar-asterisk.png && file assets/avatar-asterisk.png | grep -q PNG && grep -q "workflow_dispatch" .github/workflows/nightly.yml && grep -q "Australia/Sydney" .github/workflows/nightly.yml && grep -q "src/index.ts" .github/workflows/nightly.yml</automated>
  </verify>
  <acceptance_criteria>
    - assets/avatar-asterisk.png exists and `file` reports a PNG
    - config.ts AVATAR_PNG_URL is a public HTTPS raw URL pointing at the committed PNG (verified reachable in an incognito browser per Pitfall 4)
    - nightly.yml has schedule.cron "30 16 * * 1-5", timezone "Australia/Sydney", workflow_dispatch, node 22, and runs `node --import tsx src/index.ts`
    - .env.example lists GCHAT_WEBHOOK_URL, PRODUCTIVE_AUTH_TOKEN, PRODUCTIVE_ORG_ID with placeholder (non-real) values
    - README documents secret setup, manual workflow_dispatch trigger, the 60-day inactivity caveat, and the D-25 failed-run email alert
    - no real secret value is committed (grep the diff for token patterns returns nothing)
  </acceptance_criteria>
  <done>The avatar PNG is committed and reachable, the weekday-4:30pm-Sydney + manual-dispatch workflow runs the entrypoint with secrets from env, and the README explains setup and the failure-alert channel.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full nightly pipeline: src/index.ts wires gather -> report -> renderTemplate -> postToChat with the weekday guard and two-path reliability; the Cards v2 renderer with all variants; the Google Chat webhook transport; and the GitHub Actions weekday-4:30pm-Sydney + manual-dispatch workflow with the committed avatar PNG. Everything Claude can automate is done and the full test suite is green.</what-built>
  <how-to-verify>
    Prerequisite (one-time, user action — Claude cannot create a webhook): in the TARGET test Google Chat space, create an incoming webhook (Manage webhooks -> Add), copy the URL, and either (a) export it locally as GCHAT_WEBHOOK_URL in a gitignored .env, or (b) add it as the GCHAT_WEBHOOK_URL GitHub Actions repository secret.

    Then run ONE real manual smoke post and confirm it visually against design/chat-card-mockups.html:
    1. Local: with .env set, run `node --import tsx src/index.ts` on a weekday (or temporarily bypass the weekday guard for the test). Confirm a card posts to the test space. OR CI: push the branch, open the Actions tab, run the "Evening Studio Check-in" workflow via "Run workflow" (workflow_dispatch), confirm a card posts.
    2. In the posted card confirm: (a) the CIRCLE avatar renders (white asterisk on black) — not a broken image; (b) the header reads "Solvd Studio Check-in" + "Tomorrow Weekday Date"; (c) the verdict line names NO person; (d) per-designer rows show the emoji + bold name + coloured status, with tentative "(on top)" and brief lines at body size (NOT tiny caption); (e) the "Open in Productive" button opens tomorrow's scheduling view filtered to the design team; (f) the week bar dot-gauge + "{X}h booked {Y}h open" caption reads correctly; (g) the numbers match what you see in Productive for that day.
    3. Confirm the dot-bar glyphs align acceptably in the real client (A1); if uneven, note it so the renderer can wrap the bar in `<code>` (planner Open Item 4).
    4. (Optional) Force a degraded run (e.g. an invalid PRODUCTIVE_AUTH_TOKEN) and confirm the degraded "Couldn't reach Productive tonight." card still posts (REL-01), and that a broken webhook makes the run fail loudly (non-zero exit / GitHub failure email) rather than silently (REL-02).
  </how-to-verify>
  <resume-signal>Type "approved" if the card matches the mockup and the numbers are correct, or describe what's off (avatar, spacing, colour, copy, numbers, alignment).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI env -> process | Secrets (GCHAT_WEBHOOK_URL, PRODUCTIVE_*) injected as env vars by GitHub Actions |
| process -> Google Chat | The single outbound POST (delegated to postToChat) |
| Google image renderer -> avatar host | Google fetches the avatar PNG server-side, anonymously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Information Disclosure | webhook URL / Productive token leaking via index.ts logs or committed files | mitigate | Secrets read from env only; index.ts logs res.status never the URL; .env.example holds placeholders; .env is gitignored (existing). |
| T-03-10 | Denial of Service / Availability | silent missed night (dead webhook, stale schedule) | mitigate | Post failure -> process.exit(1) -> GitHub failed-run email (REL-02, D-25). README notes the 60-day inactivity disable. |
| T-03-11 | Spoofing / Tampering | avatar PNG host swapped or unreachable | accept | URL is a hardcoded committed constant; if unreachable the card still posts (degraded brand fidelity, not a failure) per RESEARCH Environment Availability. |
| T-03-12 | Tampering | committing a real secret value into .env.example / workflow | mitigate | .env.example uses placeholder values; secrets referenced via the secrets context only; diff scanned for token patterns. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (03-RESEARCH Package Legitimacy Audit = N/A). `npm ci` in CI installs only the committed, already-audited lockfile. No install task adds a dependency. |
</threat_model>

<verification>
- `npm test` green (all Phase 1/2/3 tests, incl. the new guard test).
- `npx tsc --noEmit` clean.
- One real manual workflow_dispatch (or local) post lands in the test space and matches the mockup (checkpoint).
- Degraded + post-failure reliability paths spot-checked (checkpoint optional step 4).
</verification>

<success_criteria>
- The check runs unattended on a weekday 4:30pm Sydney schedule and never on weekends, and can be triggered manually — SCHED-01, SCHED-02.
- A source failure posts a degraded card naming what it couldn't reach — REL-01.
- A post failure exits non-zero and fires the GitHub failure email — REL-02.
- A real card posts to the test space matching the locked mockup (avatar, verdict, rows, button, week bar).
</success_criteria>

<output>
Create `.planning/phases/03-template-renderer-chat-delivery/03-04-SUMMARY.md` when done.
</output>
