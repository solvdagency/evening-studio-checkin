---
phase: 03-template-renderer-chat-delivery
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/render/variants.ts
  - src/render/verdict.ts
  - src/render/rows.ts
  - src/render/renderMessage.ts
  - src/render/__tests__/renderMessage.test.ts
  - src/render/__tests__/fixtures/degraded.json
  - src/render/__tests__/fixtures/couldnt-read-one.json
  - src/render/__tests__/fixtures/holiday.json
  - src/render/__tests__/fixtures/closure.json
  - src/render/__tests__/fixtures/on-leave.json
  - src/render/__tests__/fixtures/half-day-leave.json
autonomous: true
requirements: [MSG-04, MSG-05, MSG-07, REL-01]
must_haves:
  truths:
    - "When a source is unreachable the card names what it couldn't reach (Productive) and still renders a postable payload"
    - "When one or two designers are unreadable, that person's row shows 🤖 (never a fake empty), and the verdict stays nameless"
    - "A public-holiday tomorrow renders its own short warm message with no rows or week bar"
    - "A studio-closure tomorrow renders its own message with no rows or week bar"
    - "A fully-on-leave designer renders a minimal '⚪ on leave / Full day off' row; a half-day leave renders a normal availability row with a leave note"
    - "Message length scales with severity — clean nights collapse rows, busy/degraded nights expand"
  artifacts:
    - path: "src/render/__tests__/fixtures/degraded.json"
      provides: "locked degraded-variant card (Productive unreachable)"
    - path: "src/render/__tests__/fixtures/holiday.json"
      provides: "locked holiday-variant card"
    - path: "src/render/__tests__/fixtures/closure.json"
      provides: "locked studio-closure-variant card"
  key_links:
    - from: "src/render/variants.ts"
      to: "RenderContext (sourceErrors / holidayTomorrow / closureTomorrow)"
      via: "ordered cascade holiday → closure → degraded → card"
      pattern: "holidayTomorrow|closureTomorrow|sourceErrors"
    - from: "src/render/rows.ts"
      to: "report.missingDesigners"
      via: "🤖 couldn't-read row for missing designers"
      pattern: "missingDesigners"
---

<objective>
Complete the renderer's variant coverage: the always-posts states (degraded source-unreachable, per-designer data miss, public holiday, studio closure) and the leave-row treatments (full-day and half-day). Each is pinned to a committed JSON fixture so the locked copy and structure can't drift. This finishes "the check always posts, in every state" — the reliability spine of the message layer.

Purpose: REL-01 (degraded message names what it couldn't reach, still posts) and MSG-04/05 (always posts, length scales with severity) are satisfied here. Builds directly on the render contract + card path from plan 01.
Output: extended `variants.ts`/`verdict.ts`/`rows.ts`/`renderMessage.ts` covering all variants, plus six new fixtures and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-template-renderer-chat-delivery/03-CONTEXT.md
@.planning/phases/03-template-renderer-chat-delivery/03-RESEARCH.md
@.planning/phases/03-template-renderer-chat-delivery/03-UI-SPEC.md
@.planning/phases/03-template-renderer-chat-delivery/03-01-SUMMARY.md
@design/chat-card-mockups.html

<interfaces>
<!-- From plan 01 (src/render/cards.ts): CardsV2Payload, RenderContext, RenderMessage, renderTemplate. -->
<!-- RenderContext variant fields (03-RESEARCH Pattern 1 lines 149-162): -->
<!--   sourceErrors: string[]   (non-empty ⇒ degraded, D-18; verbatim labels e.g. "Productive") -->
<!--   holidayTomorrow?: { dateLabel: string }   (D-20) -->
<!--   closureTomorrow?: { backDayLabel: string } (D-21) -->
<!--   designerNames: Record<string,string>; header: { subtitle; targetDate } -->
<!-- report.missingDesigners: DesignerId[]  → 🤖 per-designer-miss rows (D-19), NOT a top-level variant -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Degraded + per-designer-miss variants (REL-01, D-18, D-19)</name>
  <read_first>
    - src/render/variants.ts, renderMessage.ts, verdict.ts, rows.ts (plan 01 output — extend, don't rewrite)
    - src/render/cards.ts (the types), 03-01-SUMMARY.md (what plan 01 stubbed/left for here)
    - 03-RESEARCH.md Code Examples (variant selection lines 354-365 — the ordered cascade and the note that missingDesigners is NOT a top-level variant), Pitfall 1 (lines 316-320 — degraded posts, don't conflate with post-failure)
    - 03-UI-SPEC.md State/error variants table (lines 186-197): degraded + per-designer-miss locked copy; verdict table line 161 ("I could only read one designer tonight.")
    - 03-CONTEXT.md D-18 (degraded), D-19 (per-designer miss)
    - design/chat-card-mockups.html (degraded, couldnt-read-one, couldnt-read-two, half-data-missing scenarios)
  </read_first>
  <behavior>
    - renderTemplate(report, ctx with sourceErrors=["Productive"]) deepStrictEquals fixtures/degraded.json: verdict line "🤖 Couldn't reach Productive tonight." + body "No booking figures this run. I'll have them tomorrow evening — worth a check in Productive yourself in the meantime." NO per-designer rows, NO week bar (figures are untrusted this run). Still a complete postable CardsV2Payload.
    - renderTemplate(report with missingDesigners=[one], ctx) deepStrictEquals fixtures/couldnt-read-one.json: a 🤖 row for that designer ("couldn't read"), real rows for the others, verdict nameless "I could only read one designer tonight."
    - The degraded variant names the SOURCE verbatim from ctx.sourceErrors (so a future Calendar source would read "Couldn't reach Calendar"); confirm the label substitution is data-driven, not hardcoded "Productive" only.
  </behavior>
  <action>
    Replace the plan-01 stub branches in `variants.ts` so the ordered cascade returns `"degraded"` when `ctx.sourceErrors.length > 0` (after holiday/closure, which Task 2 fills). In `renderMessage.ts` add the `"degraded"` assembly path: header + a verdict/body section built from the locked D-18 copy with the source name interpolated from `ctx.sourceErrors` (escape it), and NO rows/week-bar section (the figures aren't trusted this run — REL-01). In `rows.ts` add a `missingDesigner` row builder: for any `designerId` in `report.missingDesigners`, emit a `decoratedText` with the 🤖 marker and "couldn't read" copy instead of a figures row (D-19) — these render INSIDE the normal card path, not as a top-level variant. In `verdict.ts` add the nameless "I could only read one/two designer(s) tonight." branch keyed off `report.missingDesigners.length` (still never names a person). Write the two fixtures FIRST + tests, confirm RED, then implement to GREEN.
  </action>
  <verify>
    <automated>cd "$REPO" && node --import tsx --test "src/render/__tests__/renderMessage.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - degraded + couldnt-read-one fixtures pass deepStrictEqual
    - `grep -q "Couldn't reach Productive tonight" src/render/__tests__/fixtures/degraded.json`
    - degraded fixture has no decoratedText figures rows and no week-bar section: `grep -c "Remaining studio time this week" src/render/__tests__/fixtures/degraded.json` equals 0
    - couldnt-read-one fixture contains the 🤖 marker and verdict has no designer name
    - degraded source label is data-driven: variants/renderMessage source contains no hardcoded `"Productive"` string literal in the degraded body (the source name comes from ctx.sourceErrors) — `grep -n '"Productive"' src/render/*.ts` returns nothing
    - `npm test` green
  </acceptance_criteria>
  <done>Degraded and per-designer-miss variants render postable cards with the locked copy, the source named from data, and a nameless verdict; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Holiday, closure, and leave-row variants (D-20, D-21, D-22)</name>
  <read_first>
    - src/render/variants.ts, renderMessage.ts, rows.ts (Task 1 output)
    - 03-RESEARCH.md Open Question 3 (lines 426-429 — half-day leave needs absence detail via RenderContext, NOT a domain change), Code Examples variant cascade (354-365)
    - 03-UI-SPEC.md State/error variants table (holiday line 192, closure line 193, on-leave line 194, half-day line 195), Open Items 2 (line 229 — half-day wording source)
    - 03-CONTEXT.md D-20 (holiday), D-21 (closure), D-22 (leave / half-day leave)
    - src/domain/capacity.ts lines 108-122 (status "off" = fully on leave; partial leave shows as a reduced-availability underbooked/ok row)
    - design/chat-card-mockups.html (holiday, studio-closure, on-leave, two-on-leave, half-day-leave scenarios)
  </read_first>
  <behavior>
    - renderTemplate(report, ctx with holidayTomorrow={dateLabel}) deepStrictEquals fixtures/holiday.json: only the warm "🎉 Public holiday tomorrow — {dateLabel}. No check-in needed. Enjoy…" message; NO rows, NO week bar, NO button.
    - renderTemplate(report, ctx with closureTomorrow={backDayLabel}) deepStrictEquals fixtures/closure.json: "📦 Studio's out tomorrow — team offsite. No check-in needed. Back {backDayLabel}." only.
    - holiday is selected even if sourceErrors is also non-empty (cascade order holiday > closure > degraded).
    - renderTemplate(report with a status="off" designer) deepStrictEquals fixtures/on-leave.json: that designer's row is exactly "⚪ {Name} — on leave / Full day off." and NOTHING more (no booked/open detail, no flags).
    - renderTemplate(half-day-leave report + ctx leave note) deepStrictEquals fixtures/half-day-leave.json: a normal availability row for the hours present, plus the note "On leave until midday · {X}h booked" sourced from RenderContext (decide the exact carrier: extend RenderContext with an optional per-designer leave note map; do NOT touch src/domain). If the exact "until midday" time isn't available, the simpler "Part-day leave · {X}h available" fallback is acceptable per Open Item 2 — implement whichever the half-day fixture pins.
  </behavior>
  <action>
    Fill the remaining `variants.ts` cascade branches: `if (ctx.holidayTomorrow) return "holiday"; if (ctx.closureTomorrow) return "closure";` BEFORE the degraded check (order is significant, D-20/D-21 beat degraded). In `renderMessage.ts` add the `"holiday"` and `"closure"` assembly paths — each is header + a single warm `textParagraph` section with the locked copy (dateLabel / backDayLabel interpolated + escaped), no rows/button/week-bar. In `rows.ts` add the leave-row branch: `status === "off"` → the minimal "⚪ {Name} — on leave / Full day off." row (D-22); a half-day leave (partial availability, signalled by an optional per-designer leave note added to `RenderContext` in `cards.ts`) → a normal availability row plus the leave note line. Extend `RenderContext` in `src/render/cards.ts` with the optional `leaveNotes?: Record<string, string>` carrier (documented as the D-22 half-day source per Open Item 2; keeps the domain untouched). Write the four fixtures FIRST + tests, confirm RED, implement to GREEN.
  </action>
  <verify>
    <automated>cd "$REPO" && node --import tsx --test "src/render/__tests__/renderMessage.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - holiday, closure, on-leave, half-day-leave fixtures pass deepStrictEqual
    - `grep -q "Public holiday tomorrow" src/render/__tests__/fixtures/holiday.json` and `grep -q "Studio's out tomorrow" src/render/__tests__/fixtures/closure.json`
    - holiday + closure fixtures have no week-bar section and no buttonList: `grep -c "buttonList" src/render/__tests__/fixtures/holiday.json` equals 0
    - on-leave fixture row is minimal: contains "Full day off" and no booked/open figure or flag line for that designer
    - cascade order proven by a test: holidayTomorrow set alongside sourceErrors still renders the holiday variant
    - RenderContext gained `leaveNotes` (optional) and src/domain was NOT modified: `git diff --name-only` shows no `src/domain/` files
    - `npm test` green
  </acceptance_criteria>
  <done>Holiday, closure, full-leave, and half-day-leave variants all render correctly with locked copy and correct cascade precedence; the domain layer is untouched; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RenderContext (sourceErrors, dateLabel, backDayLabel, leaveNotes) → card text | Strings interpolated into HTML card text; upstream-sourced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | sourceErrors / dateLabel / backDayLabel / leaveNotes interpolated into card text | mitigate | HTML-escape all dynamic strings before insertion (reuse the escape helper from plan 01). |
| T-03-05 | Availability | degraded variant must still produce a postable card (REL-01) | mitigate | Degraded path returns a complete CardsV2Payload (no rows), never throws; covered by the degraded fixture test. Do NOT swallow — post-failure handling is plan 04. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan. No install task present. |
</threat_model>

<verification>
- `npm test` green (all Phase 1/2/3 tests).
- `npx tsc --noEmit` clean.
- Each new fixture spot-checked against its scenario in design/chat-card-mockups.html.
</verification>

<success_criteria>
- The check always posts in every state: degraded, per-designer-miss, holiday, closure, leave — REL-01, MSG-04.
- Degraded names the unreachable source (data-driven) and still posts — REL-01.
- Message length scales with severity (clean/holiday/closure collapse; busy/degraded expand) — MSG-05.
- Tentative + per-designer-miss visually distinguished — MSG-07.
</success_criteria>

<output>
Create `.planning/phases/03-template-renderer-chat-delivery/03-02-SUMMARY.md` when done.
</output>
