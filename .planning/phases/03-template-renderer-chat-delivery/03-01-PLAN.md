---
phase: 03-template-renderer-chat-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/render/cards.ts
  - src/render/verdict.ts
  - src/render/rows.ts
  - src/render/weekBar.ts
  - src/render/variants.ts
  - src/render/renderMessage.ts
  - src/render/__tests__/renderMessage.test.ts
  - src/render/__tests__/fixtures/two-open.json
  - src/render/__tests__/fixtures/clean.json
  - src/render/__tests__/fixtures/overbooked.json
  - src/config.ts
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03, MSG-06, MSG-07]
must_haves:
  truths:
    - "A busy StudioReport renders to a Cards v2 payload with header avatar, verdict, per-designer rows, Open-in-Productive button, and week bar in that order"
    - "The verdict line never contains a designer name; names appear only in rows"
    - "A clean StudioReport renders header + verdict + status line + button + week bar with NO per-designer rows"
    - "Every hour figure in the card is read from a report *Hours display field; the renderer performs no hour arithmetic"
    - "The deep-link button URL carries report.targetDay as the date param and the design-team filter"
  artifacts:
    - path: "src/render/cards.ts"
      provides: "CardsV2Payload + GoogleCard widget types + RenderContext + RenderMessage interface (the LLM-01 contract)"
      contains: "export interface CardsV2Payload"
    - path: "src/render/renderMessage.ts"
      provides: "renderTemplate: RenderMessage — pure report+ctx → CardsV2Payload"
      exports: ["renderTemplate"]
    - path: "src/render/verdict.ts"
      provides: "nameless scenario-adaptive verdict line (D-12/D-13)"
    - path: "src/render/rows.ts"
      provides: "per-designer decoratedText rows with nested tentative/brief lines (D-09/D-14/D-16)"
    - path: "src/render/weekBar.ts"
      provides: "dot-bar footer formatter (D-23)"
    - path: "src/render/__tests__/fixtures/two-open.json"
      provides: "locked expected card JSON for the busy two-open scenario"
  key_links:
    - from: "src/render/renderMessage.ts"
      to: "src/domain/report.ts (StudioReport)"
      via: "type import of report output, reads *Hours fields"
      pattern: "openHours|bookedHours|availableHours|totalHours"
    - from: "src/render/rows.ts"
      to: "src/productive/brief.ts (BriefFlag)"
      via: "maps BriefFlag.reason to locked brief labels"
      pattern: "no-task|not-briefed|blank-brief"
---

<objective>
Build the pure rendering core: the Cards v2 type contract (the LLM-01 swappable interface), the nameless verdict line, per-designer rows with nested tentative/brief lines, and the week-bar footer — composed into `renderTemplate(report, ctx)` that produces the on-brand busy-night and clean-night cards. This is the thinnest end-to-end *render* slice: a real `StudioReport` in, a verified Cards v2 payload out, pinned to the locked mockup via committed JSON fixtures.

Purpose: This is the heart of the shippable v1. Phase 5's LLM renderer drops in behind the same `RenderMessage` interface; the templated renderer is the always-available default.
Output: `src/render/cards.ts`, `verdict.ts`, `rows.ts`, `weekBar.ts`, `variants.ts`, `renderMessage.ts`, three expected-JSON fixtures + their test, and an extended `src/config.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-template-renderer-chat-delivery/03-CONTEXT.md
@.planning/phases/03-template-renderer-chat-delivery/03-RESEARCH.md
@.planning/phases/03-template-renderer-chat-delivery/03-PATTERNS.md
@.planning/phases/03-template-renderer-chat-delivery/03-UI-SPEC.md
@design/chat-card-mockups.html

<interfaces>
<!-- The renderer consumes these existing OUTPUT types. Do NOT import src/productive raw schemas/client. -->

From src/domain/report.ts:
```typescript
export interface StudioReport {
  targetDay: string;            // "yyyy-MM-dd" studio zone
  window: string[];             // working days, target first
  designers: DesignerResult[];  // roster order, target day
  rollup: StudioRollup;         // { totalMin, openMin, totalHours, openHours }
  missingDesigners: DesignerId[];
}
export interface StudioRollup { totalMin: number; openMin: number; totalHours: number; openHours: number; }
```

From src/domain/capacity.ts:
```typescript
export type DayStatus = "off" | "underbooked" | "overbooked" | "ok";
export interface DesignerResult {
  designerId: DesignerId;
  availableMin: number; confirmedMin: number; tentativeMin: number; openMin: number;
  status: DayStatus; shaky: boolean;
  availableHours: number; bookedHours: number; openHours: number; // DISPLAY-ONLY, read these
}
```

From src/productive/brief.ts:
```typescript
export interface BriefFlag {
  designerId: DesignerId; bookingId: string; taskId: string | null;
  jobLabel: string; reason: "no-task" | "not-briefed" | "blank-brief"; isTentative: boolean;
}
```

From src/domain/types.ts: `export type DesignerId = string & { readonly __brand: "DesignerId" };`
RenderContext shape is fully specified in 03-RESEARCH.md Pattern 1 (lines 149-166). Implement it verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render contract — cards.ts types, RenderContext/RenderMessage interface, config.ts extension</name>
  <read_first>
    - src/render/cards.ts (being created)
    - src/config.ts (being extended — match its `as const` + docblock style, lines 1-46)
    - src/domain/capacity.ts lines 41-60 (the per-field-documented interface convention to copy)
    - src/productive/brief.ts lines 46-59 (BriefFlag — same convention)
    - 03-RESEARCH.md Pattern 1 (lines 133-169, the RenderContext + RenderMessage definition), Pattern 4 (lines 203-235, the full card skeleton these types must shape)
    - 03-UI-SPEC.md Color section (lines 106-118, the five inline `<font>` hex values), Copywriting Contract (CTA D-24, lines 199-206)
  </read_first>
  <action>
    Create `src/render/cards.ts` as a type-only module mirroring the per-field-documented exported-interface style of capacity.ts `DesignerResult`. Define `CardsV2Payload` (`{ cardsV2: Array<{ cardId: string; card: GoogleCard }> }`) and the widget types it nests — `GoogleCard` (`{ header, sections }`), `CardHeader` (`title`, `subtitle`, `imageUrl`, `imageType: "CIRCLE"`), `Section` (`{ header?: string; widgets: Widget[] }`), and the `Widget` union covering `textParagraph` (`{ text }`), `decoratedText` (`{ text; wrapText?: boolean; startIcon? }`), `divider` (`{}`), `buttonList` (`{ buttons: Array<{ text; onClick: { openLink: { url } } }> }`). Shape them to exactly match 03-RESEARCH.md Pattern 4. Define `RenderContext` and the `export type RenderMessage = (report: StudioReport, ctx: RenderContext) => CardsV2Payload` verbatim from Pattern 1 (each field carries a `/** ... */` doc citing its D-xx). Open the module with a trust docblock stating: pure, no I/O, reads only `*Hours` display fields, never recomputes a number (per 03-RESEARCH anti-pattern line 297).
    Extend `src/config.ts` (do not rewrite existing exports) adding, in the same committed-non-secret `as const` style: `AVATAR_PNG_URL` (the hosted white-asterisk-on-black-circle PNG — leave a documented placeholder public raw URL under github.com/solvdagency/evening-studio-checkin/.../assets/avatar-asterisk.png; the actual PNG host is wired in plan 04), `PRODUCTIVE_DEEPLINK_TEMPLATE` (D-24: `https://app.productive.io/34092-solvd-agency/scheduling/bookings?date={DATE}&filter=NzQ5NTY2&groupBy=people`, where `NzQ5NTY2` is base64 of 749566 — verified), and a `BRAND_COLORS` object with the five locked inline hexes (`open:"#d93025"`, `full:"#188038"`, `over:"#b06000"`, `muted:"#5f6368"`, `openDots:"#c9ccd1"`) per D-11/D-23. Keep all secrets out (webhook URL stays in env, per existing config docblock).
  </action>
  <verify>
    <automated>cd "$REPO" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export interface CardsV2Payload" src/render/cards.ts` and `grep -q "export type RenderMessage" src/render/cards.ts`
    - `grep -q "imageType" src/render/cards.ts` (CIRCLE avatar header typed)
    - cards.ts contains NO import of `src/productive/schemas`, `src/productive/client`, or any raw JSON:API type (grep returns nothing): `grep -E "productive/(schemas|client)" src/render/cards.ts` is empty
    - config.ts retains existing exports (`grep -c "export const" src/config.ts` ≥ 4) AND adds `AVATAR_PNG_URL`, `PRODUCTIVE_DEEPLINK_TEMPLATE`, `BRAND_COLORS`
    - `PRODUCTIVE_DEEPLINK_TEMPLATE` contains the literal `filter=NzQ5NTY2` and a `{DATE}` placeholder
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>cards.ts exports the full Cards v2 type contract + RenderMessage interface; config.ts carries the avatar URL, deep-link template, and brand colour tokens; the project typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure render primitives + renderTemplate (busy + clean) pinned to committed fixtures</name>
  <read_first>
    - src/render/renderMessage.ts, verdict.ts, rows.ts, weekBar.ts, variants.ts (being created)
    - src/render/cards.ts (Task 1 output — the types these implement against)
    - src/domain/report.ts lines 164-223 (computeStudioReport — the destructure+assemble+return skeleton to copy)
    - src/domain/capacity.ts lines 108-122 (classifyDay — the ordered branch cascade variants.ts mirrors), lines 41-60 (DesignerResult fields rows.ts reads)
    - src/domain/round.ts lines 1-37 (display-only formatter posture — weekBar.ts mirrors)
    - src/domain/__tests__/report.test.ts lines 1-59 (test structure), src/productive/__tests__/gather.test.ts lines 35-43 (fixture-load via new URL(import.meta.url))
    - src/productive/brief.ts lines 46-59 (BriefFlag.reason → label mapping)
    - 03-RESEARCH.md Pattern 2 (lines 171-185 row mapping), Pattern 3 (187-200 week bar), Pattern 4 (203-235 full card), Code Examples (354-389 variant select + clean-vs-busy severity + fixture test)
    - 03-UI-SPEC.md Copywriting Contract (verdict table 150-161, row copy table 165-174, week-bar 176-184), Widget Hierarchy (36-59), Accessibility (HTML-escape rule line 140)
    - design/chat-card-mockups.html (the binding visual contract — match the two-open, clean, and overbooked scenarios)
  </read_first>
  <behavior>
    - renderTemplate(busy two-open report, ctx) deepStrictEquals fixtures/two-open.json: header avatar+title+subtitle, verdict "Two designers have open time tomorrow." (no name), three decoratedText rows (Anisha 🔴 open + ⚠️ tentative-on-top line, Ella 🔴 open + 📄 brief line, Liam 🟢 full day), Open-in-Productive button with date=targetDay, week-bar section.
    - renderTemplate(clean report, ctx) deepStrictEquals fixtures/clean.json: verdict "All sorted for tomorrow." + locked status line "Three designers fully booked. Nothing to action.", NO decoratedText rows, button + week bar present.
    - renderTemplate(one-overbooked report, ctx) deepStrictEquals fixtures/overbooked.json: 🟠 row "{X.X}h over" in #b06000.
    - Tentative line is exactly `⚠️ {X.X}h tentative (on top) · {Client}` (NO job code); brief line exactly `📄 {label} · {CODE} · {X}h` with label ∈ {No brief, Brief empty, Not briefed} mapped from BriefFlag.reason (no-task→No brief, blank-brief→Brief empty, not-briefed→Not briefed).
    - A client name containing `&`/`<`/`>` is HTML-escaped in the rendered text.
    - Every hour string is derived from report `*Hours` fields — verify no `Min`-field arithmetic in render source.
  </behavior>
  <action>
    Write the three fixtures FIRST (`fixtures/two-open.json`, `clean.json`, `overbooked.json`) as the locked expected Cards v2 JSON, hand-built to match design/chat-card-mockups.html + 03-RESEARCH Pattern 4, then the test (`renderMessage.test.ts`) that builds a matching `StudioReport`+`RenderContext` per scenario and asserts `assert.deepStrictEqual(renderTemplate(report, ctx), JSON.parse(readFileSync(new URL("./fixtures/<name>.json", import.meta.url))))`. Run the test, confirm RED. Then implement the pure modules to GREEN:
    - `verdict.ts`: a function mapping the studio situation (count of underbooked/overbooked designers + brief-flag count) to the locked nameless verdict string from the UI-SPEC verdict table. NEVER interpolate a name (D-12). Leave-driven scenarios resolve to "All sorted for tomorrow." per D-13 final bullet (leave never drives the verdict).
    - `rows.ts`: maps one `DesignerResult` (+ its `BriefFlag`s + tentative info from ctx) to one `decoratedText`. Build ALL text in the single `text` field with `<br>` separators (D-09) — NEVER topLabel/bottomLabel. Line 1 `{emoji} <b>{escapedName}</b> — <font color="{stateHex}">{statusText}</font>`; line 2 greyed booked/open detail; nested `⚠️`/`📄` lines each on their own `<br>`. Read display fields ONLY (`designer.openHours`/`bookedHours`/`availableHours`) — never `openMin`/`confirmedMin`. HTML-escape every dynamic string (client name, job code, designer name) before insertion. Emoji + colour map locked per UI-SPEC.
    - `weekBar.ts`: dot-bar formatter — fixed 10 dots, `filled = round(bookedMin / totalMin * 10)` where `bookedMin = rollup.totalMin - rollup.openMin` (the ONLY place exact `*Min` is read, and it is display-only); render filled `●` as default ink + empty `●` wrapped in `<font color="#c9ccd1">`; caption `<font color="#5f6368">{rollup.totalHours - ... }</font>` — caption uses pre-rounded `rollup` hours: `"{bookedHours}h booked · {openHours}h open"` where bookedHours is rollup.totalHours minus rollup.openHours expressed via the already-rounded values (do not introduce new rounding; if exact booked-hours display is needed, surface it from rollup minutes through the existing round helper only at this display edge — confirm the simplest correct form against the mockup caption).
    - `variants.ts`: ordered cascade returning `"holiday" | "closure" | "degraded" | "card"` per 03-RESEARCH Code Examples (holiday → closure → degraded → card); this plan implements only the `"card"` path (busy vs clean by severity) — the other variants are stubbed to a clear `throw new Error("variant X handled in plan 02")` placeholder OR an explicit "not yet" branch, since plan 02 owns them. Clean-vs-busy: clean when no designer is underbooked/overbooked AND no brief flags → omit the rows section.
    - `renderMessage.ts`: `export const renderTemplate: RenderMessage` destructures report+ctx, calls selectVariant, and for `"card"` assembles header → verdict section → (rows section if busy) → button section → week-bar section, returning one `CardsV2Payload`. Pure, no I/O, never throws on well-formed input. Build the deep-link URL by substituting `report.targetDay` into `PRODUCTIVE_DEEPLINK_TEMPLATE`.
  </action>
  <verify>
    <automated>cd "$REPO" && node --import tsx --test "src/render/__tests__/renderMessage.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx --test "src/render/__tests__/renderMessage.test.ts"` passes (two-open, clean, overbooked fixtures all deepStrictEqual)
    - Trust gate — renderer reads no exact-minute fields except the documented dot-count line: `grep -rn "confirmedMin\|openMin\|availableMin\|tentativeMin" src/render/*.ts | grep -v weekBar.ts | grep -v -E '^\s*\*'` returns no production-code matches (only weekBar's documented `rollup.*Min` dot-count is permitted)
    - Verdict is nameless: the clean + two-open fixtures' verdict textParagraph contains none of "Anisha"/"Ella"/"Liam" (grep the fixture verdict widget)
    - Tentative line literal present in two-open fixture: `grep -q "tentative (on top)" src/render/__tests__/fixtures/two-open.json`
    - Brief line label present: `grep -qE "No brief|Brief empty|Not briefed" src/render/__tests__/fixtures/two-open.json`
    - Clean fixture has no decoratedText widget: `grep -c "decoratedText" src/render/__tests__/fixtures/clean.json` equals 0
    - Full suite stays green: `npm test`
  </acceptance_criteria>
  <done>renderTemplate produces the locked busy, clean, and overbooked Cards v2 payloads; all three fixtures pass deepStrictEqual; the renderer reads only display hour fields (no recomputation); the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report/gather output → renderer | Trusted typed input; dynamic strings (client names, job codes) originate upstream and are interpolated into HTML card text here |
| renderer → card JSON | The rendered text is both HTML (Chat subset) and JSON-serialised; unescaped dynamic strings could break a row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | rows.ts dynamic text (client name / job code) | mitigate | HTML-escape `&`→`&amp;` `<`→`&lt;` `>`→`&gt;` for every dynamic string before inserting into `text` (03-RESEARCH Pitfall 3 / V5). Covered by the `&/<` escape behavior test. |
| T-03-02 | Information Disclosure | renderer accidentally recomputing/exposing wrong figures | mitigate | Renderer reads only `*Hours` display fields (trust grep gate); the team's trust depends on exact numbers (CLAUDE.md). |
| T-03-03 | Spoofing | avatar imageUrl pointing at attacker-controlled host | accept | URL is a hardcoded committed constant in config.ts, not user-supplied (03-RESEARCH Pitfall 4 mitigation). |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan (03-RESEARCH Package Legitimacy Audit = N/A). No install task present. |
</threat_model>

<verification>
- `npm test` green (existing Phase 1/2 trust tests must stay green).
- `npx tsc --noEmit` clean.
- Visual spot-check of the three fixtures against design/chat-card-mockups.html (the binding contract).
</verification>

<success_criteria>
- A busy `StudioReport` renders to the locked Cards v2 payload (header → verdict → rows → button → week bar) — MSG-01, MSG-02.
- The verdict never names a person; rows name designers, never PMs — MSG-03.
- The deep-link button carries `report.targetDay` — MSG-06.
- Tentative time is shown as a distinct "(on top)" line, never folded into booked/open — MSG-07.
- The renderer performs no hour arithmetic (reads `*Hours`) — CLAUDE.md trust constraint.
</success_criteria>

<output>
Create `.planning/phases/03-template-renderer-chat-delivery/03-01-SUMMARY.md` when done.
</output>
