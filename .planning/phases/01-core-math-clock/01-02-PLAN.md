---
phase: 01-core-math-clock
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/domain/round.ts
  - src/domain/capacity.ts
  - src/domain/__tests__/round.test.ts
  - src/domain/__tests__/capacity.test.ts
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, CAP-04]
must_haves:
  truths:
    - "Each designer's available hours = 7.5h minus that day's absence, floored at 0"
    - "A fully-off designer (available 0) is classified 'off', not underbooked, but still represented"
    - "Booked hours separate confirmed from tentative; the gap uses confirmed only"
    - "Any confirmed gap below available flags the designer underbooked with open hours stated"
    - "Tentative bookings are tracked and surfaced as a distinct 'shaky' flag"
    - "A designer over capacity is surfaced as a gentle distinct 'overbooked' signal, with booked math unclamped"
    - "Displayed hour figures round to the nearest 0.25h; internal math stays in exact minutes"
  artifacts:
    - path: "src/domain/round.ts"
      provides: "minutesToHours + roundToQuarterHour (documented round-half-up, display-only)"
      exports: ["minutesToHours", "roundToQuarterHour"]
    - path: "src/domain/capacity.ts"
      provides: "availableMinutes, bookedMinutes, classifyDay, per-designer computeDesignerDay"
      exports: ["availableMinutes", "bookedMinutes", "classifyDay"]
      min_lines: 30
    - path: "src/domain/__tests__/capacity.test.ts"
      provides: "off/underbooked/overbooked/ok + shaky + zero-bookings tests"
      contains: "underbooked"
  key_links:
    - from: "src/domain/capacity.ts"
      to: "src/domain/types.ts"
      via: "import Booking, Absence, DesignerId, TARGET_MINUTES"
      pattern: "from \"\\.\\./types"
    - from: "src/domain/capacity.ts"
      to: "src/domain/round.ts"
      via: "minutesToHours / roundToQuarterHour for surfaced figures only"
      pattern: "roundToQuarterHour"
---

<objective>
Deliver the per-designer **capacity** capability slice: from typed bookings and absences for the target day, compute each designer's available / confirmed-booked / tentative / open figures in exact minutes, then classify the day as off / underbooked / overbooked / ok with an orthogonal shaky flag. Surfaced hour figures round to 0.25h; all arithmetic stays in integer minutes.

Purpose: This is the trust-critical arithmetic the whole tool exists to make exact. It encodes decisions D-01..D-06, D-15, D-16, D-17 precisely so the team can trust every number.
Output: `src/domain/round.ts`, `src/domain/capacity.ts`, and their test files. CAP-01..CAP-04 satisfied and fully unit-tested.
</objective>

<phase_goal>
**As a** project manager reading the evening check-in, **I want to** see each designer's exact available, booked, and open hours for tomorrow with tentative time flagged as shaky, **so that** I can trust the numbers and fix thin or over-booked days before the designers walk in.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-math-clock/01-CONTEXT.md
@.planning/phases/01-core-math-clock/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Consumes the contracts created in plan 01-01 (src/domain/types.ts). -->
<!-- Do NOT re-create types here; import from ../types. -->

From src/domain/types.ts (created in 01-01):
- `DesignerId`, `Booking { designerId; minutes; isTentative }`, `Absence { designerId; minutes }`
- `TARGET_MINUTES = 450` (7.5h × 60)

This plan ADDS the per-designer result contract (planner discretion on exact shape; document it). Recommended:
- `DayStatus = "off" | "underbooked" | "overbooked" | "ok"`  (RESEARCH Pattern 5)
- `DesignerResult` — { designerId; availableMin; confirmedMin; tentativeMin; openMin; status: DayStatus; shaky: boolean; availableHours; bookedHours; openHours }  where the *Min fields are exact minutes and the *Hours fields are the 0.25h-rounded display figures.
Plan 01-03 will import this `DesignerResult` shape to assemble the StudioReport — keep it exported and stable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Quarter-hour rounding helper (TDD: tests first)</name>
  <files>src/domain/round.ts, src/domain/__tests__/round.test.ts</files>
  <read_first>
    - .planning/phases/01-core-math-clock/01-CONTEXT.md (D-15 exact minutes, D-16 round to 0.25h display-only; Discretion: pick + document the rounding mode)
    - .planning/phases/01-core-math-clock/01-RESEARCH.md (§Code Examples "Quarter-hour rounding", §Common Pitfalls 3 floating-point)
  </read_first>
  <behavior>
    - minutesToHours(450) === 7.5 ; minutesToHours(0) === 0 ; minutesToHours(465) === 7.75.
    - roundToQuarterHour(6.40) === 6.5 (round-half-up at 0.25 granularity).
    - roundToQuarterHour(6.10) === 6.0.
    - roundToQuarterHour(7.125) === 7.25 (the half-quarter rounds up).
    - roundToQuarterHour(7.5) === 7.5 (exact quarter unchanged).
    - roundToQuarterHour(minutesToHours(384)) === 6.5 (384min = 6.4h → 6.5); confirms the helper composes with minutesToHours.
  </behavior>
  <action>
    Write `src/domain/__tests__/round.test.ts` FIRST (RED) covering every `<behavior>` case with `node:test` + `node:assert/strict`. Run `npm test`, confirm failure.
    Then write `src/domain/round.ts` (GREEN) exporting:
    - `minutesToHours(minutes: number): number` → `minutes / 60` (exact division; no rounding here — this is the unrounded decimal hours).
    - `roundToQuarterHour(decimalHours: number): number` → `Math.round(decimalHours * 4 + 1e-9) / 4` (round-half-up via a tiny epsilon nudge so exact-half cases round up deterministically; RESEARCH Code Examples). Add a header comment documenting the chosen mode (round-half-up, DISPLAY-ONLY — the rounded value is NEVER fed back into arithmetic per D-16).
    Run `npm test`, confirm round tests pass. Keep this helper as the single source of truth for rounding (RESEARCH "Don't Hand-Roll") — no inline `Math.round(h*4)/4` anywhere else.
  </action>
  <verify>
    <automated>node --import tsx --test "src/domain/__tests__/round.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/round.ts` exports `minutesToHours` and `roundToQuarterHour`.
    - `node --import tsx --test "src/domain/__tests__/round.test.ts"` exits 0.
    - `roundToQuarterHour(6.4) === 6.5`, `roundToQuarterHour(6.1) === 6.0`, `roundToQuarterHour(7.125) === 7.25`, `roundToQuarterHour(7.5) === 7.5` all asserted.
    - `round.ts` header comment documents the rounding mode as round-half-up, display-only.
  </acceptance_criteria>
  <done>A single documented quarter-hour rounding helper exists and is tested; it converts exact minutes to display hours without corrupting internal precision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-designer capacity + classification (TDD: tests first)</name>
  <files>src/domain/capacity.ts, src/domain/__tests__/capacity.test.ts</files>
  <read_first>
    - src/domain/types.ts (Booking, Absence, DesignerId, TARGET_MINUTES from 01-01)
    - src/domain/round.ts (Task 1)
    - .planning/phases/01-core-math-clock/01-RESEARCH.md (§Patterns 3/4/5, §Common Pitfalls 3/5, §Architectural Responsibility Map)
    - .planning/phases/01-core-math-clock/01-CONTEXT.md (D-01..D-06, D-15, D-16, D-17)
  </read_first>
  <behavior>
    - availableMinutes(0) === 450 (no absence → full day, per CAP-01 + D-02; TARGET_MINUTES=450).
    - availableMinutes(120) === 330 (2h absence → 5.5h available).
    - availableMinutes(450) === 0 ; availableMinutes(600) === 0 (over-full absence floored at 0, D-02).
    - bookedMinutes([{minutes:300,isTentative:false},{minutes:120,isTentative:true}]) === { confirmed: 300, tentative: 120 } (D-04/D-05 separation).
    - bookedMinutes([]) === { confirmed: 0, tentative: 0 }.
    - classifyDay(availableMin:0, confirmedMin:0) → status "off" (D-01 — available 0 is off, NOT underbooked, even with 0 booked).
    - classifyDay(450, 420) → status "underbooked", openMin 30 (any gap flags, D-03; open uses confirmed only, D-04).
    - classifyDay(450, 0) → status "underbooked", openMin 450 (zero-bookings = full available open, D-17).
    - classifyDay(450, 450) → status "ok", openMin 0.
    - classifyDay(450, 480) → status "overbooked", openMin -30 (booked over available; unclamped real minutes, D-06; NOT clamped to 0).
    - A designer with confirmed 0 + tentative 300 on a 450 day → status "underbooked" AND shaky true (tentative never closes the gap; D-05). open stays 450.
    - shaky === (tentativeMin > 0), orthogonal to status (an "ok" or "overbooked" day can also be shaky).
    - Display rounding: a designer with availableMin 384 (6.4h) surfaces availableHours 6.5 via roundToQuarterHour, while availableMin stays exactly 384 (D-16 — internal exact, display rounded).
  </behavior>
  <action>
    Write `src/domain/__tests__/capacity.test.ts` FIRST (RED) covering every `<behavior>` case. Run `npm test`, confirm failure.
    Then write `src/domain/capacity.ts` (GREEN), importing types from `../types.ts` and rounding helpers from `./round.ts`. Export:
    - `availableMinutes(absenceMinutesForDay: number): number` → `Math.max(0, TARGET_MINUTES - absenceMinutesForDay)` (D-02 floor). Treat a non-finite input defensively as 0 absence (RESEARCH Security Domain — never let NaN reach a surfaced figure; D-19) rather than throwing.
    - `bookedMinutes(bookings: ReadonlyArray<Booking>): { confirmed: number; tentative: number }` → sum minutes split by `isTentative` (RESEARCH Pattern 4). Coerce non-finite booking minutes to 0 defensively.
    - `classifyDay(availableMin: number, confirmedMin: number): { status: DayStatus; openMin: number }` → openMin = availableMin − confirmedMin (confirmed only, D-04). Order: if availableMin === 0 → "off" (D-01); else if confirmedMin > availableMin → "overbooked" (D-06, no clamping); else if openMin > 0 → "underbooked" (D-03/D-17); else → "ok" (RESEARCH Pattern 5 exact ordering).
    - A `computeDesignerDay(designerId, bookings, absenceMinutes)` (or similarly named) composing the above into the `DesignerResult` from `<interfaces>`: shaky = tentative > 0; *Min fields exact; *Hours fields = `roundToQuarterHour(minutesToHours(min))` (display-only, D-16). NEVER round before arithmetic (RESEARCH Pitfall 3).
    Do NOT import any Productive/framework type — consume the abstracted `isTentative` boolean only (RESEARCH Pitfall 5). NEVER throw on odd input (D-19).
    Run `npm test`, confirm all capacity tests pass.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/capacity.ts` exports `availableMinutes`, `bookedMinutes`, `classifyDay`, and a per-designer compute function producing `DesignerResult`.
    - `node --import tsx --test "src/domain/__tests__/capacity.test.ts"` exits 0.
    - `availableMinutes(450) === 0` and `availableMinutes(120) === 330` asserted (CAP-01).
    - `classifyDay(450, 420)` returns `{ status: "underbooked", openMin: 30 }` and `classifyDay(450, 0)` returns `openMin: 450` (CAP-02/CAP-03/D-17) asserted.
    - `classifyDay(0, 0).status === "off"` and `classifyDay(450, 480).status === "overbooked"` with `openMin === -30` (unclamped) asserted (D-01/D-06).
    - A confirmed-0 + tentative-300 designer is `underbooked` with `shaky === true` and `openMin === 450` (CAP-04/D-05) asserted.
    - `capacity.ts` contains NO `DateTime.now(`, NO `new Date(`, and NO inline `Math.round(*4)/4` (rounding goes through round.ts) — verifiable: `grep -v '^[[:space:]]*//' src/domain/capacity.ts | grep -c 'DateTime.now(\|new Date(\|\* *4 *) */ *4'` returns 0.
    - `capacity.ts` does not import the word `draft` or any Productive response type (RESEARCH Pitfall 5) — verifiable: `grep -ci 'draft\|productive' src/domain/capacity.ts` returns 0.
  </acceptance_criteria>
  <done>Per-designer available/confirmed/tentative/open are computed in exact minutes, classified into off/underbooked/overbooked/ok with an orthogonal shaky flag, and surfaced as 0.25h-rounded display hours — all framework-agnostic, non-throwing, and fully tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in-phase) | Pure arithmetic over already-typed inputs. No network, secrets, persistence, or untrusted string parsing. zod boundary validation is Phase 2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering (robustness) | capacity arithmetic | mitigate | Compute in exact integer minutes; coerce non-finite minute inputs to 0 so NaN/Infinity never reaches a surfaced figure (RESEARCH Security Domain / Pitfall 3; D-19). Round only at the display edge. |
| T-01-04 | Denial of trust | tentative vs confirmed mixing | mitigate | `open` uses confirmed minutes only; tentative tracked separately and surfaced as shaky (D-04/D-05) — prevents a disappearing-tentative booking from silently hiding a real gap. Asserted by the confirmed-0/tentative-300 test. |
| T-01-05 | Tampering | overbooked clamping | accept | Intentionally NOT clamping booked over available preserves the early-warning signal (D-06); negative openMin is a deliberate, tested signal, not a defect. |

No high-severity threats exist in this pure-arithmetic plan; block-on-high satisfied trivially.
</threat_model>

<verification>
- `npm test` exits 0 with round + capacity suites green.
- The off / underbooked / overbooked / ok / shaky / zero-bookings cases are each asserted with concrete values.
- `grep` confirms no system-clock, no native Date, no inline quarter-hour rounding, no Productive-type leakage in capacity.ts.
</verification>

<success_criteria>
- CAP-01: per-designer available hours = 7.5h − absence, floored at 0 — tested.
- CAP-02: confirmed booked hours computed and separated from tentative — tested.
- CAP-03: underbooked flags name the open hours via confirmed-only gap — tested.
- CAP-04: tentative tracked and surfaced as distinct shaky — tested.
- D-06 overbooked + D-16 display rounding + D-17 zero-bookings all encoded and tested.
</success_criteria>

<output>
Create `.planning/phases/01-core-math-clock/01-02-SUMMARY.md` when done.
</output>
