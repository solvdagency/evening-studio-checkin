---
phase: 01-core-math-clock
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .prettierrc.json
  - .gitignore
  - src/domain/types.ts
  - src/domain/clock.ts
  - src/domain/__tests__/clock.test.ts
autonomous: true
requirements: [SCHED-03, SCHED-04]
must_haves:
  truths:
    - "Given an injected studio-zone `now`, the core returns the correct next working day"
    - "A Friday run targets the following Monday (weekend skipped)"
    - "A holiday on the would-be target day is skipped to the next working day"
    - "Target-day derivation is correct across the Sydney DST boundary"
    - "The clock never reads the system clock — `now` is always injected"
  artifacts:
    - path: "package.json"
      provides: "Locked-stack project scaffold + `npm test` script"
      contains: '"node --import tsx --test"'
    - path: "tsconfig.json"
      provides: "Strict TypeScript config for ESM + tsx"
    - path: "src/domain/types.ts"
      provides: "Shared domain types consumed by all later plans"
      contains: "DesignerId"
    - path: "src/domain/clock.ts"
      provides: "nextWorkingDay, isWorkingDay, restOfWeekWindow (pure, luxon)"
      exports: ["nextWorkingDay", "isWorkingDay", "restOfWeekWindow"]
      min_lines: 30
    - path: "src/domain/__tests__/clock.test.ts"
      provides: "Friday→Monday, holiday-eve, and DST-boundary tests"
      contains: "2026-04-06"
  key_links:
    - from: "src/domain/clock.ts"
      to: "luxon DateTime"
      via: "import { DateTime } from 'luxon'"
      pattern: "from \"luxon\""
    - from: "src/domain/clock.ts"
      to: "injected now (no system clock)"
      via: "now passed as a parameter; DateTime.now() never called"
      pattern: "nextWorkingDay\\(now"
---

<objective>
Stand up the locked-stack project scaffold and deliver the first complete capability slice: the working-day **clock**. Given an injected studio-zone `now` and an injected holiday set, the clock derives the correct target working day (skipping weekends and holidays) and enumerates the rest-of-week window. This is the keystone of SCHED-03 / SCHED-04 and the dependency root for the capacity rollup in plan 03.

Purpose: Establish the pure-function trust boundary's date layer — deterministic, DST-safe, system-clock-free — plus the project scaffold every later plan builds on.
Output: `package.json`, `tsconfig.json`, `.prettierrc.json`, `.gitignore`, `src/domain/types.ts`, `src/domain/clock.ts`, `src/domain/__tests__/clock.test.ts`. All three mandatory date tests (Friday→Monday, holiday-eve, DST-boundary) pass under `node --import tsx --test`.
</objective>

<phase_goal>
**As a** the evening check-in automation, **I want to** derive the correct next working day from the studio timezone (skipping weekends and holidays, DST-safe), **so that** every nightly report is about the right day regardless of when or where the scheduler runs.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-math-clock/01-CONTEXT.md
@.planning/phases/01-core-math-clock/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- This plan CREATES the contracts later plans consume. Define them first (Task 1). -->
<!-- Recommended shapes (planner discretion per D-CONTEXT; keep framework-agnostic). -->

src/domain/types.ts is expected to export at minimum:
- `DesignerId` — a branded/string id for a monitored designer.
- `Booking` — { designerId: DesignerId; minutes: number; isTentative: boolean }  (per-target-day minutes; isTentative abstracts Productive `draft` per RESEARCH A2/Pitfall 5).
- `Absence` — { designerId: DesignerId; minutes: number }  (per-target-day absence minutes; abstracts Productive booking_type=event per RESEARCH A3).
- `WorkingDay` — a luxon DateTime anchored to startOf("day") in the studio zone (or a thin wrapper; planner's choice — document it).
- Re-export or define `STUDIO_ZONE = "Australia/Sydney"` and `TARGET_MINUTES = 450` as named constants here OR in a thin `src/config.ts`; if in config.ts keep clock/capacity functions parameterised, never reaching into a global (RESEARCH "Recommended Project Structure").

Holidays are represented as `ReadonlySet<string>` of `"yyyy-MM-dd"` studio-zone date keys (RESEARCH Pattern 1 — NOT Set<DateTime>).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the locked-stack project and define shared domain types</name>
  <files>package.json, tsconfig.json, .prettierrc.json, .gitignore, src/domain/types.ts</files>
  <read_first>
    - CLAUDE.md (§Technology Stack, §Installation, "What NOT to Use")
    - .planning/phases/01-core-math-clock/01-RESEARCH.md (§Standard Stack, §Installation, §Recommended Project Structure, §Code Examples `node:test` structure + package.json scripts)
    - .planning/phases/01-core-math-clock/01-CONTEXT.md (§Claude's Discretion — type shape is planner's call)
  </read_first>
  <action>
    Run `npm init -y`, then install the locked stack EXACTLY: `npm install luxon` and `npm install -D @types/luxon typescript@~5.9.3 tsx prettier`. CRITICAL: pin `typescript@~5.9.3` — npm `latest` is now 6.x which violates CLAUDE.md's locked 5.x constraint (RESEARCH §State of the Art). Do NOT install zod, date-holidays, googleapis, axios, or any test framework — `node:test` is built in.
    Set `"type": "module"` in package.json. Add scripts: `"test": "node --import tsx --test \"src/**/*.test.ts\""` and `"format": "prettier --write ."`.
    Write `tsconfig.json` for strict ESM TypeScript targeting Node 22: `"strict": true`, `"module": "nodenext"`, `"moduleResolution": "nodenext"`, `"target": "es2023"`, `"verbatimModuleSyntax": true`, `"noEmit": true` (tsx runs directly, no build step per CLAUDE.md). Write a minimal `.prettierrc.json`. Write `.gitignore` containing at least `node_modules`, `.env`, and any editor cruft.
    Create `src/domain/types.ts` defining the shared domain contract per the `<interfaces>` block above: `DesignerId`, `Booking`, `Absence`, `WorkingDay`, and the `STUDIO_ZONE`/`TARGET_MINUTES` constants (in types.ts or a thin src/config.ts — document the choice in a header comment). Keep functions parameterised; no global-config reach-ins. Define the holiday representation as `ReadonlySet<string>` of `yyyy-MM-dd` keys. These types are the contracts plans 02 and 03 implement against — no implementation logic here.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.type!=='module') throw new Error('not ESM'); if(!p.scripts.test.includes('node --import tsx --test')) throw new Error('bad test script'); const tsv=(p.devDependencies.typescript||''); if(!/[~^]?5\./.test(tsv)) throw new Error('typescript not pinned to 5.x: '+tsv); if(p.dependencies.zod||p.devDependencies.zod) throw new Error('zod must not be in phase 1'); console.log('scaffold OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` has `"type": "module"`, a `test` script equal to `node --import tsx --test "src/**/*.test.ts"`, `luxon` in dependencies, and `@types/luxon`, `typescript` (matching `5.x`, NOT 6.x), `tsx`, `prettier` in devDependencies.
    - `package.json` contains NO `zod`, `date-holidays`, `googleapis`, `axios`, `jest`, or `vitest`.
    - `tsconfig.json` has `"strict": true` and `"noEmit": true`.
    - `src/domain/types.ts` exports `DesignerId`, `Booking`, `Absence`, `WorkingDay`, and exposes `STUDIO_ZONE === "Australia/Sydney"` and `TARGET_MINUTES === 450` (here or in src/config.ts).
    - `grep -n "from \"luxon\"\|from 'luxon'" src/domain/types.ts` may be empty (types need not import luxon) — but if `WorkingDay` aliases `DateTime`, the import is present.
    - The verify command prints `scaffold OK` (exit 0).
  </acceptance_criteria>
  <done>Project installs cleanly with only the four locked packages; `npm test` script is wired; shared domain types exist and are importable by later plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the working-day clock (TDD: tests first)</name>
  <files>src/domain/clock.ts, src/domain/__tests__/clock.test.ts</files>
  <read_first>
    - src/domain/types.ts (the contracts from Task 1)
    - .planning/phases/01-core-math-clock/01-RESEARCH.md (§Architecture Pattern 1 inject-clock + holiday Set<string>, Pattern 2 Friday→Monday, Pattern 6 restOfWeekWindow, §Common Pitfalls 1/2/4, §Code Examples node:test idiom)
    - .planning/phases/01-core-math-clock/01-CONTEXT.md (D-07, D-08, D-10, D-11, D-12, D-13)
  </read_first>
  <behavior>
    - nextWorkingDay(Fri 2026-06-05T16:30 Sydney, new Set()) → DateTime whose toISODate() === "2026-06-08" (Monday). [Friday→Monday, SCHED-03/D-12 — mandatory test]
    - nextWorkingDay(Tue 2026-06-09T16:30 Sydney, new Set(["2026-06-10"])) → toISODate() === "2026-06-11" (Thursday; Wed 06-10 is the injected holiday, skipped). [holiday-eve, D-13 — mandatory test]
    - nextWorkingDay(Fri 2026-04-03T16:30 Sydney, new Set()) → toISODate() === "2026-04-06" (Monday). The window skips Sat 04-04 and Sun 04-05, the actual 2026 Sydney DST-end changeover day (AEDT→AEST). [DST-boundary, SCHED-04 — mandatory test]
    - isWorkingDay(Sat or Sun, anyHolidays) === false; isWorkingDay(a weekday present in holidays) === false; isWorkingDay(a plain weekday, new Set()) === true.
    - restOfWeekWindow(Tue 2026-06-09 startOf day, new Set()) → working days [Tue 06-09, Wed 06-10, Thu 06-11, Fri 06-12] (Tue-run covers itself through Friday per D-07). Length 4.
    - restOfWeekWindow(Mon 2026-06-08 startOf day, new Set()) → [Mon, Tue, Wed, Thu, Fri] length 5 (Friday-run rollover lands on Monday → whole week, D-08).
    - restOfWeekWindow(Tue 2026-06-09 startOf day, new Set(["2026-06-11"])) → excludes Thu 06-11 (holiday contributes 0, not a working day, D-10) → length 3.
  </behavior>
  <action>
    Write `src/domain/__tests__/clock.test.ts` FIRST (RED), using `describe`/`it` from `node:test` and `assert` from `node:assert/strict`, constructing every `now` via `DateTime.fromISO("...", { zone: "Australia/Sydney" })` (RESEARCH Code Examples). Cover every case in `<behavior>` above. The three mandatory cases (Friday→Monday, holiday-eve, DST-boundary at 2026-04-03→2026-04-06) MUST each be a distinct named `it(...)`. Run `npm test` and confirm the suite FAILS (clock.ts not yet implemented).
    Then write `src/domain/clock.ts` (GREEN) exporting:
    - `nextWorkingDay(now: DateTime, holidays: ReadonlySet<string>): DateTime` — `let day = now.startOf("day")`; do `day = day.plus({ days: 1 })` while `!isWorkingDay(day, holidays)`. Single skip loop — NO special-case Friday branch (RESEARCH Pattern 2). Friday→Monday emerges naturally; a holiday on the would-be target continues the loop.
    - `isWorkingDay(day: DateTime, holidays: ReadonlySet<string>): boolean` — false if `day.weekday === 6 || day.weekday === 7` (Sat/Sun, luxon ISO weekday); else `const key = day.toISODate(); return key !== null && !holidays.has(key)`.
    - `restOfWeekWindow(targetDay: DateTime, holidays: ReadonlySet<string>): DateTime[]` — `const friday = targetDay.plus({ days: 5 - targetDay.weekday })`; iterate `d = targetDay` while `d <= friday` stepping `d.plus({ days: 1 })`, pushing only `isWorkingDay(d, holidays)` days (RESEARCH Pattern 6). The one-liner handles both the Tue-run and the Monday-from-Friday-rollover cases with no branch.
    Use luxon `plus({ days })` / `startOf("day")` / `.weekday` / `.toISODate()` exclusively. NEVER call `DateTime.now()` and NEVER use native `Date` (CLAUDE.md "What NOT to Use", RESEARCH Pitfalls 1/4). Run `npm test` and confirm all clock tests PASS.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20; node --import tsx --test "src/domain/__tests__/clock.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/clock.ts` exports `nextWorkingDay`, `isWorkingDay`, `restOfWeekWindow`.
    - `node --import tsx --test "src/domain/__tests__/clock.test.ts"` exits 0 (all tests pass).
    - The test file contains three distinct `it(...)` cases asserting `toISODate()` equals `"2026-06-08"` (Friday→Monday), `"2026-06-11"` (holiday-eve), and `"2026-04-06"` (DST-boundary) respectively — verifiable: `grep -c '2026-06-08\|2026-06-11\|2026-04-06' src/domain/__tests__/clock.test.ts` returns 3 or more.
    - `clock.ts` contains NO `DateTime.now(` and NO `new Date(` — verifiable: `grep -v '^[[:space:]]*//' src/domain/clock.ts | grep -c 'DateTime.now(\|new Date('` returns 0.
    - `clock.ts` imports `DateTime` from `luxon` — `grep -c 'from "luxon"\|from '"'"'luxon'"'"'' src/domain/clock.ts` >= 1.
  </acceptance_criteria>
  <done>nextWorkingDay/isWorkingDay/restOfWeekWindow are implemented as pure luxon functions; the three mandatory date tests and all window tests pass; no system-clock or native-Date usage anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in-phase) | Phase 1 is pure in-memory functions over already-typed inputs. No network, no secrets, no persistence, no untrusted runtime string parsing. The type-level input-validation boundary is deferred to Phase 2 (zod). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering (robustness) | clock.ts date math | mitigate | Use luxon calendar-day math exclusively (`plus({days})`, `startOf("day")`, `.toISODate()`); never native `Date` or ms arithmetic — prevents DST-boundary off-by-one (RESEARCH Pitfall 1). Asserted by the 2026-04-03 DST test. |
| T-01-02 | Information disclosure / Denial of trust | injected `now` | accept | Determinism guaranteed by injecting `now` rather than reading the system clock; no real-world secret or PII flows through the clock. No high-severity threat. |
| T-01-SC | Tampering | npm installs (luxon, tsx, typescript, @types/luxon) | accept | RESEARCH §Package Legitimacy Audit dispositioned all four as Approved (years old, official org repos — Moment/Microsoft/DefinitelyTyped/privatenumber, no postinstall scripts). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking-human checkpoint required. |

No high-severity threats exist in this pure-logic phase; block-on-high is satisfied trivially.
</threat_model>

<verification>
- `npm test` runs the suite via `node --import tsx --test` and exits 0.
- The three mandatory date cases (Friday→Monday, holiday-eve, DST-boundary) are each a named, passing test.
- Only the four locked packages are installed; no zod/date-holidays/test-framework.
- `grep` confirms no `DateTime.now(` and no `new Date(` in clock.ts.
</verification>

<success_criteria>
- Project scaffold installs and `npm test` is wired (Task 1 verify prints `scaffold OK`).
- Shared domain types exist for downstream plans to import.
- `nextWorkingDay`, `isWorkingDay`, `restOfWeekWindow` are pure, DST-safe, system-clock-free, and fully tested.
- Friday→Monday (2026-06-08), holiday-eve (2026-06-11), and DST-boundary (2026-04-06) tests pass — satisfying ROADMAP Phase 1 success criterion 3.
</success_criteria>

<output>
Create `.planning/phases/01-core-math-clock/01-01-SUMMARY.md` when done.
</output>
