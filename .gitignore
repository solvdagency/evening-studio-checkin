# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Test coverage
coverage/
*.lcov

# Secrets / local env — keep .env.example committed as the template
.env
.env.*
!.env.example

# Credentials dropped locally (Google service-account JSON, etc.) — NEVER commit
secrets/
*.sa.json
service-account*.json

# Logs
*.log
npm-debug.log*

# Local screenshots / scratch captures — kept on disk, never committed
screenshots/

# Phase 4 labelling-spike scratch output (real calendar contents — T-04-06) — never commit
spike-output*.md
spike-output*.json

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
